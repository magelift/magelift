# Ordered GCP create-once harness cells (D-02 / Phase 7).
# Offline dry-run loops all with append_row; live iterates after one create (never recreate).
# Teardown is EXIT destroy + force_clean_orphans + PSA soak, not a cell.
#
# bootstrap:wif requires Ensure PLUS Act or gcloud STS/WIF token-exchange proof (not Ensure-only).
# composer:sm-* are explicit SC2 write/read cells (gcp-secret-manager://).
# migrate:dump runs after infra-only creation and before deploy:candidate so a
# fresh managed database has Magento's schema before setup:upgrade (D-03).
# deploy:repeat runs after deploy:candidate against the same lifecycle to prove
# the normal non-interactive update path is idempotent.
# cutover:dns needs applicationURL (D-04).
bootstrap:wif
composer:sm-write
composer:sm-read
day2:secrets
day2:state
day2:logs
day2:exec
day2:health
search:health
migrate:dump
deploy:candidate
deploy:repeat
cost:estimate
# cutover:dns is opt-in. Supply a real operator-owned Cloudflare zone and host
# through a dedicated catalog or MAGELIFT_ACCEPTANCE_CELL_CATALOG.
