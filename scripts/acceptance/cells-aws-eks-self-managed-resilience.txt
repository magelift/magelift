# Ordered self-managed EKS resilience cells. The first cell creates the cold
# stack; the final cell terminates one exact ASG-backed EC2 node and verifies a
# distinct InService/Ready replacement plus runtime health.
computeMode:self-managed
queueMode:database
resilience:node-loss
