## RC1 evidence classes (2026-08-18)

Owned with [`rc1-gcp-first-distribution`](../rc1-gcp-first-distribution/tasks.md).
This change owns the evidence catalog and release matrix. A GCP cell MUST NOT
keep an AWS, Fastly, or OVH checkbox open. Adapter impl stays in
`provider-native-lifecycle-adapters`.

### Remaining split

- [x] `impl` — provider-neutral contracts, fake clients, and harness shape for
  architecture, resilience, observability, edge, and certification scheduler
  (sections 1–2, 6–7 mostly closed).
- [x] `floci` GCS + Floci AWS IAM OIDC — `tests/floci-gcp` GCS smoke and
  `TestIAMOpenIDConnectProviderAgainstFloci`.
- [x] `floci` leftover — Secret Manager / Pub/Sub / Logging / Monitoring on
  digest-pinned floci-gcp 0.7.0 via official Go clients (`make floci-gcp-test`).
  Closed with rc1 2.6. Not Magento. Not KEEP.
- [x] `live.gcp` — packed session 1 Autopilot KEEP `gcap27` destroyed 2026-08-19
  (catalog, Fastly Magento-origin HTTPS, Magento-wired log/metric collection,
  kube failed-deployment, dump-retrieve, Cloudflare DNS, NR Contrib, destroy).
  Standard HA Magento known-content closed 2026-08-20 `gcha36` (`europe-west3`,
  experimental GKE Standard, not public certified). **2026-08-20 `gcap28`:**
  catalog 13/13 PASS, schema-mismatch (epochs 171/172, rollback refused
  `ErrSchemaMismatch`), destroy `assert_clean ok`, sealed redacted JSONL
  [`gcp-gke-autopilot-magento-gcap28-20260820.sealed.jsonl`](../../../docs/evidence/runs/gcp-gke-autopilot-magento-gcap28-20260820.sealed.jsonl).
  Magelift-owned native sink/dashboard/paging typed unsupported. Same-region
  durable-class restores already evidenced stay closed here. Do not reuse
  `gcap28`.
- [x] `live.aws` — packed session 2 ECS Fargate Magento closed by `awf18`
  (create-once + destroy). Do not re-prove Fastly/NR/CF/SendGrid. Remaining
  CloudWatch/CloudFront Magento-origin cells are not RC1-required (GCP owns
  obs/edge for RC1). Cheap Fargate Magento only if 5.1 cannot seal without an
  AWS Magento JSONL row.
- [x] `live.integration` — New Relic Contrib, Fastly Magento-origin HTTPS, and
  Cloudflare DNS-only Magento hostname closed on `gcap27`. SendGrid delivery is
  typed unsupported (preview YAML omits `email`).
- [x] `live.ovh-scw` — infra-only leftovers closed from the 2026-08-19 inventory
  (no new mutation). Magento, HA/DR, OVH OKMS, and Kapsule collection stay
  experimental.
- [x] `gcp.gke` fencing, failback, alternate-region DR, lost-credentials, and
  provider-outage — typed unsupported (3.7, 3.8, and the matching 3.9 rows).

## 1. Reconcile scope and source-of-truth catalogs

- [x] 1.1 Add cross-references from `expanded-rc1-certification`, `fastly-edge-and-paas-migrations`, and `community-provider-extensions` to this change, and document which change owns each remaining deliverable so a narrower passing test cannot close a broader production claim. This change owns the provider-neutral catalog, architecture, resilience, observability, edge composition, scheduler, and evidence gates; `expanded-rc1-certification` owns its release-specific live harness cleanup; `fastly-edge-and-paas-migrations` owns Fastly API lifecycle proof; and `community-provider-extensions` owns the clean-cache extension build proof.
- [x] 1.2 Define the capability-record schema for Adobe compatibility, provider availability, MageLift implementation status, and live evidence, including source URL, retrieval time, provider, region or zone, service major, lifecycle stage, limits, and evidence identity.
- [x] 1.3 Populate source-dated capability records for AWS, GCP, Scaleway, and OVHcloud compute, networking, storage, database, cache, search, queue, secrets, native observability, and native edge products; record `unavailable`, `unsupported`, `blocked`, and `experimental` states explicitly when an official product or adapter is absent.
- [x] 1.4 Add the current Adobe release and provider-version compatibility matrix, including the official Memorystore Valkey 9.0 GA and 9.1 Preview records, and make stale or missing source records fail validation rather than silently selecting a default.
- [x] 1.5 Change the GCP cache selection for Adobe Commerce 2.4.9 to the documented Valkey 9.0 target, retain older release mappings only where the compatibility matrix permits them, and add regression tests for the selected engine/version and unsupported combinations.
- [x] 1.6 Generate an architecture coverage report from the catalog that distinguishes `supported`, `certified`, `experimental`, `unavailable`, `unsupported`, and `blocked`, with the source and reason attached to every non-certified row.

## 2. Define and implement provider architecture profiles

- [x] 2.1 Add a versioned architecture-profile schema and canonical fingerprint covering provider, account or project, region, zones, compute mode, Kubernetes topology, managed versus self-hosted stateful services, service majors, network and ingress, native and external edge, native and external observability, backup/DR profile, Adobe release, artifact, and declared RPO/RTO.
- [x] 2.2 Implement AWS ECS profiles that enumerate the currently supported ECS capacity modes and task/service topology, load balancing, deployment strategy, storage, database, cache, search, queue, object storage, secrets, CloudWatch, and CloudFront/WAF boundaries instead of assuming Fargate is the only ECS architecture.
- [x] 2.3 Implement full AWS EKS profiles covering control-plane ownership, managed node groups and any supported alternative capacity modes, workload scheduling, pod disruption and topology rules, persistent storage, ingress/load balancing, managed stateful services, IAM, CloudWatch, and CloudFront/WAF.
- [x] 2.4 Implement GCP GKE Autopilot and Standard profiles with explicit node/workload/storage/ingress choices, Cloud SQL, Memorystore, object storage, search, queue, Secret Manager, Google Cloud Observability, and Google Cloud load-balancing/CDN/security boundaries.
- [x] 2.5 Implement Scaleway Kapsule profiles with documented cluster, node, storage, database, cache, search, queue, object-storage, secrets, observability, and edge capabilities; do not invent a native equivalent for a product not present in the source-dated catalog.
- [x] 2.6 Implement OVHcloud MKS profiles with documented cluster, node, storage, database, cache, search, queue, object-storage, secrets, observability, and edge capabilities; classify provider gaps before any resource mutation.
- [x] 2.7 Model managed and self-hosted boundaries independently for database, cache, search, queue, media/object storage, ingress, edge, and observability, including ownership, upgrade, backup, and failure-domain differences.
- [x] 2.8 Add pre-mutation profile validation that rejects unsupported service-major, region, topology, credential, network, and integration combinations with stable diagnostics and an actionable capability status.
- [x] 2.8a Add the provider-neutral read-only plan-admission port and public SDK bridge; preserve stable core plan identity and run admission before Pulumi/provider mutation while allowing destroy and cleanup to proceed during capability outages.
- [x] 2.8b Implement OVHcloud region, MKS plan/region, canonical-zone, and single- versus multi-zone admission through the provider capability API before any paid resource is created.
- [x] 2.8c Implement OVHcloud MySQL and Valkey engine/version/plan/flavor/private-network/node-count admission through the current managed-database availability endpoint, with secret-safe errors and no-mutation tests.
- [x] 2.8d Implement equivalent provider-side read-only admission for every first-party advanced catalog whose validity depends on current account, region, quota, network, service-major, or integration availability. AWS ECS/EKS, GCP, Scaleway, and OVHcloud modules expose the shared admission boundary; Fastly and New Relic remain extension-owned and are covered by their credential/capability contracts.
- [x] 2.9 Add provider-neutral contract tests and provider-specific mock tests for every profile family, including output mapping, ownership markers, asynchronous operation handling, and the no-mutation guarantee for rejected profiles.
- [x] 2.10 Make the public SDK the provider-adapter boundary for optional resilience implementations, compile the provider-neutral recovery graph once through `sdk.CompileResiliencePlan`, validate adapter descriptors during module registration, and reject provider-identity mismatches before planning.
- [x] 2.11 Keep first-release edge and observability composition explicit with `nativeProvider` and `externalProvider`; reject the removed singular `edge.provider` and `observability.provider` fields while preserving provider identity on targets and existing resources.
- [x] 2.12 Add optional public SDK lifecycle ports for edge and observability adapters, validate their descriptors and provider identity during module registration, and keep native API/resource shapes out of the core contract.
- [x] 2.13 Add optional planned adapter factories for resilience, native edge, native observability, and certification execution so first-party and community modules inject provider SDK clients after planning while the core retains lifecycle ordering, ownership, evidence, idempotency, and cleanup rules. `sdk/v1.CertificationAdapterFactory` and `internal/platform.CertificationExecutorFor` expose the same boundary without exporting internal scheduler types.
- [x] 2.14 Make AWS `fck-nat` a separately fingerprinted network boundary: validate the source-dated AMI owner/architecture, support explicit single-AZ and multi-AZ topology choices, expose preview versus standard/HA replacement semantics, model stable route targets and ownership, reject a single-instance HA claim, and keep existing-VPC egress reference-only. Live cost/egress/failure-domain and cleanup evidence remains release-gated in section 8.
- [x] 2.15 Implement progressive-disclosure YAML configuration across all provider targets: simple preset defaults, environment overlays with provenance, advanced semantic provider fields (including AWS ECS/EKS managed-service durability and fck-nat, GCP GKE/managed-service durability, Scaleway Kapsule/RDB/Redis durability, and OVH MKS/managed-service durability), strict unknown-field rejection, resolved-plan/fingerprint coverage, secret-safe output, and provider-adapter validation before mutation. Materialize named capacity and durability defaults in the effective configuration (AWS ECS/EKS workload and managed-service shapes; preview Valkey snapshots explicitly disabled, standard/HA snapshots seven days, environment-class database cleanup safety, and fck-nat topology/replacement/ARM64 sizing when selected; GCP standard/HA eight retained backups plus seven transaction-log days and contextual Cloud Armor; Scaleway standard/HA daily seven-day same-region encrypted backups; disposable preview backups explicitly disabled). Do not expose raw SDK argument maps or reintroduce singular provider selectors. Provider-wide live certification remains independently open in the provider and section-8 tasks.

## 3. Build resilience, backup, restore, and disaster-recovery contracts

- [x] 3.1 Add named resilience profiles and policy validation for availability target, RPO, RTO, retention, recovery scope, failover owner, fencing policy, and allowed recovery destinations; refuse plans whose declared target cannot be met by the selected architecture.
- [x] 3.2 Record source of truth, failure domains, backup method, restore method, integrity check, and loss/rebuild semantics independently for the database, media/object data, configuration and secrets, infrastructure state, queue, search index, and cache.
- [x] 3.3 Implement provider-specific backup planning and lifecycle operations for every durable data class, including retention, encryption, immutability or deletion protection where available, ownership, operation polling, and independent success evidence. AWS S3, AWS Secrets Manager, GCP Cloud SQL, GCP Cloud Storage, GCP Secret Manager, GCP Pub/Sub, Scaleway Object Storage, Scaleway Secret Manager, and OVHcloud Object Storage now each have bounded backup/restore/integrity cells with normalized cleanup; the shared `DatabaseRecoveryCell` now carries a scrubbed manifest through provider backup, isolated restore, restore-identity integrity verification, inventory, and fixture cleanup, enforces encryption and application evidence, and accepts missing deletion protection only through an explicit provider capability policy. The AWS RDS/Aurora, GCP Cloud SQL, Scaleway RDB, and OVH Managed Database adapters expose the provider-local lifecycle seams. The 2026-08-13 AWS RDS MySQL cell additionally proves encrypted snapshot/isolated-restore control-plane polling and deletion-protection-safe cleanup with empty direct inventories, but explicitly records `appFixture=not-run`. The 2026-08-13 Scaleway RDB `fr-par` cell `20260813m` likewise passed encrypted block-snapshot backup, isolated restore, polling, and exact cleanup with empty direct inventories, also recording `appFixture=not-run` and `protection=provider-optional`. The 2026-08-13 OVH Managed Database GRA cell `20260813q` likewise passed encrypted provider-backup selection, isolated `forkFrom` restore, polling, and exact cleanup with empty direct inventories, also recording `appFixture=not-run` and `protection=provider-optional`. The 2026-08-17 AWS Aurora MySQL `eu-west-3` cell adds an encrypted cluster snapshot, isolated cluster restore with a restored writer, one known row, restored-identity reads, permissions, health, manifest verification, a 539-second provider restore, member-before-cluster cleanup, and exact independent cleanup. The 2026-08-17 OVH Managed Database GRA application-fixture cell adds provider-managed MySQL PITR, a known row, restored-identity reads, permissions, health, manifest verification, a 295-second provider restore, and exact independent cleanup. The Scaleway Object Storage cell also proves an acceptance-only Object Lock window, exact version purge, and scheduled-free source deletion. The 2026-08-15 GCP Cloud SQL cell adds one live known-row fixture, restore-identity read, permission probe, health probe, manifest verification, and measured provider restore. Live evidence for the remaining database variants, all other durable classes/providers, production retention variants, HA, and DR remains open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 3.4 Create scrubbed, known-content recovery fixtures and restore verification that checks manifests, checksums, record or object counts, application reads, permissions, secret references, service health, and the measured restore duration. Repository fixtures, typed-reference validation, provider verifier evidence, and deterministic tests are implemented; provider live restore evidence remains release-gated in 3.5 and 8.5.
- [x] 3.5 Add same-region restore-in-place and isolated-destination restore drills for AWS, GCP, Scaleway, and OVHcloud, keeping cache reconstruction and search rebuild separate from durable-backup claims. AWS S3 and AWS Secrets Manager plus GCP Cloud SQL, GCP Cloud Storage, GCP Secret Manager, Scaleway Object Storage, Scaleway Secret Manager, and OVHcloud Object Storage now have repeatable same-region isolated-destination evidence; provider-neutral database recovery sequencing and provider control-plane translators are covered by deterministic tests. The 2026-08-15 GCP Cloud SQL cell also verifies a known row through the restored identity, permissions, health, manifest content, and a 937-second provider restore. The 2026-08-16 Scaleway RDB, 2026-08-17 AWS Aurora, and 2026-08-17 OVH Managed Database cells now add application-fixture isolated restores with restored-identity reads, permissions, health, and manifest evidence. The 2026-08-18 GCP Cloud SQL cell restores in place onto the source instance after an operator approval reference (`destination=same-region`, `instances=1`, 919-second restore, known-row reads); see [`gcp-cloudsql-inplace-live-20260818.md`](../../../docs/evidence/gcp-cloudsql-inplace-live-20260818.md). The 2026-08-18 GCP Cloud Storage cell restores in place onto the source prefix after clearing the fixture (`destination=same-region`, three objects); see [`gcp-cloud-storage-inplace-live-20260818.md`](../../../docs/evidence/gcp-cloud-storage-inplace-live-20260818.md). The 2026-08-18 GCP Secret Manager cell restores in place onto the source secret after a corrupt version (`destination=same-region`); see [`gcp-secret-manager-inplace-live-20260818.md`](../../../docs/evidence/gcp-secret-manager-inplace-live-20260818.md). The 2026-08-18 GCP infrastructure-state cell restores in place onto the source prefix after clearing the fixture (`class=infrastructure-state`, three objects); see [`gcp-infrastructure-state-inplace-live-20260818.md`](../../../docs/evidence/gcp-infrastructure-state-inplace-live-20260818.md). The 2026-08-18 GCP audit-evidence cell restores in place onto the source prefix after clearing the fixture (`class=audit-evidence`, three objects); see [`gcp-audit-evidence-inplace-live-20260818.md`](../../../docs/evidence/gcp-audit-evidence-inplace-live-20260818.md). The 2026-08-18 GCP Pub/Sub cell restores onto a new owned subscription without seeking the source (`destination=same-region-isolated`); see [`gcp-pubsub-isolated-live-20260818.md`](../../../docs/evidence/gcp-pubsub-isolated-live-20260818.md). Restore-in-place live drills for remaining AWS classes, HA/DR and retention variants, production Secret Manager variants, and OVH Secret Manager drills remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 3.6 Add HA failure exercises for process/task/pod, node, and zone loss, including topology constraints, traffic health, queue/database behavior, cache loss semantics, recovery duration, and data-integrity checks. The 2026-08-17 GCP GKE Standard HA `gcha19` cell now passes pod loss and exact backing-VM node loss: three Ready nodes and three zones were restored, the managed instance group reused the node name but changed the Compute Engine instance ID, runtime/queue/database checks passed, and measured recovery was 254 seconds with managed cache loss explicitly classified as not injected. The 2026-08-17 GCP GKE Standard HA `gcha22` cell now passes the zone-loss simulation with dynamic zone selection, exact backing-VM identity validation, a restored three-node/three-zone topology, runtime/queue/search/database checks, managed-cache and fencing classifications, and a measured 275-second recovery. The GCP GKE scenario matrix now records physical zone outage as unsupported while keeping zone-loss automated for that backing-VM simulation, and documents Magento known-content application-integrity as unproven (`setup:db:status` only) on pod/node/zone-loss; the HA harness now plants `magelift_seed_probe` (`tiny-fixture`) after `migrate:dump` and runs `magento-seed-probe` unless `MAGELIFT_HA_MAGENTO_SEED_PROBE=0`; `magento-known-content-probe` and `magento-catalog-sku-probe` stay optional. The 2026-08-18 HA cell `gcha23` failed pod-loss known-content (parser took CLI `warning:` as the seed label; fixture was present); EXIT `magelift destroy --yes --destroy-backups` then `assert_clean` with leftover backups `remaining=0`. Do not reuse `gcha23`. Harness now skips `warning:` / `Defaulted ` lines. The 2026-08-19 HA cell `gcha28` failed create-once on inherited GKE Autopilot (`MAGELIFT_GCP_ACCEPTANCE_RUNTIME=gke-autopilot`): OpenSearch 3-replica exit 78, seed-probe never ran; EXIT `assert_clean` and empty independent inventory. Do not reuse `gcha28`. The 2026-08-19 HA cell `gcha29` failed `day2:logs` on GKE Standard after create-once 58 PASS: `x509: certificate signed by unknown authority` listing web pods (stale `MAGELIFT_KUBECONFIG`); EXIT `assert_clean` and empty independent inventory. Do not reuse `gcha29`. The 2026-08-19 HA cell `gcha32` failed `deploy:candidate` on GKE Standard `europe-west4` after kube-only `day2:health`, search, and `migrate:dump` PASS: Magento HTTP redirected to localhost (seed `base_url`); EXIT `assert_clean` and empty independent inventory. Do not reuse `gcha32`. The 2026-08-20 HA cell `gcha33` aborted during Pulumi create-once on GKE Standard `europe-west4` (wrapper killed; EXIT trap did not run); Magento never started; WIF/secret/bucket were cleaned and the WIF pool is tombstoned `DELETED`. Do not reuse `gcha33`. The 2026-08-20 HA cell `gcha34` aborted during Pulumi create-once on GKE Standard `europe-west4` while creating the VPC; Magento never started; VPC/WIF/secret/bucket were cleaned and the WIF pool is tombstoned `DELETED`. Do not reuse `gcha34`. The 2026-08-20 HA cell `gcha35` failed create-once on GKE Standard `europe-west4`: GCE `not enough resources` at cluster create 106s (31 created, stack failed 51m44s); EXIT `assert_clean` and empty independent inventory. Do not reuse `gcha35`. Next Magento-wired HA cell is `gcha36` with `MAGELIFT_GCP_ACCEPTANCE_RUNTIME=gke-standard` pinned in `europe-west3`, kube-only `day2:health`, kubeconfig refreshed after create-once, and Magento storefront `base_url` set to the LoadBalancer before candidate HTTP health. The 2026-08-20 HA cell `gcha36` PASSed the 16-cell catalog in `europe-west3`: create-once 58/19m7s, Magento HTTP 200 after LoadBalancer `base_url`, seed-probe `tiny-fixture` PASS on pod/node/zone-loss (node RTO 289s, zone RTO 293s, `instanceIDChanged=verified`, `zoneReadyGapObserved=true`), EXIT `assert_clean` leftover backups `remaining=0`, empty independent inventory. Do not reuse `gcha36`. Magento known-content HA integrity on GCP GKE Standard is closed. Physical zone outage remains typed unsupported. Remaining AWS/OVH/Scaleway HA live, fencing proof, and sealed JSONL stay outside this checkbox.
- [x] 3.6a Add the provider-neutral checkpointed failure-drill runner with inject/observe/cleanup ports, exact architecture and ownership binding, no-duplicate-injection resume, explicit unsupported classification, independent cleanup state, fail-closed recovery when persistence fails after injection, and secret-safe checkpoint boundaries. Deterministic tests cover blocked quarantine, pre-observation cleanup-pending resume, observed-stage persistence failure, and rejection of secret-like injected or loaded checkpoint values; provider fault injection and live exercises remain open in 3.6-3.9 and section 8.
- [x] 3.7 Add regional DR exercises with explicit automation/operator steps for alternate-region recovery; add alternate-provider recovery only for profiles whose identity, data, networking, edge, and service contracts make it meaningful, otherwise publish a typed unsupported reason. GCP `gcp.gke` does not advertise `alternate-region`; compile/Start fail-closed. AWS/OVH/Scaleway alternate-region live remains `live.aws` / `live.ovh-scw` in the RC1 split, not a GCP close gate.
- [x] 3.8 Implement fencing, single-writer protection, split-brain detection, failover approval, failback, and stale-origin prevention for every recovery path that can create a second runtime. The provider-neutral SDK now enforces approval safety proofs even for approval-only restore stages. GCP writer fencing, traffic failover, and controlled failback are typed unsupported on `gcp.gke`. Provider-specific AWS/OVH second-runtime live evidence remains `live.aws` / `live.ovh-scw`.
- [x] 3.9 Exercise corrupted data, failed deployment, lost credentials, provider outage, region loss, partial restore, and interrupted teardown using isolated fixtures and ownership scopes; record whether each scenario is automated, operator-assisted, or unsupported. The GCP GKE matrix now records lost-credentials and provider-outage as typed unsupported until a native fence translator opts in, documents physical zone outage as unsupported while zone-loss stays automated for backing-VM simulation, treats interrupted-teardown as automated for marker-owned Cloud SQL through `magelift cleanup reconcile`, and records failed-deployment as operator-assisted until a live injector cell exists (`InjectFailedDeployment` plus `MAGELIFT_GCP_FAILED_DEPLOY_DIGEST`; unset is `failed-deployment=not-run`), and records partial-restore as operator-assisted until a multi-class injector exists. KEEP `gcap25`/`gcap27` kube failed-deployment PASS used kubectl Recreate + pause (`MAGELIFT_GCP_FAILED_DEPLOY_DIGEST`); not Go `InjectFailedDeployment`. The 2026-08-18 SQL-only cell `gcp-cloudsql-cleanup-ledger-live-20260818` passed claim/create/record/reconcile (`deleted=cloudsql-instance:magelift-cledgr-20260818t115759z-83399`, 981s). The 2026-08-18 SQL-only cell `gcp-cloudsql-corrupted-inplace-live-20260818` passed backup, fixture overwrite, in-place restore, and known-row reads (`corruption=injected`, 991s restore). Live execution of remaining 3.9 scenarios remains open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 3.10 Add release gates that require backup creation, successful restore, integrity verification, HA failure evidence, DR evidence, measured RPO/RTO, and cleanup evidence independently; passing one stage must not imply the others.

## 4. Add native observability and New Relic integration

- [x] 4.1 Define a portable observability intent for logs, metrics, traces, audit events, application health, provider operations, backup age, replication lag, queue/database/cache/search health, recovery state, edge TLS/routing/failover, and cleanup, including destinations, retention, sampling, labels, and redaction.
- [x] 4.2 Implement AWS-native observability profiles using CloudWatch Logs, Metrics, alarms, dashboards, audit signals, and the relevant container/Kubernetes integrations, with evidence that the declared signals arrive and retain the required labels. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 4.2a Implement the AWS CloudWatch SDK-backed native observability lifecycle with tag-backed ownership, log/metric delivery probes, retention, alarms/dashboards, direct inventory, foreign-resource refusal, and deterministic cleanup tests; ECS/EKS collection delivery and live evidence remain open in 4.2, 4.6a, and section 8.
- [x] 4.2b Run the bounded real-account CloudWatch Logs/metric/dashboard/alarm probe with exact provider inventory and post-run zero-resource verification; ECS/EKS collection, audit, notification, redaction, and complete architecture evidence remain open in 4.2, 4.6a, and section 8.
- [x] 4.3 Implement GCP-native observability profiles using Google Cloud Observability services and the relevant GKE/managed-service integrations, with explicit coverage or unavailability for each declared signal. The provider-local Logging/Monitoring path now includes custom log-bucket retention and sink routing with exact `_AllLogs` verification; Autopilot/Standard native collection, GMP, and GKE-tied log-alert apply are evidenced (`20260818c`/`d`/`f`/`g`/`j`/`k`) with typed gaps for notification channels and unmanaged redaction. The bounded `gcp-gke-collector-newrelic-live-2026-08-15` and `gcp-gke-standard-collector-newrelic-live-2026-08-15` cells prove Contrib paths with New Relic credentials. Magento-wired native collection on KEEP `gcap27` PASSed Cloud Logging page tokens, Monitoring uptime, and `magelift logs`. Magelift-owned sink/dashboard composition, managed-service wiring, live paging, and remaining signal boundaries remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 4.3a Implement the GCP native SLO lifecycle through the provider-local Google Service Monitoring API boundary and Pulumi resource, preserving the opaque native service reference in the provider-neutral plan and testing idempotence, ownership, drift refusal, scoped inventory, and cleanup; remaining GCP signal, retention, redaction, and live evidence stays open in 4.3 and section 8.
- [x] 4.3b Run the bounded real-project Google Cloud Logging/Monitoring probe with direct alert/dashboard verification, exact metric-descriptor cleanup, eventual-consistency reconciliation, custom log-bucket retention and sink routing verification through `_AllLogs`, and post-run zero-active-resource inventory; the provider-mandated seven-day `DELETE_REQUESTED` bucket tombstone is recorded rather than misreported as purged. Native GKE Autopilot/Standard collection, GMP, audit, and GKE-tied log-alert apply are evidenced (`20260818c`/`d`/`f`/`g`/`j`/`k`). Notification-channel delivery, managed redaction/DLP, Magento-wired architecture, and complete matrix evidence remain open in 4.3, 4.9, and section 8.
- [x] 4.4 Implement Scaleway-native observability profiles from current official capabilities and adapter support, recording missing logs, metrics, traces, audit, or alert signals as explicit unavailable rows rather than dropping them. The Cockpit implementation and focused contract tests are recorded in `docs/evidence/provider-neutral-boundaries-2026-08-11.md`; live Kapsule collection remains separately open.
- [x] 4.4a Run the bounded real Scaleway Cockpit custom logs/metrics cell with the profile-aware official SDK adapter, one-day sources, marker-scoped token, OTLP plus Mimir/Loki query verification, exact owning-service cleanup, and an independent zero-resource scan; Kapsule collection, traces, audit, alerts, dashboards, SLOs, redaction, and complete architecture evidence remain open in 4.4, 4.6a, 4.9, and section 8.
- [x] 4.5 Implement OVHcloud-native observability profiles from current official capabilities and adapter support, using the same signal contract and explicit unavailable rows where the platform lacks an equivalent. The MKS audit-stream boundary and explicit generic-signal gaps are recorded in `docs/evidence/provider-neutral-boundaries-2026-08-11.md`; live delivery remains separately open.
- [x] 4.6 Implement New Relic as an independent typed integration that selects documented collector paths where officially supported and otherwise uses OpenTelemetry or an exporter path; keep New Relic object schemas out of the portable core. ECS uses the OpenTelemetry Collector Contrib boundary, EKS/GKE may use the NRDOT collector boundary, Kapsule/MKS use OTLP or a separately verified custom collector, and generic workloads use OTLP or an explicit unsupported result.
- [x] 4.6a Implement the provider-specific ECS/Kubernetes collector deployment lifecycle behind the shared observability port, including secret references, ownership, health/readiness, rollback, direct inventory, and cleanup. The provider-owned NRDOT chart translator now maps the documented `nr-k8s-otel-collector` values, Secret reference, RBAC, provider mode, endpoint, and supported signals behind the injected release SDK; one shared Helm-backend factory is bound by thin EKS, GKE, Kapsule, and MKS adapters, and the shared Kubernetes Contrib backend emits one credential-specific YAML document, removes only resources created by a failed partial Ensure, and waits for direct Kubernetes/Helm ownership inventories to converge after delete/uninstall. The internal and public module contracts now expose a planned collector-deployment factory seam for target-scoped provider/community clients. The bounded 2026-08-15 GKE Autopilot and GKE Standard Contrib cells prove two live Kubernetes deployments and New Relic logs/metrics/traces delivery with verified cleanup. KEEP `gcap24` Autopilot Magento cluster (`CREATE_CLUSTER=0`) PASSed the same Contrib signals with collector cleanup and Magento retained (`docs/evidence/gcp-gke-autopilot-collector-existing-gcap24-20260818.md`). The separate AWS ECS Contrib exercise proves sidecar config materialization, rollout handling, eventual three-signal delivery, and exact cleanup, but its finite verifier window expired before trace visibility and therefore it is not an acceptance PASS. ECS/EKS/NRDOT/Kapsule/MKS delivery breadth and provider-wide evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 4.6b Implement the shared SDK collector deployment port and reusable ownership/readiness/rollback/cleanup lifecycle, with target-bound ECS/EKS/GKE/Kapsule/MKS constructors. `sdk/v1.RunCollectorDeploymentLifecycle` now normalizes plan/execute requests and requires ownership, idempotency, readiness, health, namespaced signal-delivery, rollback, or cleanup proofs for the selected action; the focused SDK suite passes. Concrete Contrib backends are recorded in 4.6c, while NRDOT Helm and live delivery evidence remain open in 4.6a and section 8.
- [x] 4.6c Implement the concrete AWS ECS Contrib task-definition and reusable Kubernetes client-go Contrib collector backends with immutable images, secret references, ownership collision refusal before mutation, readiness/health/signal gates, stale-task rejection, rollback, and direct cleanup inventory; provider-owned NRDOT chart translation is covered by 4.6a, and live delivery evidence remains open.
- [x] 4.6d Implement the reusable injected Kubernetes Helm release collector backend with immutable chart-version validation, opaque semantic requests, exact ownership/configuration-drift refusal, readiness/health/signal gates, rollback, and direct owning-release cleanup; provider-owned NRDOT chart translation is covered by 4.6a, and live delivery evidence remains open.
- [x] 4.7 Validate telemetry credentials as secret references, prevent values from entering plans/evidence/logs, scope vendor permissions to the selected ownership marker, and test rotation and revocation behavior. The shared SDK gate plus Fastly/New Relic adapters and focused tests are recorded in `docs/evidence/provider-neutral-boundaries-2026-08-11.md`; live replacement-credential acceptance remains separately open.
- [x] 4.8 Add actionable alerts, dashboards, SLO evidence, and runbook links for availability, backup age, restore failure, replication lag, queue depth, database/cache/search health, edge failure, telemetry loss, and cleanup failure. The provider-neutral contract now preserves alert/dashboard/SLO semantics, data residency, and every declared signal or an explicit unavailable status; AWS CloudWatch and Google Cloud provider backends now materialize and verify alert owner/severity/deduplication/runbook metadata and fail closed on provider-unsupported comparison semantics. The New Relic provider-local NerdGraph adapter now materializes and verifies one owned policy/NRQL condition, private dashboard, and application-health SLO with exact cleanup in the bounded 2026-08-13 cell. Provider-native and New Relic live alert firing, retention, redaction, notification, and broad runbook evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 4.9 Add native-only, New-Relic-only, and combined observability contract tests plus live evidence checks for signal delivery, labels, retention, redaction, alert firing, teardown, and post-cleanup inventory. The 2026-08-11 and 2026-08-13 replacement-ingest-key runs and live EU Go OTLP verifier prove bounded queryable New Relic-only data-plane delivery for logs, metrics, and traces without creating a provider resource; the 2026-08-13 NerdGraph cell proves one New Relic-only operational-object lifecycle with policy/NRQL-condition/private-dashboard/SLO verification, partial-mutation rollback, and exact cleanup. The bounded 2026-08-15 GKE Autopilot and GKE Standard Contrib cells prove two collector deployments, separate ingest/query credentials, positive logs/metrics/traces marker delivery, and direct post-run Kubernetes/cluster cleanup. The AWS ECS Contrib exercise later returned exact logs/metrics/traces counts, but New Relic trace visibility exceeded its finite verifier budget, so it remains partial data-plane evidence rather than a bounded acceptance pass. Provider-neutral action/semantic gates, AWS CloudWatch and Scaleway Cockpit bounded cells from 2026-08-13, the corrected one-hour GCP custom-sink routing/retention cell, and AWS/GCP provider alert metadata tests pass. Collector deployment breadth, native-provider composition, alert/notification/redaction breadth, and full cleanup evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).

## 5. Add native edge and Fastly edge paths

- [x] 5.1 Extend the portable edge profile to compose origin, native edge, Fastly edge, both layers, or no edge, with origin health, routing, TLS, DNS, caching, purge, WAF, security headers, failover, rollback, ownership, and cleanup intent.
- [x] 5.2 Implement AWS CloudFront and WAF profiles with origin health and authentication, cache-key and bypass rules, TLS/certificate ownership and renewal, purge verification, failover, rollback, and direct inventory cleanup. The 2026-08-13 `20260813n` control-plane cell passed ACM TLS issuance, two-origin-group apply, purge, destroy, and empty inventory with `trafficImpact=not-run`. The 2026-08-13 `20260813s` cell attached a CLOUDFRONT-scope WAFv2 WebACL in whole-group CommonRuleSet count mode (`waf=attached`); that is attach proof only. Magento-safe WAF (`waf/magento-safe`) is now the portable policy. The 2026-08-13 `20260813t` cell proved alias HTTPS `200` (`trafficImpact=alias-https`) and left empty inventories. The 2026-08-13 `20260813w` cell attached Magento-safe WAFv2 (`waf/magento-safe`, groups `OverrideAction.None`, Magento count-overrides, `KB_64`) and left empty inventories (`waf=attached trafficImpact=not-run`). The 2026-08-13 `20260813aa` cell measured origin-group member-swap RTO `rtoSeconds=49` until secondary HTML title Google (`trafficImpact=failover-https`, CachingDisabled). 5xx origin-group failover, fencing, failback, WAF block data-plane, and Magento origin remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.2a Implement the AWS CloudFront two-origin group control-plane path with exact ownership/topology checks, origin-health gates, failover/rollback ordering, shared lifecycle capabilities, and no traffic-proof overclaim; live traffic and cleanup evidence remain open in 5.2 and 5.8.
- [x] 5.3 Implement GCP load-balancing, Cloud CDN, and Cloud Armor profiles where the source-dated capability matrix and adapter support permit them, including health checks, TLS/DNS, purge, security policy, failover, rollback, and cleanup evidence. Owned target HTTPS proxy certificate drift now reconciles through the documented `setSslCertificates` operation and has deterministic official-client coverage. The 2026-08-13 `20260813r` cell proved Google-managed cert ACTIVE, CDN front-end apply/verify/destroy, and empty independent inventories (`trafficImpact=not-run`). The 2026-08-13 `20260813y` cell proved alias HTTPS `200` (`trafficImpact=alias-https`) and empty magelift-owned inventories. The 2026-08-13 `20260813ag` cell attached Magento-safe Cloud Armor (`waf/magento-safe`: beta `patchRule` body exclusions, GraphQL JSON parsing, 64 KB inspection, sensitivity 1) with `waf=attached trafficImpact=not-run` and empty independent inventories. Follow-up `mgce0816i` passed the installed-schema Magento origin, public alias title, public Cloud Armor `403` data plane, exact rule restoration, and independent cleanup. KEEP `gcap24` Autopilot Magento 2.4.9 origin (`docs/evidence/gcp-edge-magento-origin-live-gcap24-20260818.md`) passed Cloud CDN alias HTTPS `200` with matching Magento title, Armor data-plane `403`, `FAILED_NOT_VISIBLE` wait, and exact CDN cleanup; Magento stack retained. Purge, failover, and rollback traffic evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.3a Implement the GCP two-origin URL-map control-plane failover/rollback path with both-origin health gates, ownership/drift protection, shared lifecycle actions, and explicit control-plane-only evidence; live traffic and cleanup evidence remain open in 5.3 and 5.8.
- [x] 5.4 Inventory current Scaleway and OVHcloud edge products and implement only officially supported, adapter-backed paths; generate explicit unavailable or unsupported rows for profiles with no native edge equivalent. Scaleway health failover and OVH native failover are typed unsupported before mutation.
- [x] 5.5 Complete the Fastly lifecycle behind the edge boundary, including service/domain/TLS configuration, origin health, cache policy, purge, WAF or policy references, request routing, failover, rollback, ownership, and asynchronous deletion; reuse the existing Fastly change without duplicating its portable schema. The current versionless Domain Management path now has live managed-TLS/ACME challenge, routing, purge, and cleanup proof, including a 2026-08-13 replacement-credential cell. Both the public SDK and CLI now strictly decode provider-owned advanced `extensions.fastly.edge` version/VCL/origin-backend/route-snippet settings while keeping portable intent separate; policy-specific WAF/cache/purge and failover/rollback proof remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.5a Make Fastly native service-version mutation safe for existing services by cloning, validating, and activating an editable version through a separate provider SDK port, restoring the prior active version after post-activation verification failure, and rejecting versioned mutations when the adapter cannot provide that port. Traffic failover/failback exercises and policy-specific routing mutations remain open in 5.5-5.8.
- [x] 5.6 Enforce the origin safety gate so apply never directs traffic to an unhealthy or unverified origin, and make edge failover/failback prove fencing, route convergence, measured traffic impact, and edge RTO. The shared lifecycle and provider adapters now fail closed on missing origin-health proof, with deterministic AWS/GCP/Scaleway/OVH/native-plus-external safety coverage. The 2026-08-13 Fastly replacement-credential cell proved HTTPS route convergence through Fastly. The 2026-08-13 CloudFront `20260813t` cell proved alias HTTPS `200` through a custom domain. The 2026-08-13 GCP `20260813y` cell proved alias HTTPS `200` through a custom domain (`www.google.com` origin). The 2026-08-13 CloudFront `20260813aa` cell measured origin-group member-swap RTO `rtoSeconds=49` until secondary HTML title Google (`trafficImpact=failover-https`). 5xx origin-group failover, fencing, and failback remain live evidence gates. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.7 Add cache-key, bypass, purge, post-purge, TLS rotation, DNS ownership, WAF/security-header, origin-authentication, and rollback tests for native and Fastly edge combinations. Offline tests now cover AWS CloudFront, GCP CDN/Armor, Scaleway stage ordering, OVH's typed L4-only unsupported boundary, and native-plus-Fastly field isolation; Fastly live policy/traffic evidence and provider-wide failover/rollback remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.8 Add disposable real-account edge acceptance with secret references, ownership-scoped cleanup, direct inventory assertions, and preservation of unmarked user resources; keep Fastly and native edge claims independent. The 2026-08-13 Fastly replacement-credential cell proved marked TLS/service/domain/DNS cleanup and preserved the unmarked Fastly service. The 2026-08-13 CloudFront `20260813n` cell proved marked ACM/distribution cleanup with empty ownership inventory. The 2026-08-13 CloudFront `20260813s` cell proved marked ACM/WAF/distribution cleanup with empty ownership inventory. The 2026-08-13 CloudFront `20260813w` cell proved Magento-safe WAFv2 attach plus marked ACM/WAF/distribution cleanup with empty ownership inventory. The 2026-08-13 CloudFront `20260813t` cell proved alias HTTPS `200` and marked ACM/DNS/distribution cleanup. The 2026-08-13 GCP `20260813r` cell proved marked forwarding-rule/proxy/URL-map/cert/NEG/DNS cleanup with empty independent inventories. The 2026-08-13 GCP `20260813y` cell proved alias HTTPS `200` plus the same marked cleanup with empty magelift-owned inventories. The 2026-08-13 GCP `20260813ag` cell attached Magento-safe Cloud Armor plus marked forwarding-rule/proxy/URL-map/cert/NEG/Armor/DNS cleanup with empty magelift-owned inventories. The 2026-08-13 CloudFront `20260813aa` cell proved origin-group member-swap RTO `rtoSeconds=49` plus marked ACM/DNS/distribution cleanup; Scaleway/OVH native edge live cells remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).

## 6. Make cloud certification coverage-driven and resource-efficient

- [x] 6.1 Define certification dimensions and cold-boundary rules for provider, region, compute, Kubernetes topology, managed/self-hosted boundary, service majors, resilience profile, observability destination, edge path, artifact, schema, migration, and recovery fixture.
- [x] 6.2 Build, sign, and publish one immutable artifact per compatible runtime contract, record its digest and provenance, and reuse it across all compatible provider and warm-transition cells. `PrepareCertificationRun` enforces admission-before-artifact ordering and the durable `ImmutableArtifactRegistry` claim/publish/reuse boundary; distributed runners inject a conditional store through the same core interface.
- [x] 6.3 Add complete warm-session fingerprints that include architecture, service versions, resilience/backup fixture, observability configuration, edge configuration, artifact, schema, migration, ownership, and state backend; invalidate reuse on any changed boundary.
- [x] 6.4 Implement dependency-graph scheduling that creates cold baselines only at required boundaries and selects a bounded set of warm transitions for shared services, restores, edge, and observability without claiming untested combinations.
- [x] 6.5 Add quota, credential, account/project, network, state-backend, fixture, ownership, and local-resource admission checks; run independent provider groups concurrently only after those checks pass and serialize shared mutations. Normal infrastructure, bootstrap, login, and edge-apply CLI paths now share the provider plan-admission guard before mutation. The core certification admission path now rejects typed-nil probes, invalid/secret-bearing provider facts, partial error results, and stranded reusable-session claims, and each scheduled run gets a distinct generated claim owner so concurrent compatible runs cannot reuse one another's live session claim. The scheduler now carries per-unit CPU/memory requirements and acquires an injectable, cancellation-safe in-process local-capacity reservation before checkpoint or provider mutation; a distributed runner must still inject a shared conditional reservation with fencing and stale-lease handling. Certification-specific first-party provider admission adapters, multi-runner coordination, and live quota/network/fixture evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 6.5a Implement the local fail-closed admission, provider-probe, quota, mutation-key, ownership, and local-resource checks with deterministic certification tests; real account credentials, quotas, and live network admission remain open in 8.3.
- [x] 6.6 Reuse one scrubbed fixture, migration result, backup set, observability setup, and edge setup for compatible transitions, while preserving independent evidence for every cold boundary and preventing a second migration or duplicate writer. The prepared run now carries the session registry through the scheduler, `ExecuteCertificationRun` owns the provider-neutral execution boundary, and focused tests prove checkpoint resume and exact ready-session reuse; live fixture/backup/telemetry/edge reuse remains independently gated by section 8.3.
- [x] 6.6a Implement exact reuse fingerprints, atomic session claims, pending-delete blocking, immutable artifact claims, and cleanup-verified reuse tests. `FileSessionRegistry` and `FileImmutableArtifactRegistry` provide atomic mode-0600 local/CI persistence; distributed runners inject conditional stores through the same core interfaces. Live fixture/backup/telemetry/edge reuse evidence remains open in 8.3-8.8.
- [x] 6.7 Add durable checkpoints for architecture fingerprint, provider operation IDs, backup/restore IDs, edge and telemetry identities, ownership markers, completed stages, and cleanup state; interruption must resume the same scope. `FileScheduleCheckpointStore` provides atomic mode-0600 local/CI persistence, a direct scheduler writer, and bounded detached terminal writes after cancellation; the shared shell checkpoint helper now atomically finalizes cleanup state after provider-owned inventory convergence and records Kubernetes cell duration before checkpointing; distributed runners inject a transactional backend.
- [x] 6.8 Add asynchronous create/delete polling, bounded retry and timeout budgets, delayed-tombstone status, and direct owning-service inventories so slow cloud operations are amortized without treating incomplete deletion as success. Every dependency-gated top-level acceptance wrapper now starts the shared TTL watchdog only after its cleanup trap and stops it before provider-owned cleanup, with offline coverage for all thirteen wrappers.
- [x] 6.9 Run offline schema/catalog/profile/secret checks and provider mock/contract tests before any paid cloud test; publish a planned matrix with expected runtime, quota, cost, credentials, and cleanup risk.
- [x] 6.10 Add per-provider and per-session cost/time budgets, concurrency controls, cancellation behavior, and a report showing artifact reuse, warm-transition count, cloud operation time, resource lifetime, spend estimate, and cleanup latency. Terminal scheduler checkpoints retain operation and cleanup identities after an interrupted provider callback.
- [x] 6.11 Add scheduler tests for boundary coverage, safe parallelism, fingerprint invalidation, interruption/resume, quota exhaustion, provider deletion delay, stale checkpoints, duplicate ownership, and partial cleanup.

## 7. Produce evidence, security controls, and release documentation

- [x] 7.1 Extend the evidence schema with architecture identity, provider/service versions, source dates, artifact digest, fixture identity, backup/restore IDs, measured RPO/RTO, HA/DR results, observability signal coverage, edge behavior, duration, cost, cleanup truth, and operator actions.
- [x] 7.2 Make evidence provenance and secret redaction verifiable, including no plaintext provider, database, New Relic, Fastly, DNS, certificate, or recovery credentials in generated plans, logs, checkpoints, artifacts, or documents. Sealed-record validation, recursive secret-safety checks, redacted diagnostics, and provenance/digest validation are implemented; live evidence remains gated by 8.3-8.8.
- [x] 7.3 Generate capability, architecture, resilience, observability, edge, certification, and recovery-runbook documents from the catalog and evidence records; show source links and retrieval dates for fast-moving provider facts. The deterministic generator and `gencertdocs --check` gate are implemented; it publishes no capability claim without source-dated catalog data or sealed evidence.
- [x] 7.4 Add release validation that blocks a `certified` claim when any required evidence class is missing, stale, non-reproducible, non-clean, below the declared RPO/RTO, or only proven through a neighboring architecture.
- [x] 7.5 Publish operational ownership and escalation rules for automated versus operator-assisted backup restore, HA failover, regional DR, alternate-provider recovery, native telemetry, New Relic, native edge, and Fastly. Published in `docs/operations/ownership-and-escalation.md`; live provider-specific run evidence remains gated by the relevant implementation tasks.
- [x] 7.6 Add an audit report connecting each remaining task in the existing open changes to this change's coverage graph, marking completed, delegated, blocked, unavailable, experimental, and still-open work without closing unrelated tasks.

## 8. Execute staged verification and close only evidenced profiles

- [x] 8.1 Pass local unit, schema, catalog, secret-safety, profile-validation, scheduler, checkpoint, and generated-document tests before provisioning any cloud resource.
- [x] 8.2 Pass provider mock and contract suites for AWS ECS/EKS, GCP GKE, Scaleway Kapsule, OVHcloud MKS, native observability, New Relic, native edge, and Fastly, including rejected and unavailable paths.
- [x] 8.3 Run the minimum cold baseline for each declared architecture family and required service-major/resilience boundary, using immutable artifacts and shared scrubbed fixtures; record unsupported or blocked cells without retrying them as green. The corrected 2026-08-12/13 GCP probes now cover both Autopilot and GKE Standard managed-service/topology boundaries for Valkey 9.0, and the 2026-08-13 Scaleway Kapsule single-zone infrastructure-only cell covers the Kapsule/RDB/Redis topology boundary. The 2026-08-13 AWS ECS Fargate Magento 2.4.9 cell `20260813ai` passed that application's cold baseline (signed digest, seed import, ECS runtime health, 101/101 destroy, empty stack inventories). The 2026-08-13 GCP GKE Autopilot Magento 2.4.9 cell `20260813aj` passed the 12-cell preview catalog (seed import, kube runtime 1/1, PSA-race-aware destroy, empty independent `gcpaj` inventories). The 2026-08-13 AWS ECS Fargate Spot Magento 2.4.9 cell `20260813al` passed that application's cold baseline (signed digest, seed import, ECS runtime health on `computeMode:fargate-spot`, 102/102 destroy, empty stack inventories); Spot interruption was not injected. The 2026-08-13 AWS ECS EC2 ASG Magento attempt `20260813ao` failed at `magelift login` (`AWS plan contains an empty AMI`); do not reuse `ao`. The retry `20260813ap` failed `CreateCapacityProvider` on reserved name `awsap-preview-runtime-ec2`; do not reuse `ap`. The retry Magento 2.4.9 ECS EC2 Auto Scaling cell `20260813aq` passed that application's cold baseline (pinned AMI, `ml-` capacity-provider name, signed digest, seed import, ECS runtime health, 109/109 destroy, empty stack inventories). The 2026-08-13 GKE Standard Magento attempt `20260813am` failed `queue:health` (`PROFILE=preview` with `cells-gcp-standard.txt`); do not reuse `am`. The retry Magento 2.4.9 GKE Standard cell `20260813an` passed the 13-cell standard catalog (PROFILE=`standard`, seed import, kube runtime 2/2, RabbitMQ 1/1, PSA-race-aware destroy, empty independent `gcpan` inventories); that is kube runtime health, not HTTP/TLS. The 2026-08-13 AWS ECS Managed Instances Magento attempt `20260813ar` failed create (Pulumi required `vcpu_count`/`memory_mib`; AWS CLI teardown emptied inventories; do not reuse `ar`). The retry `20260813as` failed create (`instanceGenerations: current` with pinned `m6i.large`; 94/94 destroy; do not reuse `as`). The retry `20260813at` failed create (`PutClusterCapacityProviders` before ACTIVE; 95/95 destroy; do not reuse `at`). The retry Magento 2.4.9 ECS Managed Instances cell `20260813au` passed that application's cold baseline (pinned vCPU/memory, no `instanceGenerations`, no `PutClusterCapacityProviders`, signed digest, seed import, ECS runtime health, 108/108 destroy, empty stack inventories); Managed Instances interruption was not injected. The Magento EKS Auto Mode attempt `20260813av` failed seed import (unqualified Pulumi `--stack`; 85/85 destroy; do not reuse `av`). The retry `20260813aw` failed seed import (unexpanded mysql client image; 85/85 destroy; do not reuse `aw`). The retry Magento 2.4.9 EKS Auto Mode cell `20260813ax` passed that application's cold baseline (qualified Pulumi kubeconfig, expanded seed image, signed digest, seed import, kube 1/1, 85/85 destroy, empty stack inventories). The Magento 2.4.9 EKS Fargate cell `20260813ay` passed that application's cold baseline (signed digest, seed import, kube 1/1, 78/78 destroy, empty stack inventories). Remaining EKS managed-node-groups/self-managed Magento baselines, Scaleway/OVH Magento application baselines, HA/DR/fixtures, and collector intersections remain to be completed or explicitly classified. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 8.4 Run selected warm transitions for service upgrades, restore, HA, observability destination, edge composition, migration, and failover; verify the fingerprint and evidence show exactly which boundaries were reused. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 8.5 Run backup/restore, HA failure, regional DR, fencing/failback, corrupted-data, failed-deployment, credential-loss, and interrupted-teardown exercises for each profile that claims those guarantees. One bounded AWS S3 cell, one AWS Secrets Manager cell, the GCP Cloud SQL and Pub/Sub cells, one GCP Cloud Storage media cell, one GCP Secret Manager cell, one Scaleway Object Storage media cell, one Scaleway Secret Manager cell with acceptance-only Object Lock and scheduled-free source cleanup, one OVHcloud Object Storage media cell, the 2026-08-13 Scaleway RDB control-plane cell `20260813m`, and the 2026-08-13 OVH Managed Database control-plane cell `20260813q` passed with exact active-resource cleanup. The 2026-08-15 GCP Cloud SQL cell also proves one known-row application read, permissions, health, manifest content, and measured provider restore. The 2026-08-16 Scaleway RDB, 2026-08-17 AWS Aurora, and 2026-08-17 OVH Managed Database cells add application-fixture reads, restored-identity verification, permissions, health, manifest evidence, measured provider restore, and exact independent cleanup. The 2026-08-17 GCP GKE Standard HA `gcha19` cell adds live pod-loss and backing-VM node-loss evidence with a 254-second measured recovery, queue/database checks, explicit managed-cache non-injection classification, and independent cleanup. The 2026-08-17 GCP GKE Standard HA `gcha22` cell adds a bounded declared-zone backing-VM deletion simulation with exact identity validation, an observed Ready-node gap, recovery to 3/3 nodes and 3/3 zones, runtime/queue/search/database checks, a 275-second measured recovery, dynamic region-zone selection, and independent cleanup. The `gcha20` GKE-capacity and `gcha21` Memorystore-capacity attempts are recorded as clean, non-certified dead ends. Application integrity for the remaining classes, HA variants beyond these bounded cells, Magento known-content HA integrity on GKE Standard closed by `gcha36` (experimental, not public certified), physical zone/region DR, fencing/failback, corruption, failed-deployment injection, credential-loss, and interrupted-teardown evidence remain open. GCP failed-deployment is operator-assisted until an injector exists. GCP partial-restore is operator-assisted until a multi-class injector exists. The 2026-08-18 SQL-only cell `gcp-cloudsql-corrupted-inplace-live-20260818` passed in-place restore after fixture corruption (`corruption=injected`, 991s restore). **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 8.6 Run native observability and New Relic evidence checks for each claimed provider, then verify alerting, signal retention, redaction, and cleanup after the runtime is destroyed. The GCP native cell now also proves custom log-bucket retention and sink routing with no active resources after teardown (provider tombstone recorded), while the 2026-08-11 replacement-ingest-key run and live EU Go OTLP verifier are bounded queryable data-plane results for the New Relic-only OTLP cell, the 2026-08-13 NerdGraph cell proves bounded New Relic operational-object CRUD/rollback/cleanup, and the 2026-08-15 GKE Autopilot and GKE Standard Contrib cells prove two live collector paths with separate ingest/query credentials, logs/metrics/traces marker delivery, and direct cluster cleanup. The AWS ECS Contrib exercise produced eventual exact logs/metrics/traces counts and exact AWS cleanup, but its finite verifier budget expired before the trace became queryable, so it is not a bounded acceptance pass. Collector breadth, native-provider composition, alert firing/notification, redaction, retention, and full-provider cleanup gates remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 8.7 Run native edge and Fastly evidence checks for each claimed architecture, including TLS/DNS, health-gated routing, purge, WAF/security policy, failover, rollback, and post-destroy inventory. The 2026-08-13 CloudFront `20260813n` origin-group cell and `20260813s` WAF-attached cell passed control-plane apply/purge/destroy with empty inventories; `20260813t` proved alias HTTPS `200` (`trafficImpact=alias-https`); `20260813w` attached Magento-safe WAFv2 protect mode (`waf/magento-safe`, `KB_64`) and left empty inventories. The 2026-08-13 GCP `20260813r` cell proved Google-managed TLS ACTIVE and empty inventories (`trafficImpact=not-run`). The 2026-08-13 GCP `20260813y` cell proved alias HTTPS `200` (`trafficImpact=alias-https`) and empty magelift-owned inventories. The 2026-08-13 GCP `20260813ag` cell attached Magento-safe Cloud Armor (`waf=attached trafficImpact=not-run`) and left empty inventories. The 2026-08-13 CloudFront `20260813aa` cell measured origin-group member-swap RTO `rtoSeconds=49` (`trafficImpact=failover-https`) and empty inventories. The 2026-08-15 `mgpv0815d`/`mgce0815d` GCP retry proved the dedicated Cloud Armor rule-resource path in preview and real apply, but GKE capacity in `europe-west1` prevented the Magento-origin traffic gate; cleanup was empty. Follow-up `mgce0816i` passed GCP installed-schema Magento origin identity, public alias HTTPS/title, public Cloud Armor `403` data plane, exact rule restoration, and independent post-destroy inventories. Purge/failover/rollback, 5xx traffic exercises, and provider-wide native/Fastly policy evidence remain open. See [`gcp-native-edge-armor-rule-resource-live-2026-08-15.md`](../../../docs/evidence/gcp-native-edge-armor-rule-resource-live-2026-08-15.md). **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 8.8 Run the final cleanup certification stage using direct provider inventories, reconcile delayed deletions, and retain only explicitly approved debug resources with an owner, expiry, and reason. The 2026-08-12 final reconciliation returned zero exact MageLift marker resources in AWS, GCP, Scaleway, and OVHcloud, removed the stale exact-name GCP Artifact Registry repository, reconciled the OVH subnet-port 409, preserved only provider-mandated AWS KMS deletion tombstones and unmarked user resources, and recorded the Fastly/New Relic no-mutation boundaries.
- [x] 8.9 Regenerate the release matrix and readiness report from the passing evidence, review every non-certified row and reason, and close this change only when all required profile gates and existing-change dependencies are complete. **2026-08-20:** sealed GCP Autopilot Magento JSONL VALIDATED; AWS Fargate Magento prior-certified; GKE Standard / EKS / OVH / Scaleway stay experimental; no resilience row promoted from this Magento catalog run (architecture proofs 0). See [`rc1-5.1-matrix-regen-20260820.md`](../../../docs/evidence/rc1-5.1-matrix-regen-20260820.md).

## Evidence updates

- 2026-08-15 GCP edge run `20260815-edge-1` created the marked CDN backend,
  Magento-safe Cloud Armor policy, managed certificate, HTTPS front end, and
  Cloudflare DNS record, but the certificate stayed `PROVISIONING` for the
  bounded 900-second wait after public DNS convergence. The harness refused to
  claim traffic and exited 2; independent GCP and Cloudflare inventories found
  no remaining marked resources. This is negative evidence only. Tasks 5.3,
  5.6-5.8, and 8.7 remain open.

- 2026-08-15 GCP edge retry `20260815-edge-2` repeated the same marked CDN,
  Armor, managed-HTTPS, and DNS cell with an 1800-second certificate budget,
  matching the earlier successful alias-cell budget. The certificate still
  remained `PROVISIONING`; the harness failed closed and independent GCP and
  Cloudflare inventories were empty. Managed certificate issuance is
  provider-gated for this account window. No new traffic, WAF block, purge,
  failover, or rollback claim was added; tasks 5.3 and 8.7 remain open.

- 2026-08-15 GCP native observability run `20260815-gcpobs-1` passed the
  marker-scoped log/metric delivery, alert/dashboard metadata, one-day custom
  bucket and sink routing, and independent zero-active-resource cleanup checks
  with a one-hour propagation budget. The provider `DELETE_REQUESTED` bucket
  tombstone is recorded separately from active cleanup. This repeats the
  bounded Cloud Operations cell and does not close GKE Autopilot/Standard
  collector delivery, managed-service wiring, audit, redaction, notification,
  traces, or the full architecture evidence required by 4.3 and 8.6.

- 2026-08-14 GCP Secret Manager recovery passed with a redacted value
  fingerprint and exact archive, restore, source, and bucket cleanup. See
  `docs/evidence/gcp-secret-manager-live-2026-08-14.md`. This adds one repeat
  of the configuration and secrets durable-data cell; it does not close 3.3,
  3.5, or 8.5.
- 2026-08-14 GCP Pub/Sub recovery passed a seven-day subscription
  snapshot/seek cell with exact topic, subscription, snapshot, and cleanup
  evidence. See `docs/evidence/gcp-pubsub-live-2026-08-14.md`. This does not
  claim isolated-destination or regional queue recovery.
- 2026-08-14 GCP Cloud SQL created an owned MySQL 8.4 source, completed the
  on-demand backup, and started the isolated restore, but the run used an
  operator-supplied 30-minute TTL and expired before the restore window and
  cleanup completed. The trap then removed both instances and the backup, and
  independent inventory was empty. See
  `docs/evidence/gcp-cloudsql-live-2026-08-14-timeout.md`. This is a recorded
  timeout, not a release PASS; the existing task remains open for application
  fixture, restore-in-place, and broader recovery evidence.

- 2026-08-16 AWS RDS MySQL recovery passed the shared application-fixture
  cell in `eu-west-3`: one known row was restored and read through the
  isolated RDS identity, the permission and health probes passed, and the
  measured provider restore took 390 seconds. The exact-marker wrapper and
  independent provider lookups found no source, restore, snapshot, subnet
  group, or temporary security group after teardown. See
  `docs/evidence/aws-rds-application-fixture-live-2026-08-16.md`. This does
  not close 3.3, 3.5, or 8.5; Scaleway RDB and OVH Managed Database still
  need application-fixture evidence, and HA/DR/retention variants remain
  open.

- 2026-08-16 Scaleway RDB MySQL recovery passed the shared application-fixture
  cell in `fr-par`: one known row was restored and read through the isolated
  restore identity, the manifest, permission, and health probes passed, and
  the measured provider restore took 487 seconds. The exact-marker wrapper
  and independent provider lookups found no source, restore, or snapshot
  after teardown. The first attempt failed closed before mutation because
  unrelated pending GCP cleanup ledgers were being offered to the Scaleway
  cleanup provider; provider-scoped ledger reconciliation fixed that root
  cause without adopting those ledgers. See
  `docs/evidence/scaleway-rdb-application-fixture-live-2026-08-16.md`. This
  does not close 3.3, 3.5, or 8.5; OVH Managed Database and HA/DR/retention
  variants remain open.

- 2026-08-17 OVHcloud Managed Database MySQL recovery passed the shared
  application-fixture cell in `GRA`: provider-managed PITR restored one known
  row through the isolated service identity, the manifest, permission, and
  health probes passed, and the measured provider restore took 295 seconds.
  The exact-marker wrapper and independent provider lookups found no source or
  restore after teardown; direct source and restore lookups returned
  `404 ServiceNotFound`. The cell also records the failed-run corrections that
  made the PITR point provider-eligible and the permission probe compatible
  with `sql_require_primary_key`. See
  `docs/evidence/ovh-database-application-fixture-live-2026-08-17.md`. This
  does not close 3.3, 3.5, or 8.5; restore-in-place, HA/DR, retention
  variants, and broader failure scenarios remain open.

- 2026-08-17 AWS Aurora MySQL recovery passed the shared application-fixture
  cell in `eu-west-3`: an encrypted cluster snapshot restored to an isolated
  Aurora cluster and writer, one known row was read through the restored
  cluster identity, the manifest, permission, and health probes passed, and
  the measured provider restore took 539 seconds. The native cleanup path
  deleted the restored writer before its cluster and preserved the source
  cluster's writer; the exact-marker wrapper and independent provider lookups
  found no source, restore, cluster snapshot, subnet group, or temporary
  security group after teardown. Failed bounded runs exposed Aurora's
  cluster-level deletion-protection boundary and the source-member inventory
  shape; both are recorded in the evidence file and covered by regression
  tests. See
  `docs/evidence/aws-aurora-application-fixture-live-2026-08-17.md`. This
  does not close 3.3, 3.5, or 8.5; restore-in-place, HA/DR, retention
  variants, and broader failure scenarios remain open.

- 2026-08-17 GCP GKE Standard HA run `gcha19` passed the complete 15-cell
  catalog, including pod loss and exact backing-VM node loss. The deleted
  node's GKE managed instance-group identity was reused while its Compute
  Engine numeric VM ID changed; the corrected verifier recorded
  `nodeIdentityReused=true`, restored 3/3 Ready nodes across three zones,
  verified runtime traffic, RabbitMQ, and Magento database schema health, and
  measured recovery at 254 seconds. The wrapper exited 0 with `assert_clean
  ok`, and an independent exact-marker inventory was empty for GKE, Cloud SQL,
  Valkey, network, storage, secrets, WIF, and load-balancer resources. See
  `docs/evidence/gcp-gke-ha-node-loss-live-2026-08-17.md`. This closes the
  current GKE pod/node failure cell only; zone loss, DR, cache-loss injection,
  fencing/failback, and broader failure scenarios remain open. The preceding
  `gcha17` stale-image preflight stop and `gcha18` same-name verifier dead end
  are recorded in that evidence file.
