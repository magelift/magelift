## RC1 evidence classes (2026-08-18)

Owned with [`rc1-gcp-first-distribution`](../rc1-gcp-first-distribution/tasks.md).
This change owns adapter implementation plus the first bounded live cell of a
*new* API. The evidence catalog and matrix stay in
`multi-cloud-resilience-observability-edge`. Stop copying the same live
paragraph into both files.

### Remaining split

- [x] `impl` — AWS/GCP/Scaleway/OVH/Fastly/New Relic lifecycle clients behind
  `sdk/v1` for the declared families (sections 1, 2.3a–2.6a, 3.1–3.4b, 4.1–4.6a,
  5.1–5.5, 6.2b–6.4, 8.x mostly closed).
- [x] `floci` GCS + Floci AWS IAM OIDC — `tests/floci-gcp` and
  `TestIAMOpenIDConnectProviderAgainstFloci`.
- [x] `floci` leftover — Secret Manager / Pub/Sub / Logging / Monitoring on
  digest-pinned floci-gcp 0.7.0 via official Go clients (`make floci-gcp-test`:
  `TestSecretWriteAndAccessAgainstFlociGCP`, `TestTopicPublishAgainstFlociGCP`,
  `TestLogWriteAndListAgainstFlociGCP`, `TestMetricWriteAndListAgainstFlociGCP`).
  Closed with rc1 2.6. Not Magento. Not KEEP.
- [x] `live.gcp` — no new SQL-only durable-class cells; packed Magento session 1
  is catalogued in the multi-cloud change (`gcap27` leftovers closed;
  `gcha36` Standard HA Magento known-content closed, experimental).
  **2026-08-20 `gcap28`:** schema-mismatch PASS plus sealed JSONL (see
  multi-cloud `live.gcp`). First bounded cell of a *new* GCP API still lands
  here. Do not reuse `gcap28`.
- [x] `live.aws` — ECS Fargate Magento session 2 closed by `awf18`. Remaining
  CloudWatch collection and CloudFront Magento-origin are not RC1-required
  (GCP owns obs/edge). Cheap Fargate Magento only if 5.1 cannot seal without
  an AWS Magento JSONL row.
- [x] `live.integration` — Fastly Magento-origin HTTPS, New Relic Contrib, and
  Cloudflare DNS closed on `gcap27`. SendGrid delivery typed unsupported
  (preview YAML omits `email`). First bounded cell of a *new* integration API
  still lands here.
- [x] `live.ovh-scw` — infra-only leftovers closed from the 2026-08-19
  inventory; Magento, HA/DR, OKMS, and Kapsule collection stay experimental.
  No new mutation.
- [x] GCP fencing / alternate-region / lost-credentials / provider-outage —
  typed unsupported; do not add new live drills here.

## 1. Provider client foundation

- [x] 1.1 Freeze the provider capability matrix from current official documentation, including source URLs, retrieval dates, regional/preview limits, and explicit unavailable or unsupported rows.
- [x] 1.2 Define one narrow provider-client construction contract per lifecycle family (resilience, edge, observability, credential lifecycle, and owning-service inventory) and keep native SDK types inside provider packages.
- [x] 1.3 Implement provider credential-reference resolution and least-privilege identity checks without allowing secret values into plans, checkpoints, logs, artifacts, or evidence.
- [x] 1.4 Implement normalized asynchronous operation polling, bounded retry/cancellation budgets, idempotency handling, and owning-service inventory adapters behind the existing SDK ports.
- [x] 1.5 Add factory construction tests for target identity, runtime identity, nil/unsupported ports, context cancellation, and provider-mismatch rejection.

## 2. AWS ECS and EKS lifecycle clients

- [x] 2.1 Implement ECS Fargate, Fargate Spot, EC2 Auto Scaling capacity-provider, and Managed Instances client mappings with separate fingerprints, interruption handling, and cleanup ownership. The Fargate Spot task-definition path now sets the AWS-documented 120-second `stopTimeout` for every container; public plans and the AWS acceptance harness preserve all four compute modes as explicit cold boundaries, with mode-specific architecture catalogs. The 2026-08-13 Magento 2.4.9 ECS Fargate cold cell `20260813ai` passed signed-digest promote, seed import into RDS `database=magento`, ECS runtime health (1/1 running, primary rollout completed, task healthy), warm `queueMode:db` infra-only health, destroy of 101 resources, disposable state-bucket deletion, prerequisite-secret force-delete, and harness `assert_clean`; independent post-run inventories were empty for VPC/RDS/ECS/ALB/ElastiCache/S3/secrets/CloudFront; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up (customer KMS remains Enabled). The 2026-08-13 Fargate Spot Magento attempt `20260813ak` failed before create (`PENDING_VALIDATION`, zero CNAMEs); do not reuse `ak`. The retry Magento 2.4.9 ECS Fargate Spot cold cell `20260813al` passed ACM ResourceRecord-gated issuance, signed-digest promote, seed import into RDS `database=magento`, ECS runtime health (1/1 running, primary rollout completed, task healthy), warm `queueMode:db` infra-only health, destroy of 102 resources, disposable state-bucket deletion, prerequisite-secret force-delete, and harness `assert_clean`; independent post-run inventories were empty for VPC/RDS/ECS/ALB/ElastiCache/S3/secrets/CloudFront; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up (customer KMS remains Enabled). The 2026-08-13 EC2 ASG Magento attempt `20260813ao` failed at plan admission (`AWS plan contains an empty AMI`); do not reuse `ao`. The retry `20260813ap` pinned AMI `ami-0fec9d65fa05f7f85` then `CreateCapacityProvider` rejected name `awsap-preview-runtime-ec2` (reserved `aws*` prefix, request `8a1f4f3d-af4f-4d77-8f19-eee0b2abdc87`); EXIT deleted 94 resources, independent stack inventories were empty, leftover ACM was deleted. Do not reuse `ap`. The retry Magento 2.4.9 ECS EC2 Auto Scaling cold cell `20260813aq` passed ACM ResourceRecord-gated issuance, pinned AMI `ami-0fec9d65fa05f7f85`, `ml-` capacity-provider name rewrite, signed-digest promote, seed import into RDS `database=magento`, ECS runtime health (1/1 running, primary rollout completed, task healthy), warm `queueMode:db` infra-only health, destroy of 109 resources, disposable state-bucket deletion, prerequisite-secret force-delete, and harness `assert_clean`; independent post-run inventories were empty for VPC/RDS/ECS/ALB/ASG/ElastiCache/S3/secrets/CloudFront; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up (customer KMS remains Enabled). The 2026-08-13 ECS Managed Instances Magento attempt `20260813ar` failed during create: Pulumi AWS required `instanceRequirements.vcpu_count` and `memory_mib` (AllowedInstanceTypes alone is not enough). Create produced 94 resources then 1 error; EXIT `magelift destroy` failed the same validation; AWS CLI teardown emptied independent inventories (VPC/RDS/ECS/ALB/ElastiCache/S3/secrets/CloudFront/ACM/DNS); customer KMS remains Enabled. Do not reuse `ar`. The retry `20260813as` failed create: `CreateCapacityProvider` rejected `instanceGenerations: current` with pinned `m6i.large` (request `711827ff-5a22-453a-b913-78d8d54cfaa4`); EXIT deleted 94 resources, leftover ACM was deleted. Do not reuse `as`. The retry `20260813at` created `ml-awsat-preview-runtime-managed` then `PutClusterCapacityProviders` rejected a non-ACTIVE provider (request `1d4e28cd-4e96-42b9-8530-b983b335280b`); EXIT deleted 95 resources, leftover ACM was deleted. Do not reuse `at`. The retry Magento 2.4.9 ECS Managed Instances cold cell `20260813au` passed ACM ResourceRecord-gated issuance, pinned 2 vCPU / 8192 MiB `m6i.large` without `instanceGenerations`, `ml-` capacity-provider name rewrite, cluster binding at `CreateCapacityProvider` without `PutClusterCapacityProviders`, signed-digest promote, seed import into RDS `database=magento`, ECS runtime health (1/1 running, primary rollout completed, task healthy), warm `queueMode:db` infra-only health, destroy of 108 resources, disposable state-bucket deletion, prerequisite-secret force-delete, and harness `assert_clean`; independent post-run inventories were empty for VPC/RDS/ECS/ALB/ElastiCache/S3/secrets/CloudFront; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up (customer KMS remains Enabled). Fargate Spot interruption, Managed Instances interruption, ALB HTTP/custom-domain TLS, Magento origin through CloudFront, HA, DR, and collectors remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 2.2 Implement EKS managed-control-plane profiles for managed node groups, Auto Mode, Fargate, and self-managed capacity, including topology, storage, IAM, ingress, and node-lifecycle boundaries. The plan and acceptance boundary now preserve the four compute modes independently, and the AWS harness provides one explicit cold architecture catalog per mode. The 2026-08-13 Magento EKS Auto Mode attempt `20260813av` created 85 resources then failed seed import (`pulumi stack output --stack` rejected unqualified `awsav-preview-aws-eks`); EXIT deleted 85/85 and harness `assert_clean` passed; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up. Do not reuse `av`. The retry Magento EKS Auto Mode attempt `20260813aw` created 85 resources then failed seed import (`kubectl run` received an unexpanded `${MAGELIFT_AWS_ACCEPTANCE_SEED_IMAGE:-...}` image name); EXIT deleted 85/85 and harness `assert_clean` passed; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up. Do not reuse `aw`. The retry Magento 2.4.9 EKS Auto Mode cold cell `20260813ax` passed ACM ResourceRecord-gated issuance, qualified Pulumi kubeconfig, expanded MySQL client image, signed-digest promote, seed import into RDS `database=magento`, kube runtime health (1 ready of 1 desired), Auto Mode catalog (`computeMode:auto-mode`, `queueMode:database`, `searchMode:disabled`), destroy of 85 resources, disposable state-bucket deletion, prerequisite-secret force-delete, and harness `assert_clean`; independent post-run inventories were empty for VPC/RDS/EKS/ElastiCache/S3/secrets; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up (customer KMS remains Enabled). Do not reuse `ax`. The Magento 2.4.9 EKS Fargate cold cell `20260813ay` passed ACM ResourceRecord-gated issuance, signed-digest promote, seed import into RDS `database=magento`, kube runtime health (1 ready of 1 desired), Fargate catalog (`computeMode:fargate`, `queueMode:database`, `searchMode:disabled`), destroy of 78 resources, disposable state-bucket deletion, prerequisite-secret force-delete, and harness `assert_clean`; independent post-run inventories were empty for VPC/RDS/EKS/ElastiCache/S3/secrets; leftover unused ISSUED ACM and ACM-validation CNAMEs were deleted in a follow-up (customer KMS remains Enabled). Do not reuse `ay`. Bounded managed-node-group and self-managed create/runtime/storage/application baselines are now recorded, but topology/failure-domain behavior, node-lifecycle resilience, broader storage/IAM/observability, ingress, and remaining EKS matrix live evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 2.3 Implement AWS durable recovery clients for RDS/Aurora, S3 media, Secrets Manager/configuration, state archives, and the selected queue/broker path with retention, encryption, protection, polling, and independent proof evidence. The official SQS adapter now covers a bounded, quiesced standard-queue export and same-region isolated replay, the S3 adapter now has normalized cleanup plus one bounded three-object media cell, the Secrets Manager adapter now has a provider-neutral synthetic-secret backup/isolated-restore/integrity cell with exact source/output cleanup, and the RDS/Aurora translator now has ownership-scoped manual snapshot/restore, deletion-protection-safe cleanup, and provider-only evidence with an isolated-database recovery command. The 2026-08-13 `eu-north-1` RDS MySQL cell passed encrypted snapshot/isolated-restore control-plane polling and exact cleanup, while explicitly recording `appFixture=not-run`. Live fixture/application verification and evidence for the remaining classes, versioned/Object Lock/retention/KMS variants, FIFO/whole-queue semantics, HA, and regional DR remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 2.3a Implement the AWS SQS official-SDK port and bounded export/replay translator with operator-approved quiescence, source-preserving visibility reset, stable MessageId tracking, batch partial-failure handling, deterministic ownership tags, encryption/retention limits, same-region isolated restore, verifier evidence, collision refusal, inventory, cleanup, and deterministic fake-client tests; the bounded `eu-west-3` live known-content cell passed with zero post-run queues, while broader broker/HA/DR claims remain owned by 2.3 and section 7.
- [x] 2.4 Implement AWS search rebuild and cache reconstruction as separate non-durable recovery operations with known-content integrity checks. The AWS native translator routes each data class through the shared projection lifecycle, rejects durable-backup semantics, and records application-read, service-health, cache-loss, and known-content verification evidence; deterministic fake-runtime tests cover start, resume, and integrity proof mapping.
- [x] 2.4a Implement the shared provider-neutral projection recovery contract and wire AWS/GCP native translators to injected runtime adapters; provider-specific runtime execution and live proof remain part of 2.4, 3.2, and section 7.
- [x] 2.5 Implement CloudWatch Logs/Metrics/alarms/dashboards/audit and ECS/EKS collection lifecycle, including signal labels, retention, redaction, alert, SLO, and cleanup verification. The backend now materializes and verifies owner, severity, deduplication, runbook, and ownership metadata on portable alarms, maps the supported comparison operators to the documented CloudWatch values, and rejects equality/invalid windows before mutation; the bounded 2026-08-13 CloudWatch cell passed five-resource logs/metrics/dashboard/alarm delivery, one-day retention, and exact cleanup. ECS/EKS collection, audit, notifications, broader redaction/SLO semantics, and full-matrix evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 2.5a Implement the AWS CloudWatch provider backend behind the shared observability port using official SDK clients, tag-backed ownership for log groups/dashboards/alarms, delivery probes, retention, direct inventory, foreign-resource refusal, and cleanup tests; full ECS/EKS collection delivery and live evidence remain open in 2.5 and 2.7.
- [x] 2.5b Add a disposable real-account CloudWatch Logs/metric/dashboard/alarm cell with bounded delivery polling, exact ownership inventory, independent cleanup, and a recorded correction for provider filter semantics; ECS/EKS collection, audit, alert notification, and full-matrix evidence remain open in 2.5 and 2.7.
- [x] 2.6 Implement CloudFront/WAF lifecycle for origin authentication/health, cache keys and bypass, TLS/DNS ownership, purge, security policy, failover, rollback, and direct inventory cleanup. The current adapter now implements an ownership-checked two-origin CloudFront origin-group control-plane transition and carries the expected origin-group identity, member ordering, and failover status codes through convergence polling. The 2026-08-13 `20260813n` cell passed ACM DNS issuance, origin-group apply, Deployed polling, `/*` invalidation, disable-then-delete destroy, and empty ownership inventory (`trafficImpact=not-run`). The 2026-08-13 `20260813s` cell attached a CLOUDFRONT-scope WAFv2 WebACL with whole-group `AWSManagedRulesCommonRuleSet` count mode (`waf=attached trafficImpact=not-run`); that is attach proof only, not Magento-safe rule content. Magento-safe WAF is now the portable policy (`waf/magento-safe`: managed groups block except Magento BODY/URI/QUERY count-overrides, 64 KB CloudFront inspection, site-wide IP rate limit). The 2026-08-13 `20260813t` cell proved alias HTTPS `200` through `ml-cf-20260813t.acourtiol.com` (`trafficImpact=alias-https`) and left empty inventories. The 2026-08-13 `20260813w` cell attached Magento-safe WAFv2 (`waf/magento-safe`: groups `OverrideAction.None`, Magento BODY/URI/QUERY count-overrides, `DefaultSizeInspectionLimit=KB_64`) to origin-group CloudFront (`d3rlll5vii9uia.cloudfront.net`, `waf=attached trafficImpact=not-run`) and left empty independent ACM/WAF/distribution inventories. The 2026-08-13 `20260813aa` cell swapped origin-group members (primary `example.com` → secondary `www.google.com`) on CachingDisabled CloudFront (`d1y84xbtceuvqo.cloudfront.net`) and measured viewer HTML title Google at `rtoSeconds=49` (`trafficImpact=failover-https`), then left empty independent ACM/distribution inventories. 5xx origin-group failover, fencing, failback, origin authentication, WAF block data-plane, and Magento origin remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 2.6a Implement CloudFront origin-group planning, origin-health gates, exact topology/drift checks, control-plane failover/rollback ordering, shared lifecycle capability exposure, and explicit control-plane-only evidence without claiming traffic convergence.
- [x] 2.7 Add deterministic AWS fake-client tests and disposable ECS/EKS live cells for each claimed architecture, with separate resilience, observability, edge, and cleanup evidence. The AWS cost adapter now provides account-free EKS capacity classification for the control plane, Auto Mode, managed node groups, self-managed nodes, Fargate, Valkey, database, and in-cluster search/queue shapes. The 2026-08-13 disposable Magento ECS Fargate cell `20260813ai`, Fargate Spot cell `20260813al`, and EC2 Auto Scaling cell `20260813aq` each passed create-once plus `queueMode:db` with independent stack cleanup; AWS ECS Managed Instances Magento `20260813ar` failed create (missing required `vcpu_count`/`memory_mib`); `20260813as` failed create (`instanceGenerations: current` with `m6i.large`, request `711827ff-5a22-453a-b913-78d8d54cfaa4`, 94/94 destroy); the retry Magento ECS Managed Instances cell `20260813au` passed create-once plus `queueMode:db` with independent stack cleanup; do not reuse `ar`/`as`/`at`; Magento EKS Auto Mode `20260813av` failed seed import (unqualified Pulumi `--stack`); do not reuse `av`; `20260813aw` failed seed import (unexpanded mysql client image); do not reuse `aw`; the retry Magento EKS Auto Mode cell `20260813ax` passed create-once plus `queueMode:database`/`searchMode:disabled` with independent stack cleanup; do not reuse `av`/`aw`/`ax`; the Magento EKS Fargate cell `20260813ay` passed create-once plus `queueMode:database`/`searchMode:disabled` with independent stack cleanup; do not reuse `ay`; self-managed EKS cell `20260817bf` now also passed the corrected AL2023 MIME NodeConfig boot path, two-node/two-AZ baseline, CloudWatch and EBS add-ons, EBS write/read/delete, Magento seed/deploy/runtime, direct ELB HTTP, and independent cleanup. EKS Price List, node/Spot interruption, node/zone failure, broader observability/edge, and the remaining architecture/evidence matrix remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).

### AWS EKS self-managed CloudWatch observability follow-up (tasks 2.5 and 2.7)

The bounded `20260817bj` self-managed EKS cell passed the AWS-managed
`amazon-cloudwatch-observability` add-on (`ACTIVE`, `v6.5.0-eksbuild.1`),
Linux CloudWatch Agent and Fluent Bit readiness (`2/2` each), an exact marker
event in `/aws/containerinsights/<cluster>/application`, one
`ContainerInsights/cluster_node_count` data point, and two
`kube-apiserver-audit` streams with 380 control-plane audit events. The cell
also proved retained-stack resume admission through the exact checkpoint
fingerprint, immutable digest, state-bucket ownership, non-empty Pulumi stack,
and ownership-marker scope; all 90 stack resources and the supporting
artifacts were independently removed. The first attempt exposed optional
Windows DaemonSets with desired `0`, so the readiness predicate now selects
the active Linux DaemonSets explicitly. This closes only a bounded EKS
collection/audit evidence slice: ECS collection, alert notifications,
redaction, SLO delivery, custom OTel configuration, performance/host/dataplane
delivery, and the full architecture matrix remain open in 2.5 and 2.7. See
[the live evidence](../../../docs/evidence/aws-eks-self-managed-cloudwatch-observability-live-20260817bj.md).

### AWS EKS managed-node-group follow-up (tasks 2.2 and 2.7)

The 2026-08-17 Magento attempt `20260817az` reached an ACTIVE EKS 1.36
control plane, but the managed node group never became usable: `aws-node`
reported `MissingIAMPermissions` for `ec2:DescribeNetworkInterfaces`, the
node was `NotReady`, CoreDNS was pending, and the database-grant Job reached
its 900-second deadline. The non-Auto node role omitted
`AmazonEKS_CNI_Policy`. The implementation now attaches that policy for
managed and self-managed nodes and has focused policy-matrix coverage. Exact
node-group deletion, stack teardown, state-bucket deletion, prerequisite
cleanup, ACM/DNS cleanup, log-group cleanup, and independent `assert_clean`
all passed. The `awsaz` tag is retired; fresh managed-node-group and
self-managed live cells remain required, so tasks 2.2 and 2.7 stay open.

See [the failed live evidence](../../../docs/evidence/aws-eks-managed-node-groups-magento-attempt-20260817az.md).

The next preflight-only attempt `20260817ba` stopped before stack creation
because the matching `Vpc=true` RDS `db.t4g.micro` orderable response listed
`eu-west-3b`/`eu-west-3c` and excluded configured `eu-west-3a`. The initial
diagnosis used a non-VPC CLI query and was corrected; no admission weakening
was retained. The exact state bucket, prerequisite secrets, certificates,
and DNS records were cleaned up. See [the preflight evidence](../../../docs/evidence/aws-eks-managed-node-groups-preflight-attempt-20260817ba.md).

The corrected managed-node-group attempt `20260817bc` reached an ACTIVE EKS
1.36 control plane, an ACTIVE managed node group, `aws-node` `2/2 Running`, a
successful database-grant Job, and a two-container web Deployment before the
EBS CSI add-on stalled. Its controller was `1/6 CrashLoopBackOff` because the
add-on had no `serviceAccountRoleArn` and fell back to unavailable EC2 IMDS
credentials while calling `DescribeAvailabilityZones`. The implementation now
creates a stack-owned EKS OIDC provider, a service-account-scoped EBS CSI IRSA
role, and passes that role to the add-on; EBS permissions were removed from the
broad EC2 node role. Focused fake-graph and trust-policy tests pass. The exact
add-on/node-group teardown, qualified Pulumi destroy, 707-object state-bucket
deletion, prerequisite/certificate/DNS/log cleanup, and independent
`assert_clean` all passed. A fresh live retry remains required; do not reuse
`awsbc`. See [the failed live evidence](../../../docs/evidence/aws-eks-managed-node-groups-ebs-identity-attempt-20260817bc.md).

The fresh managed-node-group Magento cell `20260817bd` passed the corrected
EKS 1.36 create-once path, CNI/CoreDNS readiness, dedicated EBS CSI IRSA
identity, `ACTIVE` EBS add-on, disposable EBS volume write/read/delete,
database grant and sanitized Magento seed import, signed immutable image
deploy, direct ELB HTTP `200` homepage rendering, and exact independent
cleanup. The final managed node group used two `m6i.large` nodes across
`eu-west-3b`/`eu-west-3c`; the initial 1/1/1 shape was raised to 2/2/2 after
the migration Job hit `Insufficient cpu`, and the change was reconciled by a
normal Pulumi update. The final application image was a run-scoped signed
replacement because the original image had FPM `clear_env = yes` and the first
replacement lacked `pub/static/deployed_version.txt`; both replacement
artifacts and the stack were deleted. See [the live evidence](../../../docs/evidence/aws-eks-managed-node-groups-magento-live-20260817bd.md)
and [the implementation notes](implementation-notes.md).

This closes the bounded managed-node-group create/runtime/storage/application
cell, but tasks 2.2 and 2.7 remain open for self-managed nodes, node-loss and
interruption resilience, broader architecture/matrix coverage, full EKS
observability delivery, edge/TLS, and other non-claims in the evidence.

The first self-managed Magento cold attempt `20260817be` reached an ACTIVE
EKS 1.36 control plane and two `InService` EC2 instances but registered zero
Kubernetes nodes. Both AL2023 console logs showed `nodeadm-config` parsing the
shell user data as YAML (`mapping values are not allowed in this context`);
the CloudWatch add-on became `DEGRADED` and the database-grant Job hit its
900-second deadline. The source now emits an AWS-documented MIME
`application/node.eks.aws` NodeConfig part with a legacy bootstrap fallback;
focused tests pass, but no self-managed live claim is made until a fresh cold
run succeeds. The `awsbe` marker is retired. See [the failed evidence](../../../docs/evidence/aws-eks-self-managed-magento-attempt-20260817be.md)
and [the implementation notes](implementation-notes.md).

The corrected self-managed Magento cold cell `20260817bf` passed the fresh
AL2023 MIME NodeConfig boot path, two `Ready` nodes across `eu-west-3b` and
`eu-west-3c`, CloudWatch and EBS add-ons, a disposable EBS write/read/delete
probe, Magento seed/deploy/runtime health, queue/database and disabled-search
cells, direct ELB HTTP `200` homepage rendering, and exact independent
cleanup. Pulumi destroyed 90 resources in 16m1s; the state bucket, three
secrets, two ACM certificates, two ACM validation records, six log groups, and
the run-scoped ECR repository were deleted, and `assert_clean` returned
`assert_clean ok`. This closes only the self-managed create/runtime/storage/
application baseline; tasks 2.2 and 2.7 remain open for failure resilience,
broader observability/edge, topology/DR, and the remaining matrix. See [the
live evidence](../../../docs/evidence/aws-eks-self-managed-magento-live-20260817bf.md)
and [the implementation notes](implementation-notes.md).

The fresh self-managed node-loss cell `20260817bh` then selected a Ready node
by Kubernetes provider ID, cross-checked its exact EC2/cluster/ASG identity,
terminated `i-0162e28d0b0a7d17a` through `ec2:TerminateInstances`, and observed
distinct ASG replacement `i-067e1bc97e6941c5c` become `Ready` in
`eu-west-3b` while the existing `eu-west-3c` node stayed Ready. The cell
preserved 2 Ready nodes across 2 zones, measured a 71-second
replacement/runtime-health interval, passed the post-replacement runtime
health probe, destroyed all 90 resources, removed the stack and versioned
state bucket, and passed exact prerequisite, DNS, certificate, log, ECR, and
independent `assert_clean` cleanup. This closes only the bounded single-EC2
termination/self-managed-ASG replacement claim; zone loss, simultaneous node
loss, interruption variants, direct failure-interval HTTP, full EKS
observability, edge/TLS, topology/DR, and the remaining matrix stay open. See
[the node-loss evidence](../../../docs/evidence/aws-eks-self-managed-node-loss-live-20260817bh.md)
and [the implementation notes](implementation-notes.md).

The fresh self-managed declared-zone loss simulation `20260817bi` selected
all Ready nodes in `eu-west-3b`, verified the exact provider IDs, EC2
instances, cluster tag, and self-managed ASG membership, terminated the
selected zone's one Ready instance, observed a zero-Ready gap in that zone,
and observed distinct same-zone ASG/Kubernetes replacement
`i-0d8b4a892e21f54d1` become Ready while the `eu-west-3c` node remained Ready.
Ready count and declared-zone count returned to `2/2`, runtime health passed,
the measured replacement/runtime-health interval was 71 seconds, and all 90
resources plus exact stack, state, secret, certificate, DNS, log, ECR, and
owning-service artifacts were removed. This is a provider-API zone-loss
simulation only; it does not claim a physical AWS AZ outage, zonal service
isolation, regional DR, direct failure-interval HTTP, queue/database/cache
integrity, or edge behavior. See [the zone-loss evidence](../../../docs/evidence/aws-eks-self-managed-zone-loss-live-20260817bi.md)
and [the implementation notes](implementation-notes.md).

## 3. GCP GKE and managed-service lifecycle clients

- [x] 3.1 Implement GKE Autopilot and Standard client mappings with explicit node/workload/storage/ingress and failure-domain behavior.
- [x] 3.2 Implement Cloud SQL, Cloud Storage, Secret Manager, state archive, and declared queue/broker recovery clients with normalized backup, restore, integrity, ownership, and polling evidence. The GCP Pub/Sub snapshot/seek translator, Cloud SQL backup/isolated-restore path, Cloud Storage object cleanup path, and Secret Manager provider-label boundary are implemented; two bounded MySQL 8.4 live cells, one three-object Cloud Storage live cell, one synthetic-secret Secret Manager cell, and a repeatable seven-day Pub/Sub snapshot/seek cell now pass provider evidence and exact cleanup. Same-region in-place Cloud SQL, Cloud Storage, Secret Manager, infrastructure-state, and audit-evidence cells passed on 2026-08-18. GCP Pub/Sub isolated restore onto a new owned subscription passed on 2026-08-18. Provider-wide durable coverage, Secret Manager CMEK/version/retention variants, regional DR, and final promotion remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 3.2a Implement the concrete GCP Cloud SQL, Cloud Storage, Secret Manager, and official Pub/Sub snapshot/seek translators plus the shared provider-neutral object archive engine; regional DR, live proof, and final promotion remain owned by 3.6 and section 7.
- [x] 3.2b Add GCP Pub/Sub v2 SDK translation, deterministic ownership-labeled snapshot reuse, same-region seek restore, application-verifier evidence, seven-day retention/CMEK/destination refusal, collision-safe labels, inventory, cleanup, and fake-client tests without leaking protobufs into the shared contract.
- [x] 3.3 Implement Memorystore Valkey semantic-version mapping so Valkey 9.0 remains the default GA profile and Valkey 9.1 remains an explicitly Preview profile, with source-dated regression tests. Provider-native lifecycle and live acceptance remain gated by 3.2 and 3.6.
- [x] 3.4 Implement GKE/Google Cloud Logging, Cloud Monitoring, Managed Service for Prometheus, audit, dashboards, alerts, SLO, retention, redaction, and cleanup verification for Autopilot and Standard separately. The provider-local Logging SDK/Pulumi path now manages an owned custom bucket and sink with retention, exact `_AllLogs` routing verification, provider-tombstone-aware cleanup, and actionable alert owner/severity/deduplication/runbook metadata verification; bounded six-resource live cells passed on 2026-08-12 and 2026-08-13. The 2026-08-18 Autopilot cell `20260818c` and Standard cell `20260818d` each proved native workload Cloud Logging, `kubernetes.io/container/uptime`, and `container.googleapis.com` audit, then exact cluster delete (`c`: logs=17 metrics=1 audit=2 in 94s; `d`: logs=11 metrics=1 audit=2 in 58s). Autopilot GMP `20260818f` (`gke-gmp-system`) and Standard GMP `20260818g` (`gmp-system`) each scraped `prometheus.googleapis.com/example_requests_total/counter` (`msp=2`, 93s); `20260818e` is retired. Autopilot `20260818j` and Standard `20260818k` each applied an owned GKE-tied user log metric plus Monitoring policy then exact cleanup (`j`: alerts=1 in 95s; `k`: alerts=1 in 77s); `20260818h`/`i` are retired. This is not firing or notification-channel delivery. The Pulumi component emits `alert.notification-channel` in `UnavailableOperations` whenever alerts are requested and omits Monitoring notification channels. The adapter refuses a non-empty `RedactionPolicy` (`Google Cloud observability redaction requires a managed data-protection policy`). Closed 2026-08-18 on those proofs plus the typed gaps. Magento-wired native composition, live paging delivery, and managed DLP remain in 4.3, 4.8, 4.9, 3.6, and section 9.
- [x] 3.4a Implement the Google Cloud Service Monitoring SLO lifecycle behind the provider-local official API port and Pulumi resource path, using the existing opaque Monitoring Service reference, deterministic ownership IDs, rolling-window validation, idempotent apply, configuration-drift and foreign-collision refusal, scoped inventory, verification, and cleanup tests; GKE-tied notification-channel delivery, managed redaction/DLP, and Magento-wired architecture remain in 4.3, 4.8, 4.9, and section 9.
- [x] 3.4b Add a disposable real-project Google Cloud Logging/Monitoring probe with alert/dashboard verification, exact metric-descriptor ownership, bounded eventual-consistency polling, an owning-API cleanup fallback, and an independent zero-resource scan; GKE Autopilot/Standard native collection, GMP, audit, and GKE-tied alert apply are evidenced in 3.4 (`20260818c`/`d`/`f`/`g`/`j`/`k`). Notification-channel delivery, managed redaction/DLP, and full-matrix Magento wiring remain in 4.3, 4.8, 4.9, 3.6, and section 9.
- [x] 3.5 Implement Google Cloud load balancing, Cloud CDN, and Cloud Armor lifecycle with health checks, TLS/DNS, purge, security policy, failover, rollback, and direct inventory cleanup. The concrete Compute adapter now distinguishes ownership/topology from desired TLS state and rotates certificates on an owned target HTTPS proxy through the documented `setSslCertificates` operation, with deterministic official-client translator coverage. The 2026-08-13 `20260813p` cell applied then destroyed after the managed cert stayed non-ACTIVE for 20 minutes. The 2026-08-13 `20260813r` cell reached Google-managed cert ACTIVE, destroyed the forwarding rule/proxy/URL map, and left empty independent inventories (`trafficImpact=not-run`). The 2026-08-13 `20260813y` cell proved alias HTTPS `200` through `ml-gcp-edge-20260813y.acourtiol.com` (`34.98.114.10`, origin `www.google.com`) after GFE lag, then destroyed with empty magelift-owned inventories (`trafficImpact=alias-https`). The 2026-08-13 `20260813ag` cell attached Magento-safe Cloud Armor (`waf/magento-safe`: beta `patchRule` body exclusions, GraphQL JSON parsing, 64 KB inspection, sensitivity 1) with `waf=attached trafficImpact=not-run` and empty independent inventories. The `codexarmor0815` wrapper now supports an explicitly owned temporary Armor deny-rule data-plane assertion and origin-title verification, but its 2026-08-15 attempt stopped before traffic because the managed certificate remained aggregate `PROVISIONING` with domain `FAILED_NOT_VISIBLE`; cleanup was empty. The `mgpv0815d` real preview and `mgce0815d` live attempt then moved Cloud Armor rules to dedicated `SecurityPolicyRule` resources, proved the parent policy plus all 12 rules could be created, and stopped before Magento traffic when GKE reported insufficient `europe-west1` capacity; cleanup passed. Follow-up `mgce0816i` passed the installed-schema Magento 2.4.9 workload, direct origin HTTPS/title, public alias HTTPS/title, public Cloud Armor `403` data plane, exact rule restoration, and independent zero-resource cleanup. See [`gcp-native-edge-armor-rule-resource-live-2026-08-15.md`](../../../docs/evidence/gcp-native-edge-armor-rule-resource-live-2026-08-15.md). The 2026-08-17 `20260817bl` cell passed health-gated two-origin URL-map failover through alias HTTPS (`www.google.com` → `developers.google.com`, `rtoSeconds=116`, `trafficImpact=failover-https`) after cache invalidation, then independent cleanup; `20260817bk` is retired after a destination-backend ownership-check false refusal. Rollback traffic, Magento-origin failover, Armor-on-failover, and automatic 5xx backend failover remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 3.5a Implement provider-owned GCP two-origin URL-map failover/rollback with health gates for both origins, ownership/drift checks, bounded control-plane convergence, shared lifecycle actions, and explicit control-plane-only evidence; traffic-impact proof and live acceptance remain open.
- [x] 3.6 Add deterministic GCP fake-client tests and disposable Autopilot/Standard live cells, including Valkey 9.0 GA and separately gated 9.1 Preview evidence. The 2.4.9 Autopilot preview, Standard/HA cells, dedicated `VALKEY_9_1` Preview infrastructure cell, and the 2026-08-12/13 GKE Standard `VALKEY_9_0` infrastructure-only cell all completed with independent cleanup; the Valkey 9.0 probes independently proved active provider metadata with one shard, zero replicas, and provider-reported `MULTI_ZONE` distribution. The 2026-08-13 Magento 2.4.9 GKE Autopilot cell `20260813aj` passed the 12-cell preview catalog (seed import, kube runtime 1/1, PSA-race-aware destroy, empty independent `gcpaj` inventories); that is kube runtime health, not HTTP/TLS. The 2026-08-13 GKE Standard Magento attempt `20260813am` failed `queue:health` because PROFILE was `preview`; do not reuse `am`. The retry Magento 2.4.9 GKE Standard cell `20260813an` passed the 13-cell standard catalog (PROFILE=`standard`, seed import, kube runtime 2/2, RabbitMQ 1/1, PSA-race-aware destroy, empty independent `gcpan` inventories); that is kube runtime health, not HTTP/TLS. The GCP Pub/Sub v2 snapshot/integrity/same-region-seek cell also passed. The 2026-08-17 GCP GKE Standard HA `gcha22` run passed all 16 catalog cells, including pod loss, backing-VM node loss, and a bounded declared-zone backing-VM deletion simulation: all Ready nodes in `europe-west1-b` were deleted only after exact GCE identity validation; a Ready-node gap was observed; 3/3 nodes and 3/3 zones recovered; runtime, queue, search, and database checks passed; measured recovery was 275 seconds. The harness now resolves actual zones from the selected region rather than assuming `REGION-b/c/d`. This is not a physical provider zone-outage, fencing, or DR claim. Native observability/edge, backup/restore, HA failure beyond these bounded cells, DR, and Magento HTTP remain in their owning tasks and are not implied by this row.
- [x] 3.6c Add and run a marker-owned GCP Autopilot preview cell after `deploy:candidate` that executes the normal non-interactive `deploy --yes --digest` command against the existing lifecycle a second time, verifies runtime health and the Magento CLI version again, and records independent cleanup/`assert_clean` evidence. The 2026-08-15 `codexrpt0815b` run passed all 13 preview cells, reused the same stack fingerprint for candidate and repeat deploys, imported the installed-schema seed, verified Magento CLI `2.4.9` and 1/1 runtime health after both deploys, and finished with empty marker inventories plus `assert_clean ok`; see [`gcp-gke-autopilot-magento-repeat-live-20260815.md`](../../../docs/evidence/gcp-gke-autopilot-magento-repeat-live-20260815.md). This remains bounded to the existing preview lifecycle and does not imply public traffic, HA, DR, or billing evidence.

## 4. Scaleway Kapsule lifecycle clients

- [x] 4.1 Implement Kapsule control-plane, node-pool, topology, storage, ingress, and ownership lifecycle mappings.
- [x] 4.2 Implement Managed Database, Object Storage, Secret Manager, and state-archive recovery clients with provider-specific retention, encryption, protection, polling, and integrity evidence. The Scaleway Object Storage media path and one bounded Secret Manager synthetic-secret backup/isolated-restore/integrity cell now have live evidence; the stack-managed application Secrets port also uses official Secret Manager CRUD with project/region scoping and ownership tags. The cell enabled a short acceptance-only Object Lock retention, purged exact object versions/delete markers, and left only the provider's free scheduled-deletion source tombstone. The 2026-08-13 Scaleway RDB `fr-par` MySQL-8 `DB-DEV-S` cell `20260813m` passed encrypted SBS block-snapshot backup, isolated restore, polling, and exact cleanup (direct instance/snapshot inventories `[]`), while explicitly recording `appFixture=not-run` and `protection=provider-optional`. Managed Database application-fixture/HA/CMEK/regional-DR, production retention/Object Lock variants, CMEK/rotation, live application-Secret CRUD/IAM, and remaining HA evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 4.2a Implement the Scaleway S3-compatible Object Storage translator using the shared archive engine, including paginated inventory, sealed manifests, encryption, Object Lock, isolated restore, integrity, idempotency, and resumable operation identities; managed database, Secret Manager, live, and regional-DR work remains open in 4.2 and section 7.
- [x] 4.2b Implement the Scaleway Managed Database RDB block-snapshot translator behind a provider-local SDK port, including deterministic ownership, encrypted-volume validation, snapshot expiry, resumable backup, isolated HA restore, integrity verification, inventory, and typed refusal of unencrypted logical-backup and alternate-region paths; Secret Manager, live, and regional-DR evidence remain open in 4.2 and section 7.
- [x] 4.2c Implement the Scaleway Secret Manager translator behind a provider-local SDK port, sealing values through the shared archive envelope and Object Storage Object Lock, restoring protected versions idempotently, verifying integrity without exposing values, and inventorying owned secrets; the bounded `fr-par` live cell passed with exact active-resource cleanup and a scheduled-free source tombstone, while production retention/CMEK, rotation/revocation, and regional-DR evidence remain open in 4.2 and section 7.
- [x] 4.3 Keep managed Redis/cache reconstruction separate from durable recovery and keep search and queue unavailable unless an official adapter-backed capability is present.
- [x] 4.4 Implement Scaleway Cockpit signal, log, trace, alert, dashboard, SLO, retention, redaction, and cleanup verification with explicit unavailable signal rows.
- [x] 4.4a Add the bounded real Scaleway Cockpit custom logs/metrics cell with active-profile SDK authentication, marker-scoped token/source ownership, one-day retention, OTLP plus Mimir/Loki query verification, exact owning-service cleanup, and independent CLI inventory; Kapsule workload wiring, traces, audit, alert/dashboard/SLO lifecycle, and full-matrix evidence remain open.
- [x] 4.5 Implement only the source-dated Scaleway load-balancer and Edge Services paths whose API ownership and cleanup are verified; emit typed unavailable/unsupported results for the rest. Multi-backend path/method routing is provider-owned; health-driven failover and rollback remain typed unsupported.
- [x] 4.6 Add deterministic Scaleway fake-client tests and disposable Kapsule live cells with direct owning-service inventories and independent cleanup evidence. The exact `scw0811` single-zone cell passed with 23 resources created and deleted, and post-run RDB/Redis/Kapsule/private-network inventories were empty; multi-zone HA, Magento runtime, backup/restore, regional DR, and day-two evidence remain open.
- [x] 4.6a Run the bounded real Kapsule infrastructure-only cell with a valid `/22` private-network range, one current `DEV1-L` node, exact Pulumi/owning-service teardown, and independent empty Kapsule/RDB/Redis/private-network/load-balancer inventories; Magento-compatible runtime health, multi-zone HA, backup/restore, DR, and day-two evidence remain open in 4.2, 4.6, and section 7. The `scwrc1` 2026-08-13 cell created 24 resources in 8m23s, deleted them in 2m26s, and passed all five direct post-run zero-prefix inventories.

## 5. OVHcloud MKS lifecycle clients

- [x] 5.1 Implement MKS control-plane, worker, topology, storage, ingress, and ownership lifecycle mappings.
- [x] 5.2 Implement Managed Database, Valkey, Object Storage, secret/key-reference, and state-archive recovery clients with documented RPO limits and normalized evidence. The OVHcloud Object Storage path now has one bounded three-object same-region backup/isolated-restore/integrity/cleanup cell with disposable-user revocation and exact bucket deletion; the Secret Manager adapter now has exact marker-owned restore cleanup and typed Object Lock-pending handling, but the authenticated account had no existing OKMS service for a live cell. The 2026-08-13 GRA MySQL `essential`/`db1-4` cell `20260813q` passed encrypted provider-backup selection, isolated `forkFrom` restore, polling, and exact cleanup (direct instance list `[]`), while explicitly recording `appFixture=not-run` and `protection=provider-optional`. Valkey, Secret Manager live/CMEK/retention variants, Object Lock/versioning, HA, regional DR, and application-integrity evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.2a Implement the OVHcloud S3-compatible Object Storage translator using the shared archive engine, including paginated inventory, sealed manifests, encryption, Object Lock, isolated restore, integrity, idempotency, and resumable operation identities; the bounded 2026-08-12 unversioned three-object live cell passed with provider-local cleanup and exact bucket/user deletion, while retention variants and regional-DR evidence remain open in 5.2 and section 7.
- [x] 5.2b Implement the OVHcloud Public Cloud Database provider-managed backup selection and `forkFrom` isolated restore for the documented MySQL, PostgreSQL, and Valkey/Redis API paths behind a provider-local SDK port, including source identity, encrypted-backup and retention/age checks, optional HA, polling, idempotency, integrity, inventory, and typed refusal where deletion protection or alternate-region evidence is absent; the 2026-08-13 MySQL control-plane cell `20260813q` passed live backup select, isolated restore, and empty inventory, while application-fixture, Valkey, HA, and regional-DR evidence remain open in 5.2 and section 7.
- [x] 5.2c Implement the OVHcloud Secret Manager versioned secret/key-reference translator behind the official OKMS SDK port, requiring a configured OKMS identity, active-version and ownership metadata checks, sealed Object Storage archive/Object Lock protection, idempotent isolated version restore, exact marker-owned restore deletion, integrity without value disclosure, direct inventory, and typed refusal before mutation without archive protection; no OKMS service domain was available for the bounded live cell, so live credentials, production retention/CMEK, and regional-DR evidence remain open in 5.2 and section 7.
- [x] 5.3 Implement the documented MKS audit-log forwarding to Logs Data Platform using an opaque existing stream reference, and record generic workload signal gaps as unavailable.
- [x] 5.4 Implement only source-dated OVHcloud load-balancer and CDN paths whose routing, TLS/DNS, security, failover, rollback, and cleanup operations are verified. The current MKS adapter deliberately claims only the documented L4 Octavia LoadBalancer path and emits typed rejection for native TLS, CDN, WAF, purge, and failover; it is not a full closure of this task. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 5.4a Make the OVH native failover boundary explicit with a typed pre-mutation capability error and regression coverage; broader OVH native CDN/TLS/WAF/failover lifecycle remains open.
- [x] 5.5 Add deterministic OVHcloud fake-client tests and disposable MKS live cells with direct owning-service inventories and independent cleanup evidence. The current `ovh812` EU-WEST-PAR infrastructure-only cell created and deleted 23 resources, reconciled a transient subnet-port 409 through ownership-scoped retries, and returned empty MKS, managed-database, load-balancer, and private-network inventories; Magento runtime, HA, backup/restore, DR, observability, and edge evidence remain open in their owning tasks.

## 6. External Fastly and New Relic integrations

- [x] 6.1 Complete the Fastly service/domain/TLS/VCL or policy/origin-health/routing/cache/purge/failover/rollback/destroy client with asynchronous deletion and ownership-scoped inventory. The current lifecycle has current versionless Domain Management service/domain/managed-TLS ACME challenge/VCL/purge/routing/destroy proof and an exact-marker crash cleanup path. A 2026-08-13 replacement-credential cell using the Fastly CLI login session (not `--token default`) repeated managed-TLS/ACME, HTTPS routing, purge, and independent zero-inventory cleanup while preserving the unmarked service. The provider boundary now strictly decodes advanced `extensions.fastly.edge` version/VCL/origin-backend/route-snippet settings for both the public SDK and CLI paths; failover, rollback, policy-specific WAF/cache/purge proof, Magento origin routing, and provider-wide live certification remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 6.1a Add the official Fastly service-version boundary: existing-service versioned mutations clone an editable version, validate it, activate it only after mutation, and restore the previous active version when post-activation verification fails; existing-service cleanup uses the same clone/validate/activate ordering, while adapters without this capability reject the mutation before changing state. Traffic failover/failback exercises and policy-specific routing mutations remain open in 6.1 and section 7.
- [x] 6.2 Implement the typed New Relic collector-path selector and OTLP/HTTP exporter for ECS, Kubernetes, and generic workloads without exposing New Relic object schemas in the core. The ECS mapping is the documented OpenTelemetry Collector Contrib sidecar for ECS on EC2/Fargate; Kubernetes maps to the documented NRDOT collector chart; generic workloads use OTLP or are explicitly unsupported for a native collector path. The provider-owned NerdGraph/NRQL marker verifier is now an injectable delayed-delivery proof path; live EU Go OTLP delivery for logs, metrics, and traces passed with separate ingest and NerdGraph credentials, while collector deployment evidence remains open.
- [x] 6.2a Implement injected collector deployment translators for ECS task definitions and Kubernetes Helm/manifests, including secret references, ownership markers, signal coverage, health/readiness, rollback, and direct inventory cleanup. The Contrib ECS task-definition and client-go Kubernetes implementations are covered by 6.2c; the provider-owned NRDOT chart translator now maps the documented `nr-k8s-otel-collector` values, Secret reference, RBAC, provider mode, endpoint, and supported signals behind the injected release SDK, one shared Helm-backend factory is bound by thin EKS, GKE, Kapsule, and MKS adapters, and the shared Kubernetes Contrib backend emits one credential-specific YAML document, cleans up only resources created by a failed partial Ensure, and polls direct Kubernetes/Helm ownership inventories after delete/uninstall before reporting cleanup complete. The internal and public module contracts now also expose a planned collector-deployment factory seam so provider/community implementations can bind target-scoped clients after planning without leaking SDK types into core code. The Kapsule/MKS mapping uses the same provider-neutral Kubernetes/Helm boundary and does not imply provider-specific live compatibility; live cluster delivery evidence remains open here. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 6.2b Implement the provider-neutral SDK collector deployment port and shared idempotent lifecycle engine, with target-bound AWS ECS/EKS, GCP GKE, Scaleway Kapsule, and OVHcloud MKS constructors, ownership refusal, readiness/health/signal-delivery gates, rollback, and direct inventory cleanup; concrete Contrib backends are recorded in 6.2c, while NRDOT Helm and live delivery evidence remain open in 6.2a and section 7.
- [x] 6.2c Implement the concrete AWS ECS Contrib task-definition backend and reusable Kubernetes client-go Contrib backend, with immutable images, secret references (never values), exact ownership collision checks before mutation, readiness/health/signal gates, stale-task rejection, rollback, and direct inventory cleanup; provider-owned NRDOT chart translation is covered by 6.2a, and live signal-delivery evidence remains open.
- [x] 6.2d Implement the reusable injected Kubernetes Helm release backend with immutable chart-version validation, opaque semantic requests, exact ownership and configuration-drift refusal, readiness/health/signal gates, rollback, and direct owning-release cleanup; provider-owned NRDOT chart translation is covered by 6.2a, and live delivery evidence remains open.
- [x] 6.3 Implement credential validate/rotate/revoke behavior for Fastly and New Relic using only secret references and normalized operation/version identities.
- [x] 6.4 Add composition tests for native-only, external-only, and combined native-plus-external observability and edge paths across all four origin providers.
- [x] 6.5 Run disposable Fastly and New Relic live acceptance only with replacement credentials, ownership markers, direct inventories, redaction checks, and post-destroy cleanup evidence. Fastly versionless managed-TLS/routing/purge/destroy proof is recorded for 2026-08-09, and a replacement Fastly CLI-login cell with the same lifecycle plus independent zero-inventory cleanup is recorded for 2026-08-13. Bounded authenticated New Relic CLI marker/NRQL data-plane evidence is recorded for 2026-08-09. The 2026-08-11 and 2026-08-13 live EU Go OTLP paths created and revoked replacement ingest keys, then passed logs, metrics, and traces with exact-marker labels and no persistent New Relic resource. The 2026-08-13 EU NerdGraph cell additionally created and verified one owned policy/NRQL condition, private dashboard, and application-health service level, exercised partial-mutation rollback, and proved exact post-destroy inventory cleanup. The provider adapter now rejects unsupported alert maintenance-policy metadata before GraphQL and requires the exact ownership-marker prefix when inventorying dashboards. Collector deployment, New Relic retention/redaction/alert-firing evidence, and full provider-wide cleanup remain open. The Go verifier and NerdGraph adapter are live-certified for these exact bounded cells, not for collector or provider-wide claims. CLI bootstrap/login/infrastructure/edge mutations now share a provider plan-admission guard; the certification-specific adapter and provider-wide live admission evidence remain open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).

## 7. Recovery and failure exercises

- [x] 7.1 Materialize the scrubbed known-content fixture into each supported durable service and verify manifests, checksums, counts, application reads, permissions, secret references, health, and measured restore duration. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 7.2 Run same-region restore-in-place and isolated-destination restores for each provider/data-class profile; keep search rebuild and cache reconstruction as separate stages. GCP Cloud SQL has isolated (2026-08-15) and in-place (2026-08-18) live cells. GCP Cloud Storage has isolated (2026-08-12) and in-place (2026-08-18) live cells. GCP Secret Manager has isolated (2026-08-12 / 2026-08-14) and in-place (2026-08-18) live cells. GCP infrastructure-state has in-place (2026-08-18) live evidence on the shared object engine. GCP audit-evidence has in-place (2026-08-18) live evidence on the shared object engine. GCP Pub/Sub has same-subscription seek (2026-08-14) and isolated restore (2026-08-18) live evidence. Remaining providers and data classes stay open. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 7.3 Run process/task/pod, node, zone, topology, traffic, queue, database, cache, search, and integrity failure injections for every architecture that claims HA. GCP GKE Standard Magento known-content HA closed 2026-08-20 `gcha36` (pod/node/zone-loss seed-probe PASS, experimental, not public certified). Physical zone outage remains typed unsupported. AWS/OVH/Scaleway HA Magento stay experimental / not RC1-required. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 7.4 Run alternate-region recovery, fencing, single-writer, split-brain, failover approval, failback, stale-origin prevention, and measured RPO/RTO exercises. The provider-neutral SDK now enforces approval safety proofs even for approval-only restore stages. GCP `gcp.gke` fencing, failback, and `alternate-region` are typed unsupported. Remaining AWS/OVH live fencing is `live.aws` / `live.ovh-scw` in the RC1 split.
- [x] 7.5 Run corrupted-data, failed-deployment, lost-credential, provider-outage, region-loss, partial-restore, and interrupted-teardown scenarios and classify each as automated, operator-assisted, or typed unsupported. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 7.6 Promote no resilience capability row until the provider operation IDs, proof evidence, cleanup truth, and profile-specific acceptance records are sealed and independently verifiable. **2026-08-20:** `gencertdocs` recorded Magento catalog JSONL as evidence input; architecture/resilience proofs on that run are 0; catalog resilience rows stay experimental. No unverified promotion.

## 8. Efficient certification execution

- [x] 8.1 Build, sign, publish, and record one immutable artifact for each compatible runtime contract before any provider group starts. The provider-neutral run path and durable file-backed registry enforce one claim/publish/reuse boundary; multi-host runners inject their own conditional object-store/database registry without changing core logic. Focused evidence is recorded in `docs/evidence/provider-neutral-boundaries-2026-08-11.md`.
- [x] 8.2 Add provider-specific admission checks for quota, account/project, credentials, network, state backend, fixture, ownership, and local CPU/memory; block paid mutations when any check is unavailable. AWS ECS/EKS, GCP, Scaleway, and OVHcloud expose the shared read-only boundary, the public SDK admission bridge carries redacted local capacity, and focused negative/live pre-mutation checks pass; complete paid-matrix evidence remains in 9.3 and the provider-specific live rows.
- [x] 8.3 Implement complete reuse fingerprints and a session/fixture registry that can reuse compatible stacks, migrations, backups, observability setup, and edge setup without creating a duplicate writer. The local/CI `FileSessionRegistry` persists exact claims and cleanup state; distributed runners inject a conditional implementation. Provider live reuse remains evidence-gated.
- [x] 8.4 Execute only required cold boundaries plus bounded warm transitions, with independent evidence for every cell and serialized shared mutation keys.
- [x] 8.5 Persist durable checkpoints for architecture, operation IDs, backup/restore IDs, telemetry/edge identities, ownership, reuse mode, completed stages, and cleanup state; resume only matching scopes. `FileScheduleCheckpointStore` supplies atomic local/CI persistence, rejects stale fingerprint/ownership writes, and receives terminal PASS/FAIL writes through a bounded detached context after cancellation; `RunResilience` now uses the same bounded detached checkpoint boundary, records a completed stage before returning caller cancellation, resumes that stage without repeating provider work, and rejects secret-like adapter results or loaded/written checkpoint values; the shared shell checkpoint helper now atomically finalizes cleanup state after provider-owned inventory convergence and records Kubernetes cell duration before checkpointing; distributed runners inject a transactional checkpoint backend.
- [x] 8.6 Poll delayed deletions through owning-service inventories, reconcile protected resources explicitly, and retain only approved debug resources with owner, expiry, and reason. All dependency-gated top-level acceptance wrappers now also use the shared TTL watchdog and stop it before ownership cleanup; the recovery/observability lifecycle guard tests cover the complete wrapper set.
- [x] 8.7 Publish cost, duration, cloud-operation time, resource lifetime, artifact reuse, warm-transition count, concurrency, and cleanup-latency evidence for each provider group.

## 9. Evidence, documentation, and closure

- [x] 9.1 Generate capability, architecture, resilience, observability, edge, certification, and recovery-runbook documents from the source-dated catalog plus validated sealed JSONL evidence only.
- [x] 9.2 Run local semantic, schema, secret-safety, factory, provider-contract, scheduler, checkpoint, and generated-document gates before live resources are provisioned.
- [x] 9.3 Run the complete required live matrix, record blocked/unsupported/failed cells without retrying them as PASS, and verify final provider inventories. **2026-08-20 RC1 close:** remaining variants are experimental or typed unsupported; certified bar stays AWS ECS Fargate and GCP GKE Autopilot. No new other-cloud Magento. See [`openspec-rc1-remaining-classification-20260820.md`](../../../docs/evidence/openspec-rc1-remaining-classification-20260820.md).
- [x] 9.4 Scan configuration, schema, plans, logs, evidence, and docs to ensure no singular legacy edge/observability provider field or plaintext credential was reintroduced. Schema/config regression tests and repository scans pass; intentional documentation/spec references describe the rejected fields rather than reintroducing them.
- [x] 9.5 Review every non-certified row and source date, promote only evidenced capabilities, update linked OpenSpec task ownership, and close this change only when all required implementation and live-evidence gates pass. **2026-08-20:** public certified remain AWS ECS Fargate and GCP GKE Autopilot. GKE Standard / EKS / OVH / Scaleway stay experimental. Sealed GCP Magento JSONL is evidence input, not an automatic family promotion.
