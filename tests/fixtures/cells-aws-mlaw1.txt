# mlaw1 session catalog: campaign catalog with searchMode:provisioned per ADR 0012 + plan 1.5 (no serverless apply).
# Requires MAGELIFT_AWS_ACCEPTANCE_KEEP=true and MAGELIFT_AWS_ACCEPTANCE_ALLOW_COSTLY=true.
# Origin YAML must list three AZs (amazon-mq CLUSTER_MULTI_AZ) and start on
# computeMode:fargate, queueMode:db, searchMode:disabled, databaseEngine:rds-mysql.
# Cold Managed Instances and EKS stay on their architecture catalogs / workdirs.
# Destroy plus orphan assert at campaign close. Do not promote experimental cells.
# observability:xray stays typed unsupported until an ObservabilityAdapter registers X-Ray.
computeMode:fargate
queueMode:db
queueMode:ecs-rabbitmq
queueMode:amazon-mq
searchMode:disabled
searchMode:provisioned
databaseEngine:rds-mysql
databaseEngine:aurora-mysql
ha:multi-az
