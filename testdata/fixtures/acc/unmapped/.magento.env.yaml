# Synthetic ACC env — allowlisted + residual vars (D-07 / D-05).
stage:
  global:
    SCD_STRATEGY: compact
    CUSTOM_FEATURE_FLAG: "must-appear-in-unmapped-report"
  deploy:
    CRYPT_KEY: "synthetic-crypt-must-not-leak"
    WEIRD_DEPLOY_HOOK: "also-unmapped"
