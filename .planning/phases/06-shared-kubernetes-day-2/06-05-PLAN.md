---
phase: 06-shared-kubernetes-day-2
plan: 05
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-03
  - 06-04
files_modified:
  - internal/cloud/ovh/stack/ops.go
  - internal/cloud/ovh/stack/ops_test.go
  - internal/cloud/scaleway/stack/ops.go
  - internal/cloud/scaleway/stack/ops_test.go
  - internal/cloud/aws/eksops/ops.go
  - docs/capability-matrix.md
  - docs/ovh-experimental.md
  - docs/scaleway-experimental.md
  - docs/release-readiness.md
autonomous: true
requirements:
  - KUBE-06
  - KUBE-07

must_haves:
  truths:
    - "OVH/SCW unsupported shells no longer stub Observe, Steps, or State where shared layer provides them (D-05, KUBE-07, SC4)"
    - "Bootstrap, Secrets, and Cost remain unsupported with tier-named ErrNotSupported — no nil-success path (D-05, KUBE-06, SC5)"
    - "AcquireLock on OVH/SCW uses real State.Lock instead of warn-then-noop once State works (D-04/D-05)"
    - "capability-matrix and experimental docs record working day-2 vs gap cells for eks-autopilot/mks/kapsule (SC5)"
  artifacts:
    - "Allowlist tests for remaining ErrNotSupported methods on ovh/scaleway"
    - "Updated docs/capability-matrix.md day-2 rows"
    - "Updated docs/ovh-experimental.md and docs/scaleway-experimental.md"
  key_links:
    - "Ops.AcquireLock → State.Lock → aws/state manager"
    - "CLI notSupportedMessage tier naming unchanged for Bootstrap/Secrets/Cost"
---

<objective>
Finish honesty surgery: delete shared-layer stubs, flip AcquireLock to real DIY lock, keep Bootstrap/Secrets/Cost loud, update matrix/docs (D-05, KUBE-06, KUBE-07, SC4–SC5).

Purpose: TRUST-02 / capability-matrix must match what the code actually does before Phase 7.
Output: Trimmed unsupported shells, AcquireLock flip, allowlist tests, matrix + experimental doc rows.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-kubernetes-day-2/06-CONTEXT.md
@.planning/phases/06-shared-kubernetes-day-2/06-RESEARCH.md
@.planning/phases/06-shared-kubernetes-day-2/06-01-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-03-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-04-SUMMARY.md
@internal/cloud/ovh/stack/ops.go
@internal/cli/ports.go
@docs/capability-matrix.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end remaining-unsupported allowlist + AcquireLock flip</name>
  <files>internal/cloud/ovh/stack/ops.go, internal/cloud/ovh/stack/ops_test.go, internal/cloud/scaleway/stack/ops.go, internal/cloud/scaleway/stack/ops_test.go, internal/cloud/aws/eksops/ops.go</files>
  <reversibility rating="costly">Capability-matrix honesty and AcquireLock semantics become operator-visible (D-05).</reversibility>
  <action>
Per D-05, KUBE-06, KUBE-07, SC4, RESEARCH pitfall 4:

1. Ensure `unsupported{}` on OVH/SCW only covers Bootstrap/Secrets/Cost (VerifyAccount, Ensure, List, Set, Remove, Estimate — and any remaining cloud-shaped gaps). Observe/Steps/State methods must not live on unsupported after 06-01/03/04 wiring.
2. Flip `Ops.AcquireLock` on OVH/SCW from warn-then-noop to real `State.Lock` (same pattern as AWS/GCP). Update EKS AcquireLock similarly if it still warns while State already works. Keep diyLockWarnOut only if a path still lacks State.
3. Replace the old "15 ErrNotSupported sites" tests with an allowlist of remaining unsupported methods; assert RuntimeObserve/State/NewDeploySteps are not ErrNotSupported. Add nil-success guards for Bootstrap/Secrets. Serial GOMAXPROCS=1. No live accounts (D-06).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ ./internal/cloud/aws/eksops/ -count=1 -run 'Unsupported|Allowlist|AcquireLock|NilSuccess|Bootstrap|Secrets'</automated>
  </verify>
  <done>Allowlist matches remaining gaps; AcquireLock takes a real DIY lock when State is configured; Bootstrap/Secrets stay loud failures.</done>
</task>

<task type="auto">
  <name>Matrix + experimental docs honesty for day-2 surface</name>
  <files>docs/capability-matrix.md, docs/ovh-experimental.md, docs/scaleway-experimental.md, docs/release-readiness.md</files>
  <action>
Per D-05, D-06, KUBE-07, SC5, RESEARCH Open Question 4 (RESOLVED wording): update `docs/capability-matrix.md` rows for `eks-autopilot`, `mks`, and `kapsule` so Day-2 / Magento deploy Ops state Observe+Steps+State offline (unit/Floci) with Bootstrap/Secrets/Cost unsupported (experimental). Update `docs/ovh-experimental.md` and `docs/scaleway-experimental.md` Deferred/Day-2 bullets so they no longer claim every day-2 port returns ErrNotSupported. Touch `docs/release-readiness.md` only if a Phase 6 gate row exists or needs an evidence pointer — do not claim live GKE. Tier remains experimental. No live GCP (D-06). DNS/managed dump stay Phase 7.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ -count=1 -run 'Unsupported|Allowlist' && rg -n 'Observe\+Steps\+State|shared kube|experimental' docs/capability-matrix.md docs/ovh-experimental.md docs/scaleway-experimental.md</automated>
  </verify>
  <done>Docs match code: shared day-2 offline surface recorded; Bootstrap/Secrets gaps explicit; tier experimental.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs matrix → operator trust | Overclaiming day-2 is a product integrity failure |
| AcquireLock → DIY S3 | Lock must actually exclude concurrent mutators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Repudiation | capability-matrix | high | mitigate | Evidence labels say offline unit/Floci; no certified claim (D-06) |
| T-06-13 | Elevation of Privilege | nil-success Bootstrap/Secrets | high | mitigate | Allowlist + nil-success tests for remaining stubs |
| T-06-14 | Tampering | AcquireLock noop | medium | mitigate | Flip to State.Lock; remove false release-fn implication |
| T-06-SC | Tampering | package installs | low | accept | Docs-only + existing Go pkgs |
</threat_model>

<verification>
Allowlist tests green; matrix/experimental docs mention shared offline day-2 and remaining gaps.
</verification>

<success_criteria>
KUBE-06/07 and SC4–SC5 honesty complete offline.
</success_criteria>

<output>
Create `.planning/phases/06-shared-kubernetes-day-2/06-05-SUMMARY.md` when done
</output>
