---
phase: 06-shared-kubernetes-day-2
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - internal/cloud/kube/steps.go
  - internal/cloud/kube/candidate.go
  - internal/cloud/kube/steps_test.go
  - internal/cloud/gcp/ops/module.go
  - internal/cloud/aws/eksops/ops.go
  - internal/cloud/ovh/stack/ops.go
  - internal/cloud/scaleway/stack/ops.go
  - internal/cloud/gcp/deployment/steps.go
autonomous: true
requirements:
  - KUBE-04

must_haves:
  truths:
    - "deploy.Steps sequence Validate→RegisterCandidate→RunMigrations→CleanupCandidate→UpdateServices→Stabilize→Health→Record runs once in kube.Steps against fake cluster (D-03, KUBE-04, SC2)"
    - "All four NewDeploySteps return *kube.Steps (type-identity)"
    - "No internal/deploy/kube package; ECS deployflow untouched"
  artifacts:
    - "internal/cloud/kube/steps.go implementing deployflow.Steps"
    - "internal/cloud/kube/candidate.go Job-based migrate path (JobAPI inject)"
    - "internal/cloud/kube/steps_test.go full sequence on fake.NewClientset"
  key_links:
    - "Ops.NewDeploySteps → kube.New(...) → fake JobAPI / clientset"
    - "GCP module collapses gcp/deployment.Steps usage into kube.Steps"
---

<objective>
Lift Magento Kubernetes deploy Steps into `internal/cloud/kube` and register the same concrete type on all four modules (D-03, KUBE-04, SC2).

Purpose: One migrate→cutover path so Phase 7 certifies four targets.
Output: kube.Steps + candidate Jobs + four NewDeploySteps wirings + fake sequence test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-kubernetes-day-2/06-CONTEXT.md
@.planning/phases/06-shared-kubernetes-day-2/06-RESEARCH.md
@.planning/phases/06-shared-kubernetes-day-2/06-02-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-03-SUMMARY.md
@internal/deploy/orchestrator.go
@internal/cloud/gcp/deployment/steps.go
@internal/cloud/gcp/ops/module.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end kube.Steps sequence on fake JobAPI</name>
  <files>internal/cloud/kube/steps.go, internal/cloud/kube/candidate.go, internal/cloud/kube/steps_test.go, internal/cloud/gcp/ops/module.go</files>
  <reversibility rating="costly">KUBE-04 type-identity locks Steps in internal/cloud/kube; locked as D-03 in CONTEXT (discuss already confirmed).</reversibility>
  <action>
Per D-03, KUBE-04, RESEARCH A3 (fake clientset + JobAPI inject — no envtest):

1. Create `kube.Steps` implementing `deployflow.Steps` with the full Magento sequence: Validate → RegisterCandidate → RunMigrations → CleanupCandidate → UpdateServices → Stabilize → Health → Record. Lift logic from `gcp/deployment.Steps` and Job candidate helpers in `gcp/operations`; keep Magento sequencing in `internal/deploy` orchestrator — do not create `internal/deploy/kube`; do not merge ECS deployflow.
2. Injectable JobAPI / CandidateRunner / RuntimeChecker so fake.NewClientset (or fakes) can drive Jobs/Deployments without a real apiserver. Prefer `fake.NewClientset` over deprecated NewSimpleClientset.
3. Point GCP `Ops.NewDeploySteps` at `kube.New(...)`. Leave thin re-exports in gcp/deployment only if needed for compile during move; prefer deleting duplicate Steps body once kube owns it. Serial GOMAXPROCS=1 tests: TestStepsSequence walks all eight steps. D-06: no live GKE.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/kube/ ./internal/cloud/gcp/ops/ -count=1 -run 'Steps|DeploySteps|Sequence'</automated>
  </verify>
  <done>kube.Steps full sequence passes on fake inject; GCP NewDeploySteps returns shared type.</done>
</task>

<task type="auto" tdd="true">
  <name>Four-module NewDeploySteps type-identity</name>
  <files>internal/cloud/aws/eksops/ops.go, internal/cloud/ovh/stack/ops.go, internal/cloud/scaleway/stack/ops.go, internal/cloud/kube/steps_test.go, internal/cloud/gcp/deployment/steps.go</files>
  <behavior>
    - eksops, ovh, scaleway NewDeploySteps return *kube.Steps (or same concrete named type)
    - Wrong backend type fails with clear error (mirror existing GCP/AWS patterns)
    - unsupported NewDeploySteps ErrNotSupported paths removed where shared Steps is registered
  </behavior>
  <action>
Per D-03, D-05, KUBE-04, SC2: wire EKS/OVH/SCW `NewDeploySteps` to return/wrap `kube.Steps` using backend Outputs + client factory from 06-02. Add type-identity tests covering all four modules. Remove duplicate GCP Steps implementation once callers compile against kube. Serial tests only; fake inject only; no live cloud (D-06).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/kube/ ./internal/cloud/gcp/ops/ ./internal/cloud/aws/eksops/ ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ -count=1 -run 'Steps|NewDeploySteps|TypeIdentity'</automated>
  </verify>
  <done>SC2: shared kube.Steps exercised through all four module registrations offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy Request image digest → cluster Jobs | Mutating Magento migrate path |
| Backend Outputs → Steps | Missing kubeconfig/service outputs must fail before mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Tampering | RegisterCandidate / migrate Job | high | mitigate | Validate digest vs planned artifact before Jobs; fail closed |
| T-06-10 | Denial of Service | Stabilize wait loops | medium | mitigate | Keep bounded waitTimeout; tests use short intervals |
| T-06-11 | Repudiation | Record step | low | mitigate | Preserve release journal hook wiring from GCP Ops |
| T-06-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Serial kube Steps sequence + four-module type-identity tests green.
</verification>

<success_criteria>
KUBE-04 / SC2 true offline with fake clientset only.
</success_criteria>

<output>
Create `.planning/phases/06-shared-kubernetes-day-2/06-04-SUMMARY.md` when done
</output>
