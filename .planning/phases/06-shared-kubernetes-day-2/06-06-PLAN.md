---
phase: 06-shared-kubernetes-day-2
plan: 06
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - internal/cloud/kube/identity_test.go
  - internal/cloud/kube/steps_test.go
  - docs/release-readiness.md
  - .planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md
autonomous: true
requirements:
  - KUBE-01
  - KUBE-02
  - KUBE-03
  - KUBE-04
  - KUBE-05
  - KUBE-06
  - KUBE-07

must_haves:
  truths:
    - "Cross-module type-identity package asserts *kube.Observe and *kube.Steps for gcp, eksops, ovh, scaleway (SC1–SC2)"
    - "Steps sequence test remains green and is reachable from all four registrations (SC2)"
    - "Floci/local AES256 state gate documented as Phase 6 close-out evidence (SC3, KUBE-05)"
    - "Phase 7 handoff records Cloudflare DNS preview host and token scope (not implemented in Phase 6)"
  artifacts:
    - "internal/cloud/kube/identity_test.go (or equivalent cross-module gate)"
    - ".planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md"
  key_links:
    - "Four Module registrations → shared concrete types → offline fake/Floci proofs"
    - "Handoff → Phase 7 MIGRATE-04 Cloudflare DNS on magelift-preview.alexandrecourtiol.com"
---

<objective>
Close Phase 6 with offline integration gates across all requirements and a Phase 7 handoff note for Cloudflare DNS (SC1–SC5 glue; D-06).

Purpose: Prove the shared layer hangs together before any paid GCP pass.
Output: Cross-module identity tests, Floci evidence pointer, Phase 7 DNS handoff doc, release-readiness touch.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-kubernetes-day-2/06-CONTEXT.md
@.planning/phases/06-shared-kubernetes-day-2/06-RESEARCH.md
@.planning/phases/06-shared-kubernetes-day-2/06-01-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-02-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-03-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-04-SUMMARY.md
@.planning/phases/06-shared-kubernetes-day-2/06-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end cross-module type-identity + Steps gate</name>
  <files>internal/cloud/kube/identity_test.go, internal/cloud/kube/steps_test.go</files>
  <action>
Per D-01, D-03, SC1–SC2, KUBE-01..04: add `identity_test.go` that constructs (or obtains) gcp ops Module, aws/eksops Module, ovh stack Module, and scaleway stack Module and asserts `RuntimeObserve()` is `*kube.Observe` and `NewDeploySteps` returns `*kube.Steps` (same concrete types). Re-run / keep `TestStepsSequence` green under fake clientset. Import modules carefully to avoid illegal CLI→kube cycles — tests may live under `internal/cloud/kube` with blank imports of provider modules only if the import graph allows; otherwise place the gate in a small `internal/cloud/kube/identity` test package or provider-neutral test file that already imports all four. Serial GOMAXPROCS=1 GOFLAGS=-p=1. No envtest. No live GCP (D-06).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/kube/... ./internal/cloud/gcp/ops/ ./internal/cloud/aws/eksops/ ./internal/cloud/ovh/... ./internal/cloud/scaleway/... -count=1 -run 'TypeIdentity|StepsSequence|Observe|AES256'</automated>
  </verify>
  <done>SC1–SC2 integration gate green offline across four modules.</done>
</task>

<task type="auto">
  <name>Floci state close-out + Phase 7 Cloudflare DNS handoff</name>
  <files>docs/release-readiness.md, .planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md</files>
  <action>
Per D-06, KUBE-05, SC3, CONTEXT deferred DNS:

1. Confirm Floci AES256 (or EncryptionNone) state evidence from 06-01 is cited in release-readiness / SUMMARY; if `make floci-test` is available locally, run it serially and record the result in 06-06-SUMMARY (skip cleanly if Docker unavailable — unit AES256 remains the offline floor).
2. Write `06-PHASE7-HANDOFF.md` stating Phase 7 will implement MIGRATE-04 Cloudflare DNS on preferred host `magelift-preview.alexandrecourtiol.com` (fallback `magelift-preview.acourtiol.com`); zones are authorized; API token needs Zone.DNS Edit — Wrangler OAuth is zone:read only and is insufficient for cutover writes. Managed dump cell rides the GCP certification pass. Phase 6 must not implement DNS or live GKE.
3. Light-touch `docs/release-readiness.md` pointer to Phase 6 offline shared-kube evidence and Phase 7 DNS prerequisites above. Serial verifies only.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md && rg -n 'magelift-preview\.alexandrecourtiol\.com|Zone\.DNS Edit|Wrangler' .planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/state/ ./internal/cloud/kube/ -count=1 -run 'AES256|TypeIdentity|StepsSequence'</automated>
  </verify>
  <done>Phase 6 close-out evidence + Phase 7 DNS handoff recorded; no DNS/live GCP code in Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase handoff docs → Phase 7 spend | Wrong DNS auth guidance wastes paid pass |
| Integration tests → import graph | Must not create CLI↔cloud cycles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Spoofing | Cloudflare token scope note | medium | mitigate | Handoff explicitly requires Zone.DNS Edit; document Wrangler OAuth insufficiency |
| T-06-16 | Repudiation | release-readiness | medium | mitigate | Label evidence offline-only; defer live cells to Phase 7 |
| T-06-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Full serial kube + provider package suite for identity/Steps/AES256 green; handoff file present with Cloudflare scope note.
</verification>

<success_criteria>
All KUBE-01..07 offline acceptance glued; Phase 7 DNS prerequisites documented; zero cloud spend.
</success_criteria>

<output>
Create `.planning/phases/06-shared-kubernetes-day-2/06-06-SUMMARY.md` when done
</output>
