---
phase: 06-shared-kubernetes-day-2
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - internal/cloud/kube/observe.go
  - internal/cloud/kube/observe_test.go
  - internal/cloud/gcp/ops/day2.go
  - internal/cloud/aws/eksops/ops.go
  - internal/cloud/ovh/stack/ops.go
  - internal/cloud/scaleway/stack/ops.go
  - internal/cli/exec.go
autonomous: true
requirements:
  - KUBE-01
  - KUBE-02
  - KUBE-03

must_haves:
  truths:
    - "TailLogs, CheckRuntime, and PrepareExec live once on kube.Observe (D-01, KUBE-01..03, SC1)"
    - "GCP, EKS, OVH, and Scaleway RuntimeObserve() return *kube.Observe (type-identity)"
    - "PrepareExec returns kubectl launcher + portable argv; gke-job launcher debt removed"
  artifacts:
    - "internal/cloud/kube/observe.go implementing platform.RuntimeObserve"
    - "internal/cloud/kube/observe_test.go using fake.NewClientset"
    - "Collapsed GCP ops.Observe body — one-liner into kube"
  key_links:
    - "Module.RuntimeObserve → kube.NewObserve(client|factory)"
    - "fake.NewClientset pods/deployments → TailLogs / CheckRuntime"
---

<objective>
Ship shared `kube.Observe` and collapse provider-local GCP Observe so all four K8s modules share TailLogs/CheckRuntime/PrepareExec (D-01, KUBE-01..03).

Purpose: Phase 7 certification lifts three more targets only if Observe exists once.
Output: kube.Observe + four RuntimeObserve wirings + fake-clientset tests + type-identity assertions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-kubernetes-day-2/06-CONTEXT.md
@.planning/phases/06-shared-kubernetes-day-2/06-RESEARCH.md
@.planning/phases/06-shared-kubernetes-day-2/06-02-SUMMARY.md
@internal/platform/observe.go
@internal/cloud/gcp/ops/day2.go
@internal/cloud/gcp/operations/runtime.go
@internal/cli/exec.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end TailLogs via kube.Observe + fake clientset</name>
  <files>internal/cloud/kube/observe.go, internal/cloud/kube/observe_test.go, internal/cloud/gcp/ops/day2.go</files>
  <reversibility rating="costly">Type-identity to *kube.Observe is the SC1 contract; locked as D-01 in CONTEXT (discuss already confirmed).</reversibility>
  <action>
Per D-01, KUBE-01, RESEARCH Open Question 2 (fake.NewClientset only — no envtest):

1. Create `kube.Observe` holding `kubernetes.Interface` (ctor inject for tests) plus optional ClientFactory from outputs for live path. Implement `TailLogs` by lifting pod list + GetLogs logic from `gcp/operations` / `gcp/ops` Observe — namespace default, label selector from service/deployment outputs. Do not expand `platform` with kube-specific methods. Do not copy ECS Observe.
2. Collapse GCP `RuntimeObserve()` to return `kube.NewObserve(...)` (delete or thin the local Observe body in day2.go). Prefer factory that uses ClientFromOutputs when outputs present; tests inject fake clientset.
3. `observe_test.go`: fake.NewClientset with Pod → TailLogs returns events (or clear empty-pod error). Serial GOMAXPROCS=1. D-06: no live GKE.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/kube/ ./internal/cloud/gcp/ops/ -count=1 -run 'Observe|TailLogs|RuntimeObserve'</automated>
  </verify>
  <done>kube.Observe.TailLogs works on fake clientset; GCP RuntimeObserve returns shared type.</done>
</task>

<task type="auto" tdd="true">
  <name>CheckRuntime + PrepareExec + four-module wire + type-identity</name>
  <files>internal/cloud/kube/observe.go, internal/cloud/kube/observe_test.go, internal/cloud/aws/eksops/ops.go, internal/cloud/ovh/stack/ops.go, internal/cloud/scaleway/stack/ops.go, internal/cli/exec.go</files>
  <behavior>
    - CheckRuntime reports healthy/unhealthy from fake Deployment ready replicas (KUBE-02)
    - PrepareExec returns Launcher kubectl and Args suitable for CLI runExecTarget (KUBE-03); no gke-job launcher
    - eksops, ovh, scaleway RuntimeObserve() return *kube.Observe
    - Type assertion test: four modules → *kube.Observe
  </behavior>
  <action>
Per D-01, D-02, KUBE-02, KUBE-03, RESEARCH pitfall 5: implement CheckRuntime (lift deploymentHealth) and PrepareExec (portable kubectl target from cluster/service outputs). If CLI `runExecTarget` empty-binary path breaks kubectl, fix `internal/cli/exec.go` in the same plan so launcher+args run correctly (AWS Args shape must keep working). Wire EKS/OVH/SCW `RuntimeObserve()` to shared Observe (ctor OK). Use D-02 thin wrapper only if a module cannot emit usable kubeconfig — default is shared type. Add type-identity tests. Remove Observe methods from ovh/scaleway `unsupported{}` only if State already moved (06-01); otherwise delete TailLogs/CheckRuntime/PrepareExec stubs when wiring RuntimeObserve to kube. Serial tests; fake clientset only; no live cloud (D-06).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/kube/ ./internal/cloud/gcp/ops/ ./internal/cloud/aws/eksops/ ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ ./internal/cli/ -count=1 -run 'Observe|CheckRuntime|PrepareExec|TypeIdentity|RuntimeObserve|Exec'</automated>
  </verify>
  <done>KUBE-01..03 satisfied offline; four modules share *kube.Observe; PrepareExec is kubectl-portable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stack outputs → Observe | Untrusted/missing outputs must fail closed |
| Exec argv → kubectl | Operator command strings cross into process exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Elevation of Privilege | PrepareExec | high | mitigate | Preserve CLI NUL/newline rejection; fixed argv construction |
| T-06-07 | Information Disclosure | TailLogs | medium | mitigate | Do not log kubeconfig; stream pod logs only |
| T-06-08 | Spoofing | Client factory | medium | mitigate | Require non-empty kubeconfig output; no silent GKE ADC fallback after collapse |
| T-06-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Serial Observe + module type-identity tests green on fake clientset.
</verification>

<success_criteria>
SC1 truths for Observe hold offline across four registrations (D-01).
</success_criteria>

<output>
Create `.planning/phases/06-shared-kubernetes-day-2/06-03-SUMMARY.md` when done
</output>
