---
phase: 06-shared-kubernetes-day-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/platform/outputs.go
  - internal/cloud/kube/client.go
  - internal/cloud/kube/client_test.go
  - internal/cloud/gcp/stack/component.go
  - internal/cloud/aws/eksops/component.go
  - internal/cloud/ovh/stack/component.go
  - internal/cloud/scaleway/stack/component.go
autonomous: true
requirements:
  - KUBE-01
  - KUBE-02
  - KUBE-03
  - KUBE-04

must_haves:
  truths:
    - "All four K8s stack Outputs export optional secret key kubeconfig (D-01 discretion RESOLVED)"
    - "kube.ClientFromKubeconfig builds kubernetes.Interface via clientcmd.RESTConfigFromKubeConfig"
    - "kubeconfig is not in RequiredOutputKeys (ECS unchanged)"
  artifacts:
    - "platform.OutputKubeconfig constant"
    - "internal/cloud/kube/client.go with ClientFromKubeconfig / ClientFromOutputs"
    - "Four K8s Component.Outputs entries for kubeconfig as pulumi secret"
  key_links:
    - "Outputs[kubeconfig] → ClientFromOutputs → kubernetes.NewForConfig"
    - "Tests inject kubernetes.Interface directly without network"
---

<objective>
Close the kubeconfig export gap and ship a portable client factory so Observe/Steps never depend on GKE ADC (D-01, RESEARCH pitfall 1).

Purpose: Shared day-2 needs a provider-neutral kubernetes.Interface source before Observe/Steps land.
Output: OutputKubeconfig + four stack exports + kube client helpers with offline unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-kubernetes-day-2/06-CONTEXT.md
@.planning/phases/06-shared-kubernetes-day-2/06-RESEARCH.md
@internal/platform/outputs.go
@internal/cloud/kube/kube.go
@internal/cloud/gcp/stack/component.go
@internal/cloud/aws/eksops/component.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end kubeconfig bytes → ClientFromKubeconfig</name>
  <files>internal/platform/outputs.go, internal/cloud/kube/client.go, internal/cloud/kube/client_test.go</files>
  <reversibility rating="costly">Output key name is the cross-provider Observe/Steps contract; locked as D-01 in CONTEXT (discuss already confirmed).</reversibility>
  <action>
Per D-01 / RESEARCH Open Question 1 (RESOLVED): add `platform.OutputKubeconfig = "kubeconfig"` as an optional Magento-facing key — document that it is not in `RequiredOutputKeys` (ECS stays free of it).

1. Implement `kube.ClientFromKubeconfig([]byte) (kubernetes.Interface, error)` using `clientcmd.RESTConfigFromKubeConfig` + `kubernetes.NewForConfig`. Add `ClientFromOutputs(map[string]any)` via `RequireStringOutput(..., OutputKubeconfig)`.
2. Unit test: BuildStaticTokenKubeconfig sample → ClientFromKubeconfig returns non-nil Interface; missing/empty output key errors clearly. Prefer injecting Interface in later Observe tests — this plan proves parse path only. Serial GOMAXPROCS=1. No envtest. No live cluster (D-06).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/kube/ ./internal/platform/ -count=1 -run 'ClientFrom|OutputKubeconfig|Kubeconfig'</automated>
  </verify>
  <done>OutputKubeconfig exists; ClientFromKubeconfig/ClientFromOutputs work offline from static-token kubeconfig bytes.</done>
</task>

<task type="auto">
  <name>Export secret kubeconfig from four K8s stack Outputs</name>
  <files>internal/cloud/gcp/stack/component.go, internal/cloud/aws/eksops/component.go, internal/cloud/ovh/stack/component.go, internal/cloud/scaleway/stack/component.go</files>
  <action>
Per D-01 / RESEARCH pitfall 1–2: each K8s `Component.Outputs()` exports `platform.OutputKubeconfig` as a Pulumi secret (preserve `pulumi.ToSecret` / secret marking already used on component kubeconfig fields). Prefer static-token kubeconfig already produced by OVH/SCW/GKE helpers (`kube.BuildStaticTokenKubeconfig`). For EKS, export the runtime-generated kubeconfig already on the component; do not require live AWS STS in this plan — D-02 escape hatch stays for later if exec-plugin kubeconfig cannot build a client offline. Do not add kubeconfig to RequiredOutputKeys or Module.OutputKeys required set unless the module already returns extras safely. No live GCP/AWS (D-06). Serial tests for any Outputs unit coverage touched.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/gcp/stack/ ./internal/cloud/aws/eksops/ ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ -count=1 -run 'Output|Component|Kubeconfig'</automated>
  </verify>
  <done>All four K8s stacks export secret kubeconfig under OutputKubeconfig; RequiredOutputKeys unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pulumi stack outputs → day-2 client | kubeconfig token/CA is credential material |
| Emulator / CI logs | Must not print kubeconfig YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Outputs kubeconfig | high | mitigate | Export as Pulumi secret; forbid printing kubeconfig in diagnostics/tests |
| T-06-05 | Elevation of Privilege | ClientFromKubeconfig | medium | mitigate | Fail closed on parse errors; no silent empty client |
| T-06-SC | Tampering | package installs | low | accept | client-go already pinned in go.mod |
</threat_model>

<verification>
Serial kube client + stack Outputs tests green offline.
</verification>

<success_criteria>
Portable kubeconfig output + client factory ready for Observe/Steps; zero cloud spend.
</success_criteria>

<output>
Create `.planning/phases/06-shared-kubernetes-day-2/06-02-SUMMARY.md` when done
</output>
