---
phase: 06-shared-kubernetes-day-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cloud/aws/state/encryption.go
  - internal/cloud/aws/state/lock.go
  - internal/cloud/aws/state/archive.go
  - internal/cloud/aws/state/client.go
  - internal/cloud/aws/state/lock_test.go
  - internal/cloud/aws/state/archive_test.go
  - internal/cloud/aws/ops/day2.go
  - internal/cloud/aws/ops/module.go
  - internal/cloud/aws/eksops/ops.go
  - internal/cloud/ovh/stack/state.go
  - internal/cloud/ovh/stack/ops.go
  - internal/cloud/scaleway/stack/state.go
  - internal/cloud/scaleway/stack/ops.go
  - tests/floci/state_aes256_test.go
autonomous: true
requirements:
  - KUBE-05

must_haves:
  truths:
    - "AWS DIY lock/backup still uses KMS SSE when callers pass EncryptionKMS (D-04)"
    - "OVH and Scaleway Module.State() use aws/state.Manager via NewAWSWithEndpoint with EncryptionAES256 — no forked state package (D-04, KUBE-05)"
    - "AES256 lock acquire succeeds against Floci or documented EncryptionNone fallback if Floci rejects SSE headers (D-04, D-06)"
  artifacts:
    - "internal/cloud/aws/state/encryption.go with ObjectEncryption / EncryptionMode"
    - "OVH and Scaleway concrete State adapters (not unsupported for Lock/Backup/Restore/Status)"
    - "tests/floci/state_aes256_test.go or equivalent AES256 Floci coverage"
  key_links:
    - "NewAWSWithEndpoint → NewManager(ObjectEncryption) → PutObject SSE policy"
    - "ovh/scaleway State() → aws/state + endpoint.Parse loopback gate"
---

<objective>
Generalize DIY S3 SSE so OVH/Scaleway reuse `aws/state` with AES256 + endpoint override while AWS keeps KMS (D-04, KUBE-05).

Purpose: Unblock AcquireLock hard-fail and honest State ports without a third lock package.
Output: ObjectEncryption API, AWS callers on KMS, OVH/SCW State adapters, offline AES256 proof.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-kubernetes-day-2/06-CONTEXT.md
@.planning/phases/06-shared-kubernetes-day-2/06-RESEARCH.md
@internal/cloud/aws/state/lock.go
@internal/cloud/aws/state/client.go
@internal/cloud/aws/endpoint/endpoint.go
@tests/floci/state_test.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end AES256 lock PutObject via ObjectEncryption</name>
  <files>internal/cloud/aws/state/encryption.go, internal/cloud/aws/state/lock.go, internal/cloud/aws/state/archive.go, internal/cloud/aws/state/client.go, internal/cloud/aws/state/lock_test.go, internal/cloud/aws/state/archive_test.go, internal/cloud/aws/ops/day2.go, internal/cloud/aws/ops/module.go, internal/cloud/aws/eksops/ops.go</files>
  <reversibility rating="costly">AcquireLock hard-fail and OVH/SCW DIY lock depend on this SSE seam (D-04).</reversibility>
  <action>
Per D-04 / RESEARCH Open Question 3 (RESOLVED): introduce `ObjectEncryption` with modes EncryptionKMS (require ARN, AwsKms) and EncryptionAES256 (no ARN, Aes256). Optional EncryptionNone only if unit/Floci proves the emulator rejects SSE headers.

1. Add `encryption.go`; change `NewManager` / `NewArchiveFromClient` to take `ObjectEncryption` instead of bare kmsARN string. Keep lock object key path `locks/{project}/{env}.json` and JSON Info schema unchanged (Runtime State Inventory).
2. Update `NewAWS` / `NewAWSWithEndpoint` to accept `ObjectEncryption` (or thin wrappers that build EncryptionKMS from ARN for AWS callers). Migrate `aws/ops`, `eksops` State/archive call sites to EncryptionKMS with existing KMS ARNs.
3. Unit tests: AES256 manager Lock succeeds without ARN; KMS mode still rejects empty/non-ARN; AES256 PutObject sets Aes256 (fake S3API asserts). Serial only: GOMAXPROCS=1 GOFLAGS=-p=1 per AGENTS.md. No live cloud. No new Go modules.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/state/ ./internal/cloud/aws/ops/ ./internal/cloud/aws/eksops/ -count=1 -run 'Encryption|AES256|Manager|Archive|Lock|NewAWS'</automated>
  </verify>
  <done>ObjectEncryption drives SSE; AWS production path remains KMS; AES256 unit path locks without ARN; lock key paths unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire OVH/SCW State through NewAWSWithEndpoint + Floci AES256</name>
  <files>internal/cloud/ovh/stack/state.go, internal/cloud/ovh/stack/ops.go, internal/cloud/scaleway/stack/state.go, internal/cloud/scaleway/stack/ops.go, tests/floci/state_aes256_test.go</files>
  <behavior>
    - Module.State() on ovh and scaleway returns a concrete type implementing platform.State that delegates to aws/state.Manager
    - Construction uses NewAWSWithEndpoint + EncryptionAES256 and aws/endpoint.Parse for emulator URLs
    - Floci (MAGELIFT_FLOCI=1) acquires lock with AES256 (or EncryptionNone if SSE headers fail — document which mode in SUMMARY)
    - No new ovh/state or scaleway/state packages
  </behavior>
  <action>
Per D-04, D-05, KUBE-05: replace `Module.State() return unsupported{}` with concrete State adapters in `ovh/stack/state.go` and `scaleway/stack/state.go` that build managers via `aws/state.NewAWSWithEndpoint` + EncryptionAES256. Read DIY bucket/region/endpoint from planned target config and/or existing Floci env (`MAGELIFT_AWS_ENDPOINT_URL` / `MAGELIFT_FLOCI_ENDPOINT`) — add the minimum config fields needed (schema generate if YAML keys are required). Leave Bootstrap/Secrets/Cost on unsupported. Do not flip AcquireLock in this plan (06-05). Add `tests/floci/state_aes256_test.go` under build tag floci proving AES256 lock path; skip cleanly when MAGELIFT_FLOCI unset. Serial Go tests; no live OVH/SCW accounts (D-06).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ ./internal/cloud/aws/state/ -count=1 -run 'State|AES256|Encryption' && (test "${MAGELIFT_FLOCI:-}" = "1" && make floci-test || GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/state/ -count=1 -run 'AES256')</automated>
  </verify>
  <done>OVH/SCW State uses shared aws/state + endpoint; Floci or unit AES256 proof green; no forked state packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DIY S3 endpoint override → PutObject | Emulator URL must not exfiltrate credentials to non-loopback hosts |
| Operator ObjectEncryption mode | Wrong mode could write unencrypted or rejected objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Spoofing | NewAWSWithEndpoint | high | mitigate | Reuse aws/endpoint.Parse loopback-only for emulator endpoints |
| T-06-02 | Tampering | DIY lock objects | medium | mitigate | Keep IfNoneMatch acquire; unchanged lock key schema |
| T-06-03 | Information Disclosure | encryption mode logs | low | mitigate | Do not log ARNs or object bodies in diagnostics |
| T-06-SC | Tampering | package installs | low | accept | No new registry packages — go.mod pins only |
</threat_model>

<verification>
Serial aws/state + ovh/scaleway State tests green; AES256 path proven offline (unit and/or Floci).
</verification>

<success_criteria>
KUBE-05 foundation landed: one S3-compatible manager, AWS KMS preserved, OVH/SCW AES256+endpoint wired, zero cloud spend.
</success_criteria>

<output>
Create `.planning/phases/06-shared-kubernetes-day-2/06-01-SUMMARY.md` when done
</output>
