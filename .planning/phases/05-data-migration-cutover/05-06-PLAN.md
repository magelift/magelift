---
phase: 05-data-migration-cutover
plan: 06
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - docs/migrating-from-paas.md
  - docs/capability-matrix.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/05-data-migration-cutover/scratch/05-cutover-local-proof.md
autonomous: true
requirements:
  - MIGRATE-04

must_haves:
  truths:
    - "docs/migrating-from-paas.md contains cutover runbook sections for DNS, maintenance mode, reindex, verification, and rollback (D-06, MIGRATE-04, SC5 docs)"
    - "A recorded local scratch proof exercises dump + media + maintenance/reindex/verify/rollback steps offline (D-06)"
    - "REQUIREMENTS/STATE explicitly split DNS + live non-prod cutover + managed dump cell to Phase 7 HUMAN_GATE; no new paid AWS pass (D-06)"
  artifacts:
    - "Cutover runbook section in docs/migrating-from-paas.md"
    - "scratch/05-cutover-local-proof.md with commands and outcomes"
    - "REQUIREMENTS MIGRATE-04 note + STATE session continuity for Phase 7 gate"
  key_links:
    - "Runbook → env import-dump / media-sync / reindex / exec maintenance → scratch log"
---

<objective>
Document the cutover runbook, prove executable steps locally with a scratch recording, and lock the Phase 7 HUMAN_GATE split for DNS/live/managed dump (D-06, MIGRATE-04, SC5 local half).

Purpose: Prevent marking MIGRATE-04 Complete from docs-only or local-only without the live gate; no fourth paid pass in Phase 5.
Output: Runbook + scratch proof + REQUIREMENTS/STATE honesty.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-data-migration-cutover/05-CONTEXT.md
@.planning/phases/05-data-migration-cutover/05-RESEARCH.md
@docs/migrating-from-paas.md
@docs/capability-matrix.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Cutover runbook sections in migrating-from-paas.md</name>
  <files>docs/migrating-from-paas.md, docs/capability-matrix.md</files>
  <reversibility rating="costly">Marking MIGRATE-04 complete without the Phase 7 HUMAN_GATE split would over-claim SC5 (D-06).</reversibility>
  <action>
Per D-06 / MIGRATE-04 / SC5: replace the deferred one-liner in `docs/migrating-from-paas.md` with a cutover runbook covering DNS cutover, Magento maintenance mode, reindex, verification checks, and rollback. Reference real MageLift commands shipped in this phase (`env import-dump`, `env media-sync`, existing `reindex` / `exec` / destroy-restore patterns). State clearly that live DNS + non-prod cutover evidence and managed-instance dump import ride Phase 7 under HUMAN_GATE (no Phase 5 paid AWS pass). Update `docs/capability-matrix.md` media/Floci row honesty if needed so media-sync evidence tier matches offline proof. Run `make check-clean-room` after doc edits. Do not invent paid-cloud evidence.
  </action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path('docs/migrating-from-paas.md').read_text().lower()
need = ['dns', 'maintenance', 'reindex', 'rollback', 'verif']
missing = [k for k in need if k not in text]
assert not missing, missing
assert 'phase 7' in text or 'human_gate' in text or 'human gate' in text
print('runbook headings ok')
PY
make check-clean-room</automated>
  </verify>
  <done>Runbook documents DNS/maintenance/reindex/verification/rollback and Phase 7 live/DNS split; clean-room check passes.</done>
</task>

<task type="auto">
  <name>Local scratch proof + REQUIREMENTS/STATE Phase 7 split</name>
  <files>.planning/phases/05-data-migration-cutover/scratch/05-cutover-local-proof.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
Per D-06: record an offline cutover proof under `.planning/phases/05-data-migration-cutover/scratch/05-cutover-local-proof.md` listing the commands run (or simulated with noted skips), expected outcomes, and that DNS/live were not claimed. Prefer real local steps against `magelift dev` MySQL + media-sync unit/Floci when available; if Docker unavailable, document blocked steps honestly without claiming SC5 live. Update `.planning/REQUIREMENTS.md` MIGRATE-04 row/notes so DNS + live non-prod cutover + managed dump cell are Pending/split to Phase 7 HUMAN_GATE while local runbook proof is what Phase 5 closes. Update `.planning/STATE.md` session continuity with the same split and “no new paid AWS pass.” Do not mark MIGRATE-04 Complete for live DNS.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-data-migration-cutover/scratch/05-cutover-local-proof.md && rg -n 'Phase 7|HUMAN_GATE|human gate' .planning/REQUIREMENTS.md .planning/STATE.md && rg -n 'MIGRATE-04' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Scratch local proof exists; REQUIREMENTS and STATE record Phase 7 HUMAN_GATE split for DNS/live/managed dump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator production cutover | Guidance that can cause downtime if wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Repudiation | SC5 over-claim | medium | mitigate | Scratch log + REQUIREMENTS/STATE Phase 7 HUMAN_GATE split (D-06) |
| T-05-15 | Tampering | runbook omitting rollback | medium | mitigate | Mandatory rollback section in docs verify gate |
| T-05-SC | Tampering | package installs | low | accept | Docs-only plan |
</threat_model>

<verification>
Runbook keyword gate + scratch file present + REQUIREMENTS/STATE Phase 7 mentions + clean-room.
</verification>

<success_criteria>
MIGRATE-04 local half done: documented runbook + local scratch; live DNS deferred explicitly to Phase 7 HUMAN_GATE.
</success_criteria>

<output>
Create `.planning/phases/05-data-migration-cutover/05-06-SUMMARY.md` when done
</output>
