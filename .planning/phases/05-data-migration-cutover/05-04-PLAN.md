---
phase: 05-data-migration-cutover
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - internal/cli/env.go
  - internal/cli/root.go
  - internal/cli/lifecycle.go
  - internal/cli/env_import_dump_test.go
  - internal/cli/lifecycle_seeddump_test.go
autonomous: false
requirements:
  - MIGRATE-01
  - MIGRATE-02
  - MIGRATE-05

must_haves:
  truths:
    - "magelift env import-dump runs the dumpimporter with journal transitions and binds overwrite to persistent --yes (D-01, D-04, MIGRATE-01/05)"
    - "After successful deployflow.Run, auto-import fires once only when SeedDump set and journal status is recorded (D-01, D-02, SC1 local)"
    - "Later deploys do not re-import when status is imported/failed/importing (D-02)"
    - "Command is named import-dump not seed (D-01)"
  artifacts:
    - "env import-dump cobra command"
    - "maybeAutoImportSeedDump hook after deploy success in lifecycle"
    - "AutoImportOnce / RecordedOnly tests with fake deploy steps + fake/real importer seam"
  key_links:
    - "deploy success → maybeAutoImportSeedDump → seeddump Mark* + dumpimport.Import"
    - "env import-dump → same importer path for retries"
---

<objective>
Wire hybrid auto-import after first successful deploy and explicit `magelift env import-dump` for retries (D-01, D-02, D-04, MIGRATE-01/02/05, SC1 offline).

Purpose: Make seedDump stop being inert for the local path; managed Aurora cell stays Phase 7.
Output: CLI command + lifecycle hook + once-from-recorded tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-data-migration-cutover/05-CONTEXT.md
@.planning/phases/05-data-migration-cutover/05-RESEARCH.md
@internal/cli/lifecycle.go
@internal/cli/env.go
@internal/cli/root.go
@internal/dumpimport/import.go
@internal/seeddump/store.go
@docs/adr/0010-database-dump-seed.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Lock D-01 hybrid auto-import placement</name>
  <decision>When dump import runs relative to deploy (D-01 one-way door)</decision>
  <context>
CONTEXT locked D-01: after first successful infra deploy + Magento install/upgrade candidate, auto-run importer once when seedDump set and status recorded; plus explicit env import-dump. Never inside Pulumi create. Never name the command env seed. Auto/yolo may select option-a to match CONTEXT.
  </context>
  <options>
    <option id="option-a">
      <name>Hybrid: post-deployflow once-from-recorded + env import-dump (CONTEXT D-01)</name>
      <pros>Matches ADR 0010; retries covered; no Pulumi timeout bloat</pros>
      <cons>Deploy CLI exits nonzero if auto-import fails after infra is up — document recovery via import-dump</cons>
    </option>
    <option id="option-b">
      <name>Manual-only import-dump (no auto)</name>
      <pros>Simpler</pros>
      <cons>Breaks --dump after-first-deploy operator expectation (D-01)</cons>
    </option>
    <option id="option-c">
      <name>Import inside Pulumi create</name>
      <pros>None</pros>
      <cons>Rejected by ADR 0010; timeout/state footgun</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (CONTEXT lock), option-b, or option-c</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>env import-dump command with --yes nonempty guard</name>
  <files>internal/cli/env.go, internal/cli/root.go, internal/cli/env_import_dump_test.go</files>
  <reversibility rating="one-way">Public CLI verb import-dump and --yes exit semantics become script contracts (D-01, D-04).</reversibility>
  <behavior>
    - env import-dump for env with seedDump runs importer; journal moves importing→imported on success
    - On importer error journal is failed+reason and command exits nonzero
    - When DB nonempty and status is not the first recorded auto path, missing o.yes returns invalid (nonzero)
    - Help/Use string is import-dump; no command named seed under env
  </behavior>
  <action>
Per D-01/D-04: add `envImportDumpCommand` on the env group. Resolve SeedDump path from config; MarkImporting; call dumpimport with Yes=o.yes; MarkImported or MarkFailed. First recorded auto path exception for nonempty stays in the shared helper used by lifecycle (RESEARCH Pattern 3) — explicit import-dump after imported/failed requires Yes when nonempty. Prefer injectable importer func on options for unit tests (same style as newDeploySteps fakes). Serial tests (AGENTS.md). Do not add a second confirmation flag.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ -count=1 -run 'ImportDump|SeedDump' && ! grep -nE 'Use:[[:space:]]+"seed"|env seed' internal/cli/env.go internal/cli/root.go</automated>
  </verify>
  <done>env import-dump imports with journal transitions; nonempty retry requires --yes; command name is import-dump only.</done>
</task>

<task type="auto" tdd="true">
  <name>Post-deploy auto-import once from recorded</name>
  <files>internal/cli/lifecycle.go, internal/cli/lifecycle_seeddump_test.go</files>
  <reversibility rating="costly">Silent re-import on every deploy would be a data-loss footgun; once-from-recorded is the D-02 contract.</reversibility>
  <behavior>
    - After deployflow.Run returns nil, if SeedDump set and journal status recorded, auto-import runs once
    - Second successful deploy with status imported does not call importer
    - Auto-import runs after lock release / outside deployflow.Run (not inside Pulumi or RunMigrations)
    - Auto-import failure marks failed+reason and makes deploy command exit nonzero (RESEARCH honesty A4)
  </behavior>
  <action>
Per D-01 option-a and D-02: in `runDeploymentWithOptions`, after successful `deployflow.Run` (and equivalently after successful infra-only update path only if Magento install/upgrade candidate already applied — prefer the deployflow success path for Phase 5), call maybeAutoImportSeedDump. Gate on resolved SeedDump nonempty and journal status exactly recorded. Do not hold deploy lock across dump I/O (Pitfall 1). Unit-test with fake steps + injectable importer counting calls (AutoImportOnce / RecordedOnly). Never invoke importer from cloud Pulumi packages. Serial tests (AGENTS.md).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ -count=1 -run 'AutoImportOnce|RecordedOnly|maybeAutoImport|SeedDump'</automated>
  </verify>
  <done>Auto-import fires once from recorded after deploy success; later deploys no-op; failure fails the deploy CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy success → auto-import | Privileged lifecycle side effect |
| Operator --yes → schema-replace | Destructive confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | auto re-import | high | mitigate | D-02 gate status==recorded only; tests AutoImportOnce |
| T-05-10 | Denial of Service | dump under deploy lock | medium | mitigate | Run after deployflow.Run completes / lock released |
| T-05-11 | Tampering | half-applied dump as success | high | mitigate | Journal failed+reason; deploy exits nonzero on auto-import failure |
| T-05-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
CLI import-dump + lifecycle once-only tests green under serial GOMAXPROCS=1.
</verification>

<success_criteria>
Local SC1 path: create --dump + successful deploy triggers one import; retries via import-dump + --yes; no env seed command.
</success_criteria>

<output>
Create `.planning/phases/05-data-migration-cutover/05-04-SUMMARY.md` when done
</output>
