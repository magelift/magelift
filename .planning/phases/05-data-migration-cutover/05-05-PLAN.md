---
phase: 05-data-migration-cutover
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - internal/cli/env.go
  - internal/cli/root.go
  - internal/mediasync/sync.go
  - internal/mediasync/sync_test.go
  - internal/cli/env_media_sync_test.go
  - testdata/fixtures/migrate/media/catalog/product/fixture.txt
  - tests/floci/media_sync_test.go
autonomous: true
requirements:
  - MIGRATE-03

must_haves:
  truths:
    - "magelift env media-sync --source DIR uploads media tree to the env media bucket and listing diff vs source is empty (D-05, MIGRATE-03, SC4)"
    - "Default is merge (upload/overwrite keys; no bucket wipe); key mapping documented in command help (D-05 discretion)"
    - "Offline proof via unit fake and/or Floci MAGELIFT_FLOCI=1; no seedMedia auto-after-deploy this phase"
  artifacts:
    - "internal/mediasync package or equivalent CLI helper"
    - "env media-sync cobra command with --source"
    - "Floci or unit listing-diff test"
  key_links:
    - "media-sync --source → S3 PutObject walk → ListObjectsV2 → empty key-set diff"
---

<objective>
Ship `magelift env media-sync --source` with listing-diff verification proven offline on Floci/unit (D-05, MIGRATE-03, SC4).

Purpose: Media follows DB dumps without a paid AWS pass; seedMedia auto remains deferred.
Output: media-sync command, fixtures, Floci/unit proof.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-data-migration-cutover/05-CONTEXT.md
@.planning/phases/05-data-migration-cutover/05-RESEARCH.md
@tests/floci/storage_test.go
@internal/cli/env.go
@internal/cli/root.go
@docs/capability-matrix.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end media-sync --source → empty listing diff</name>
  <files>internal/mediasync/sync.go, internal/mediasync/sync_test.go, internal/cli/env.go, internal/cli/root.go, internal/cli/env_media_sync_test.go, testdata/fixtures/migrate/media/catalog/product/fixture.txt</files>
  <reversibility rating="reversible">seedMedia auto can layer later without breaking the explicit command (D-05).</reversibility>
  <action>
Per D-05 / RESEARCH Pattern 4: implement media sync as a library (`internal/mediasync`) plus `env media-sync` with required `--source`. Walk source files; map keys — if path contains `pub/media/`, strip to relative under that root; else treat source root as media root. Default merge: PutObject each file; do not delete remote keys absent locally. After upload, ListObjectsV2 and assert key set equals expected relative file set (empty diff) — return error if drift. Confine `--source` (clean path; reject `..` escape). Document key mapping + merge default in Short/Long help. Wire injectable S3 client for unit tests using existing aws-sdk-go-v2 service/s3. Synthetic fixture tree under testdata/fixtures/migrate/media/. Do not add seedMedia YAML or auto-after-deploy. Resolve target bucket from stack outputs / planned media bucket seam already used by Magento media; for unit tests inject bucket+client. Serial GOMAXPROCS=1 (AGENTS.md).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/mediasync/ ./internal/cli/ -count=1 -run 'MediaSync|ListingDiff|mediasync'</automated>
  </verify>
  <done>env media-sync --source uploads fixture tree; listing diff empty in unit tests; help documents merge + key mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Floci media-sync listing-diff proof</name>
  <files>tests/floci/media_sync_test.go</files>
  <behavior>
    - With MAGELIFT_FLOCI=1, sync fixture media to a Floci bucket and listing diff is empty
    - Test skips cleanly when MAGELIFT_FLOCI unset (same pattern as storage_test.go)
  </behavior>
  <action>
Per MIGRATE-03 / SC4: add `tests/floci/media_sync_test.go` under `//go:build floci` mirroring `tests/floci/storage_test.go` client setup (path-style endpoint). Call mediasync against Floci bucket; assert empty listing diff. Do not require paid AWS. If Floci down during execute, leave skip path and note in SUMMARY — unit proof from task 1 still required green. Serial when run via make floci-test.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/mediasync/ ./internal/cli/ -count=1 -run 'MediaSync|ListingDiff' && (MAGELIFT_FLOCI=1 make floci-test || GOMAXPROCS=1 GOFLAGS=-p=1 go test -tags=floci ./tests/floci/ -count=1 -run 'MediaSync|ListingDiff|VersionedMedia' )</automated>
  </verify>
  <done>Unit listing-diff green; Floci case present with skip-or-pass offline proof path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --source directory → object keys | Untrusted local tree |
| CLI → S3/GCS media bucket | Cloud object write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Tampering | path traversal via --source | high | mitigate | Clean + reject .. escape before walk |
| T-05-13 | Information Disclosure | media fixtures | low | accept | Synthetic fixture text only; no customer media in repo |
| T-05-SC | Tampering | package installs | low | accept | Reuse existing aws-sdk-go-v2/service/s3 |
</threat_model>

<verification>
mediasync + CLI tests green; Floci test exists with MAGELIFT_FLOCI skip gate.
</verification>

<success_criteria>
SC4 satisfied offline: media-sync fixture → empty listing diff (unit and/or Floci).
</success_criteria>

<output>
Create `.planning/phases/05-data-migration-cutover/05-05-SUMMARY.md` when done
</output>
