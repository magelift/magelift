---
phase: 05-data-migration-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/model.go
  - internal/config/schema/magelift.schema.json
  - docs/configuration-reference.md
  - internal/seeddump/doc.go
  - internal/seeddump/store.go
  - internal/seeddump/store_test.go
  - internal/cli/env.go
  - internal/cli/env_seeddump_test.go
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02

must_haves:
  truths:
    - "magelift env create --dump <readable.sql> writes environments.<name>.seedDump and config.Load accepts the file (D-01, D-03, MIGRATE-01 Wave 0)"
    - "Create output seedDumpStatus is recorded from the .magelift journal, not the ADR placeholder prose (D-03, MIGRATE-02)"
    - "make generate-check stays green after SeedDump lands on Config and Environment"
  artifacts:
    - "SeedDump string field on config.Config and config.Environment"
    - "internal/seeddump InitRecorded writing .magelift/seed-dumps/<env>.json"
    - "internal/cli/env_seeddump_test.go covering create --dump → Load → Resolve"
  key_links:
    - "env create --dump → YAML seedDump + seeddump.InitRecorded → create JSON seedDumpStatus=recorded"
    - "config.Load KnownFields accepts seedDump after generate"
---

<objective>
Unblock ADR 0010: add `SeedDump` to the KnownFields schema, initialize the seed-dump journal to `recorded` on `env create --dump`, and stop treating the create-output placeholder string as the status source of truth (D-01, D-03).

Purpose: Wave 0 gap — today `--dump` fails Load with `field seedDump not found`. Everything else in Phase 5 depends on this seam.
Output: Schema field + generate artifacts, minimal `internal/seeddump` InitRecorded, create path wiring, offline tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-migration-cutover/05-CONTEXT.md
@.planning/phases/05-data-migration-cutover/05-RESEARCH.md
@docs/adr/0010-database-dump-seed.md
@internal/config/model.go
@internal/cli/env.go
@internal/releasejournal/store.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end create --dump → SeedDump Load → journal recorded</name>
  <files>internal/config/model.go, internal/seeddump/doc.go, internal/seeddump/store.go, internal/seeddump/store_test.go, internal/cli/env.go, internal/cli/env_seeddump_test.go</files>
  <reversibility rating="costly">Journal path under .magelift/seed-dumps becomes the CI/automation status contract (D-03).</reversibility>
  <action>
Per D-01 and D-03 / RESEARCH Wave 0: ship the thinnest production-quality path that makes `--dump` real for config + journal init.

1. Add `SeedDump string` with yaml/json `seedDump,omitempty` and a short config description to both `config.Config` and `config.Environment` in `internal/config/model.go` (mirror Account/Class overlay fields so Resolve populates Effective.Config). Run `make generate` so schema + configuration reference update; `make generate-check` must pass.
2. Create `internal/seeddump` with InitRecorded(projectRoot, env, dumpPath) writing single-document JSON at `.magelift/seed-dumps/<env>.json` (status=recorded, optional dumpPath echo, updatedAt UTC). Mirror releasejournal exclusive lock + atomic rename + 0600 mode (copy pattern from `internal/releasejournal/store.go`; do not reuse the jsonl append API — status is a state machine). Package tests cover init + read roundtrip.
3. In `envCreateCommand` after successful YAML write: when dumpPath nonempty, call InitRecorded using the project root next to the config file; set create result `seedDumpStatus` to the journal status string `recorded` only. Remove the ADR prose placeholder currently at env.go create output. Keep dump path readability check already present.
4. Add `internal/cli/env_seeddump_test.go`: temp magelift.yaml + tiny readable dump file → run create --dump → assert Load/Resolve see SeedDump and journal file status is recorded. Serial only: GOMAXPROCS=1 GOFLAGS=-p=1 (AGENTS.md). Do not implement importer, env status command, or auto-deploy hook yet.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/seeddump/ ./internal/cli/ ./internal/config/ -count=1 -run 'SeedDump|Create.*Dump|InitRecorded' && make generate-check</automated>
  </verify>
  <done>create --dump persists SeedDump through Load/Resolve; journal InitRecorded yields status recorded; create output no longer uses placeholder prose; generate-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Reject unknown seedDumpStatus YAML field honesty</name>
  <files>internal/config/config_test.go, internal/seeddump/store_test.go</files>
  <behavior>
    - YAML with environments.x.seedDump path Loads after SeedDump field exists
    - YAML must not accept a committed seedDumpStatus field on Environment (status lives only in journal per D-03)
    - InitRecorded is idempotent or safely overwrites recorded→recorded without inventing imported
  </behavior>
  <action>
Per D-03: add/adjust config tests proving SeedDump path is accepted and that a bogus `seedDumpStatus` key on an environment overlay still fails KnownFields (status must not become a YAML field). Assert seeddump InitRecorded does not write status into any magelift.yaml helper. Serial Go tests only (AGENTS.md).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/config/ ./internal/seeddump/ -count=1 -run 'SeedDump|KnownFields|InitRecorded|Status'</automated>
  </verify>
  <done>SeedDump path Loads; seedDumpStatus is not a YAML Environment field; journal remains sole status store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator dump path → YAML seedDump | Local filesystem path recorded as intent |
| Journal under .magelift/ | Runtime status; gitignored; must not leak dump contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | magelift.yaml status | medium | mitigate | Status only in journal; KnownFields rejects seedDumpStatus YAML key (task 2) |
| T-05-02 | Information Disclosure | journal / create output | medium | mitigate | Journal stores status/reason/path echo only; never dump file bytes; 0600 files |
| T-05-03 | Tampering | journal write races | medium | mitigate | Exclusive lock + atomic rename copied from releasejournal |
| T-05-SC | Tampering | package installs | low | accept | No new registry packages this plan |
</threat_model>

<verification>
Focused SeedDump/create tests green under serial GOMAXPROCS=1; make generate-check passes.
</verification>

<success_criteria>
`--dump` no longer fails KnownFields; create initializes journal `recorded`; placeholder create-status prose is gone.
</success_criteria>

<output>
Create `.planning/phases/05-data-migration-cutover/05-01-SUMMARY.md` when done
</output>
