---
phase: 05-data-migration-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - internal/seeddump/store.go
  - internal/seeddump/status.go
  - internal/seeddump/store_test.go
  - internal/cli/env.go
  - internal/cli/root.go
  - internal/cli/env_status_test.go
autonomous: true
requirements:
  - MIGRATE-02

must_haves:
  truths:
    - "Operator can run magelift env status <env> and see seedDump path from YAML merged with journal seedDumpStatus (D-03, MIGRATE-02, SC2)"
    - "Journal transitions recorded→importing→imported and recorded→importing→failed+reason are durable under .magelift/ (D-03)"
    - "Status is never written into committed magelift.yaml"
  artifacts:
    - "seeddump.Store with MarkImporting / MarkImported / MarkFailed"
    - "magelift env status cobra command on env group"
    - "env_status_test.go covering merge + failed reason"
  key_links:
    - "env status → Load SeedDump path + seeddump.Read → merged JSON/table output"
---

<objective>
Complete the seed-dump status machine and expose `magelift env status` merging YAML `seedDump` with the `.magelift/` journal (D-03, MIGRATE-02, ROADMAP SC2).

Purpose: Operators need an honest recorded→importing→imported|failed+reason surface before importer and auto-hook land.
Output: Full journal API + `env status` command + tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-data-migration-cutover/05-CONTEXT.md
@.planning/phases/05-data-migration-cutover/05-RESEARCH.md
@.planning/phases/05-data-migration-cutover/05-01-PLAN.md
@internal/seeddump/store.go
@internal/cli/env.go
@internal/cli/root.go
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Journal state machine MarkImporting/Imported/Failed</name>
  <files>internal/seeddump/store.go, internal/seeddump/status.go, internal/seeddump/store_test.go</files>
  <reversibility rating="costly">Status enum strings become automation contracts for CI artifacts (D-03).</reversibility>
  <behavior>
    - InitRecorded then MarkImporting then MarkImported yields status imported
    - MarkFailed stores nonempty reason and status failed
    - Illegal transitions (e.g. imported→importing without operator retry path) are either rejected or documented and tested — prefer explicit errors for corrupt/missing journal
    - Concurrent writers serialize via lock (no torn JSON)
  </behavior>
  <action>
Per D-03 and RESEARCH Pattern 1: expand `internal/seeddump` with Read + MarkImporting + MarkImported + MarkFailed(reason). Persist status, reason (empty when not failed), updatedAt, optional dumpPath. Keep path `.magelift/seed-dumps/<env>.json`. Tests cover happy path and failed+reason. Do not write status into YAML. Serial tests (AGENTS.md).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/seeddump/ -count=1 -run 'Mark|Status|Failed|Import'</automated>
  </verify>
  <done>Journal supports recorded→importing→imported|failed+reason with tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire magelift env status merge YAML + journal</name>
  <files>internal/cli/env.go, internal/cli/root.go, internal/cli/env_status_test.go</files>
  <behavior>
    - env status for a named environment prints/writes seedDump path from config when set
    - seedDumpStatus and failure reason come from journal when present
    - Missing journal with seedDump path still reports path and implies recorded or clearly says status unavailable — prefer reading after create so recorded exists
    - Env without seedDump omits or nulls seed fields honestly
  </behavior>
  <action>
Per D-03 / MIGRATE-02 / SC2: add `envStatusCommand` under the existing `env` cobra group in `internal/cli/root.go`. Resolve environment overlay SeedDump via Load/Resolve; merge journal Read. Use existing `o.write` JSON/table path. Do not invent `env seed`. Do not call importer. Tests use temp config + journal fixtures. Serial GOMAXPROCS=1 (AGENTS.md).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ ./internal/seeddump/ -count=1 -run 'EnvStatus|SeedDumpStatus|Status'</automated>
  </verify>
  <done>magelift env status merges YAML seedDump path with journal status/reason; tests cover imported and failed+reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Journal file → CLI status output | Local runtime state presented to operator/CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Spoofing | env status | medium | mitigate | Merge read-only from journal+YAML; never trust a status field in YAML |
| T-05-05 | Tampering | journal transitions | medium | mitigate | Locked atomic writes; failed requires nonempty reason |
| T-05-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
seeddump transition tests + env status tests green under serial builds.
</verification>

<success_criteria>
SC2 observable offline: status moves recorded→importing→imported|failed with reason via journal APIs and env status merge.
</success_criteria>

<output>
Create `.planning/phases/05-data-migration-cutover/05-02-SUMMARY.md` when done
</output>
