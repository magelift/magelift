---
phase: 02-tag-ready-release-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements:
  - RELEASE-06
user_setup:
  - service: local-toolchain
    why: "Full make verify requires PHP 8.2+ and Composer (php-test). Research host lacked both on 2026-07-28."
    env_vars: []
    dashboard_config:
      - task: "Install PHP 8.2+ and Composer on the machine that will run the fresh-clone proof"
        location: "Host package manager (e.g. brew install php composer) — confirm with `php -v` and `composer -V`"

must_haves:
  truths:
    - "CONTRIBUTING.md alone lists every toolchain prerequisite needed for a full green make verify (D-04, RELEASE-06)"
    - "CONTRIBUTING.md states that local make verify is the contributor gate and that hosted force-all CI remains deferred (Phase 1 HUMAN_GATE / D-07) — it must not claim CI is green on main"
    - "A fresh clone (clean checkout, not the dirty working tree) following only CONTRIBUTING.md reaches green make verify, or the SUMMARY records an unmet PHP/Composer blocker with the exact failing target"
  artifacts:
    - "CONTRIBUTING.md Prerequisites section covering Go (go.mod), PHP 8.2+, Composer, MkDocs, and noting golangci/go-licenses/actionlint come via make go run pins"
    - "CONTRIBUTING.md Testing section with honest local-vs-hosted CI paragraph + optional make ci-act-go note"
  key_links:
    - "CONTRIBUTING Prerequisites → make verify chain (generate-check cli-docs-check fmt-check lint test license-check php-test docs workflow-check)"
    - "Honest deferral pointer to docs/lint-policy.md deferred-ci and/or .planning/loop/HUMAN_GATE without promising hosted green"
---

<objective>
Make CONTRIBUTING.md a trustworthy clone→green path: complete toolchain prerequisites, honest Phase 1 hosted-CI deferral, and a recorded fresh-clone `make verify` proof (D-04, D-07, RELEASE-06).

Purpose: Strangers must not be told "run make verify" while prerequisites or CI claims are incomplete or false.
Output: Updated CONTRIBUTING.md and SUMMARY evidence of a clean-checkout verify run (or explicit toolchain blocker).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tag-ready-release-surface/02-CONTEXT.md
@.planning/phases/02-tag-ready-release-surface/02-RESEARCH.md
@.planning/phases/02-tag-ready-release-surface/02-01-SUMMARY.md
@CONTRIBUTING.md
@Makefile
@docs/lint-policy.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end CONTRIBUTING verify narrative — prerequisites + honest local gate</name>
  <files>CONTRIBUTING.md</files>
  <action>
Rewrite CONTRIBUTING.md so a stranger can follow it alone to a full local `make verify` (per D-04, RELEASE-06), without false hosted-CI claims (per D-07).

Expand Prerequisites to list, at minimum: Go toolchain matching go.mod; PHP 8.2+ and Composer (required because Makefile `verify` includes `php-test`); MkDocs (required because `verify` includes `docs`); Docker only when running Floci/images/Compose (not required for default verify); cloud credentials only for acceptance targets. Note that golangci-lint, go-licenses, and actionlint are pulled via pinned `go run` from Makefile targets — no separate global install required for those.

In Testing (or a short Local verification subsection), state clearly: the contributor gate is local `make verify`; hosted GitHub Actions force-all / QUALITY-06 green-on-main proof remains deferred while Actions minutes are exhausted (Phase 1 HUMAN_GATE). Point to docs/lint-policy.md deferred-ci language and/or `.planning/loop/HUMAN_GATE` for the deferral record. Optionally mention `make ci-act-go` as a Go-only local stand-in via nektos/act — must say it is not a substitute for full `make verify`.

Do not claim CI is green on main. Do not remove the Adding a provider pointer. Keep principles and change workflow intact.
  </action>
  <verify>
    <automated>rg -n 'make verify' CONTRIBUTING.md &amp;&amp; rg -ni 'PHP|Composer|MkDocs' CONTRIBUTING.md &amp;&amp; rg -ni 'deferred|HUMAN_GATE|Actions minutes|hosted' CONTRIBUTING.md &amp;&amp; ! rg -n 'CI is green on main|CI green on main' CONTRIBUTING.md</automated>
  </verify>
  <done>CONTRIBUTING names make verify, PHP/Composer/MkDocs, and an honest hosted-CI deferral; no false green-on-main claim.</done>
</task>

<task type="auto">
  <name>Prove fresh-clone make verify following CONTRIBUTING alone</name>
  <files>CONTRIBUTING.md</files>
  <precondition>PHP 8.2+ (`php -v`) and Composer (`composer -V`) are installed on the proof host, and MkDocs is available (`mkdocs --version`), matching CONTRIBUTING Prerequisites — otherwise halt and record the missing tool in SUMMARY rather than claiming RELEASE-06 closed.</precondition>
  <action>
Execute ROADMAP success criterion 4: a fresh clone / clean checkout following only CONTRIBUTING.md reaches green `make verify`.

Procedure (serial; respect AGENTS.md — no parallel go builds):
1. Create a clean proof tree: `git clone` this repo to a temp directory OR `git worktree add` a clean path at the same commit as HEAD after task 1's CONTRIBUTING commit. Do not use the dirty agent working tree as the proof root.
2. In that clean tree, install nothing beyond what CONTRIBUTING lists; follow CONTRIBUTING steps only.
3. Run `make verify` with `GOMAXPROCS=1` inherited if the Makefile/test paths spawn go test (do not raise parallelism).
4. On success, write `.planning/phases/02-tag-ready-release-surface/scratch/02-02-verify-proof.txt` containing lines: `VERIFY_PROOF_OK`, proof path type (clone vs worktree), commit SHA, UTC timestamp, and `make verify exit 0`. Also summarize the same in SUMMARY.
5. On failure: write the same scratch file with `VERIFY_PROOF_BLOCKED`, failing Make target name, and first actionable error line; mirror into SUMMARY. If the failure is missing PHP/Composer, mark RELEASE-06 blocked on user_setup — do not weaken CONTRIBUTING to a Go-only subset and call it green (research: Go-only is insufficient for RELEASE-06).

Do not run hosted Actions. Do not run `make release-smoke` here (plan 02-04).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-tag-ready-release-surface/scratch/02-02-verify-proof.txt &amp;&amp; rg -q 'VERIFY_PROOF_OK|VERIFY_PROOF_BLOCKED' .planning/phases/02-tag-ready-release-surface/scratch/02-02-verify-proof.txt</automated>
  </verify>
  <done>scratch/02-02-verify-proof.txt and SUMMARY record either green fresh-checkout make verify (VERIFY_PROOF_OK) or an explicit unmet toolchain blocker (VERIFY_PROOF_BLOCKED) naming the failing target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CONTRIBUTING.md → new contributor | Docs claim is the only onboarding contract for clone→verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Spoofing | CONTRIBUTING.md CI claims | high | mitigate | Explicit HUMAN_GATE deferral; forbid green-on-main language; verify grep |
| T-02-04 | Denial of service | make verify on 16GB Mac | medium | mitigate | Serial GOMAXPROCS=1; abort on swap; no parallel agent builds |
| T-02-SC | Tampering | package installs | low | accept | No new Go module deps; host PHP/Composer via user_setup only |
</threat_model>

<verification>
CONTRIBUTING lists full verify toolchains and honest CI deferral; fresh-checkout verify evidence exists in SUMMARY (or blocker documented without false Complete).
</verification>

<success_criteria>
RELEASE-06 truths hold, or SUMMARY clearly blocks on PHP/Composer with no dishonest green claim.
</success_criteria>

<output>
Create `.planning/phases/02-tag-ready-release-surface/02-02-SUMMARY.md` when done
</output>
