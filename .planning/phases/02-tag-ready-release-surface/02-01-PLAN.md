---
phase: 02-tag-ready-release-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/versioning.md
  - docs/release-readiness.md
  - CONTRIBUTING.md
  - SUPPORT.md
  - SECURITY.md
  - CHANGELOG.md
autonomous: true
requirements:
  - RELEASE-01
  - RELEASE-02

must_haves:
  truths:
    - "README.md, docs/versioning.md, and docs/release-readiness.md each name v1.0.0-rc.1 as the first public tag (D-01, RELEASE-01)"
    - "Those three surfaces no longer describe the product as an unreleased maturity label or a v0-first public line (CHANGELOG historical notes may remain)"
    - "docs/versioning.md carries an RC stability statement for sdk/v1 and platform.StackModule that explicitly reserves Phase 6 shared-Kubernetes port consolidation as allowed RC churn (D-02, RELEASE-02)"
    - "The release-readiness gate board contract row names the RC freeze surface, not a v0 freeze label"
  artifacts:
    - "README.md status banner naming v1.0.0-rc.1"
    - "docs/versioning.md rewritten around RC.1 with stability + Phase 6 reservation sections"
    - "docs/release-readiness.md contract-row wording aligned to RC.1"
    - "SUPPORT.md, SECURITY.md, CONTRIBUTING.md opening product-status claims aligned (satellite strip per D-01)"
    - "CHANGELOG.md [Unreleased] forward claim aligned to RC.1 (history entries untouched)"
  key_links:
    - "README status banner → docs/versioning.md RC contract → release-readiness contract row evidence"
    - "versioning.md Phase 6 reservation must name shared-Kubernetes Observe / deploy.Steps consolidation so Phase 6 can change kube-shaped platform ports without a fake v2 wait"
---

<objective>
Make the public version story and RC contract consistent: first public tag is `v1.0.0-rc.1`, with an explicit `sdk/v1` + `platform.StackModule` stability statement that reserves Phase 6 shared-Kubernetes port changes.

Purpose: Phase 2 cannot close packaging or contributor paths while README, versioning, and the gate board still advertise a v0-first / unreleased maturity narrative (per D-01, D-02, RELEASE-01, RELEASE-02).
Output: Updated README, versioning.md, release-readiness contract row, satellite claim strips, and Unreleased CHANGELOG alignment.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tag-ready-release-surface/02-CONTEXT.md
@.planning/phases/02-tag-ready-release-surface/02-RESEARCH.md
@README.md
@docs/versioning.md
@docs/release-readiness.md
@CONTRIBUTING.md
@SUPPORT.md
@SECURITY.md
@CHANGELOG.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end RC.1 naming across README, versioning intro, and gate-board contract row</name>
  <files>README.md, docs/versioning.md, docs/release-readiness.md</files>
  <action>
Wire ONE consistent first-public-tag claim through the three ROADMAP criterion-1 surfaces (per D-01, RELEASE-01). Do not yet write the full RC stability matrix — that is the next task.

In README.md, replace the project-status banner so it names `v1.0.0-rc.1` as the first public tag and points to docs/versioning.md for the frozen CLI/YAML / contract surface. Remove any product-status wording that still frames the project as waiting on a v0-first public line.

In docs/versioning.md, rewrite the opening so SemVer applies from `v1.0.0-rc.1`, retire the heading/framing that freezes a v0 surface, and keep the existing freeze-table rows as the RC freeze surface (schemaVersion additive rules, CLI verb names, exit codes, certified AWS ECS cells honesty labels, provider ID strings). Leave a clear placeholder heading or short stub for the stability statement section that task 2 will fill — do not invent Phase 6 reservation text here beyond a one-line TODO comment if needed for structure.

In docs/release-readiness.md gate board, rename the contract-freeze row so it is no longer labeled as a v0 freeze; evidence still points at versioning.md + CHANGELOG Unreleased. Do not change the Packaging smoke row status (stays Partial until plan 02-04 / D-03).

Do not edit SUPPORT.md, SECURITY.md, CONTRIBUTING.md, or CHANGELOG.md in this tracer — satellites are task 3.
  </action>
  <verify>
    <automated>rg -n 'v1\.0\.0-rc\.1' README.md docs/versioning.md docs/release-readiness.md &amp;&amp; test "$(rg -n 'v1\.0\.0-rc\.1' README.md docs/versioning.md docs/release-readiness.md | wc -l | tr -d ' ')" -ge 3</automated>
  </verify>
  <done>All three named surfaces mention v1.0.0-rc.1; packaging row still Partial; stability section not required complete yet.</done>
</task>

<task type="auto">
  <name>Write RC stability statement for sdk/v1 and platform.StackModule with Phase 6 reservation</name>
  <files>docs/versioning.md</files>
  <action>
Complete RELEASE-02 / D-02 in docs/versioning.md. Add an explicit RC stability statement covering `sdk/v1` and `platform.StackModule` (discretion on exact wording; Phase 6 reservation must be explicit).

Stable during RC series (lock these as stable unless a documented RC break note lands): `magelift.yaml` schemaVersion additive rules; CLI verb names; documented exit codes; provider ID strings; certified AWS ECS honesty labels; `sdk/v1` Target / Capability / Hook shapes.

May change during RC: experimental providers and experimental cells returning `ErrNotSupported`; day-2 port shapes on Kubernetes-shaped modules pending Phase 6.

Explicit reservation (required): Phase 6 shared-Kubernetes consolidation of Observe + `deploy.Steps` into `internal/cloud/kube` may change `platform` port wiring for EKS / GKE / MKS / Kapsule modules during the RC series without waiting for a major `v2` bump. Cite ROADMAP Phase 6 as the planned work.

Keep SemVer honesty: `1.0.0-rc.1` is a pre-release and may not satisfy full `1.0.0` compatibility — state what is stable vs reserved above rather than inventing a fake full freeze.

Update the Changelog section so post-tag release-note language no longer assumes a first tag of `v0.1.0`; point first public tag criteria at `v1.0.0-rc.1` and release-readiness.md.
  </action>
  <verify>
    <automated>rg -n 'sdk/v1' docs/versioning.md &amp;&amp; rg -n 'StackModule' docs/versioning.md &amp;&amp; rg -n 'Phase 6' docs/versioning.md &amp;&amp; rg -ni 'shared.?kubernetes|internal/cloud/kube|Observe' docs/versioning.md</automated>
  </verify>
  <done>versioning.md names sdk/v1, StackModule, Phase 6, and shared-Kubernetes / kube port reservation as allowed RC churn.</done>
</task>

<task type="auto">
  <name>Strip satellite product-status claims and align CHANGELOG Unreleased</name>
  <files>CONTRIBUTING.md, SUPPORT.md, SECURITY.md, CHANGELOG.md</files>
  <action>
Per D-01 satellite strip (research A2): remove opening product-status claims in CONTRIBUTING.md, SUPPORT.md, and SECURITY.md that still frame MageLift as an unreleased maturity product. Keep each file's real purpose (contrib process, support posture, vulnerability reporting). Do not rewrite CONTRIBUTING's verify prerequisites or CI honesty — plan 02-02 owns RELEASE-06 depth; only the status sentence / equivalent claim changes here.

In CHANGELOG.md `[Unreleased]`, rewrite the forward-looking contract-freeze bullet so it names `v1.0.0-rc.1` / RC contract (research A1). Do not rewrite older dated historical entries that truthfully describe past work.

After edits, run a product-surface grep (not CHANGELOG history): README.md, docs/versioning.md, docs/release-readiness.md, CONTRIBUTING.md, SUPPORT.md, SECURITY.md must not retain product-status claims of the banned maturity label or a v0-first public line. Record any remaining intentional historical mentions in SUMMARY only if found outside those files.
  </action>
  <verify>
    <automated>rg -n 'v1\.0\.0-rc\.1' CHANGELOG.md &amp;&amp; ! rg -n 'pre-alpha' README.md docs/versioning.md docs/release-readiness.md CONTRIBUTING.md SUPPORT.md SECURITY.md &amp;&amp; ! rg -n 'v0\.x' README.md docs/versioning.md docs/release-readiness.md CONTRIBUTING.md SUPPORT.md SECURITY.md</automated>
  </verify>
  <done>Satellite files no longer carry banned product-status claims; Unreleased CHANGELOG aligns to RC.1; criterion-1 greps pass on named surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs → contributor trust | Readers treat README / versioning / gate board as truth about release readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | README / versioning / gate board | high | mitigate | Align all three surfaces to v1.0.0-rc.1; grep gate in task verify |
| T-02-02 | Repudiation | docs/versioning.md RC contract | medium | mitigate | Explicit Phase 6 reservation so later kube port churn is foreshadowed, not silent |
| T-02-SC | Tampering | package installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
`rg -n 'v1\.0\.0-rc\.1' README.md docs/versioning.md docs/release-readiness.md` hits all three; `rg -n 'StackModule|Phase 6' docs/versioning.md` hits; banned maturity / v0-first product claims absent from the named product surfaces (CHANGELOG history excepted).
</verification>

<success_criteria>
RELEASE-01 and RELEASE-02 observable truths hold; packaging row still Partial; CONTRIBUTING verify-depth still pending plan 02-02.
</success_criteria>

<output>
Create `.planning/phases/02-tag-ready-release-surface/02-01-SUMMARY.md` when done
</output>
