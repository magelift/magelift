---
phase: 02-tag-ready-release-surface
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - docs/release-readiness.md
autonomous: false
requirements:
  - RELEASE-04

must_haves:
  truths:
    - "make release-smoke (or ./scripts/release-smoke-local.sh) completes with exit 0 on the maintainer machine in a plain Terminal — serial single-target only (D-03, RELEASE-04, AGENTS.md)"
    - "The Packaging smoke gate board row moves from Partial to Closed with run date and output recorded"
    - "No full multi-platform goreleaser matrix and no parallelism above 1 were used"
  artifacts:
    - "docs/release-readiness.md Packaging smoke row Status Closed with evidence date + one-line smoke ok output"
    - "Updated Packaging smoke record bullet for the successful run (board table row sufficient; no dist/ binaries committed)"
  key_links:
    - "make release-smoke → scripts/release-smoke-local.sh → goreleaser check + build --snapshot --single-target --parallelism=1 → board Closed"
---

<objective>
Close the packaging gate: successful serial host `make release-smoke` outside Cursor, then mark Packaging smoke Closed on the release-readiness board with date + output (D-03, RELEASE-04).

Purpose: The board is still Partial after a 2026-07-22 Cursor abort; tag readiness requires a real host binary smoke, not `goreleaser check` alone.
Output: Board row Closed + smoke record; HUMAN_GATE for the heavy build.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tag-ready-release-surface/02-CONTEXT.md
@.planning/phases/02-tag-ready-release-surface/02-RESEARCH.md
@docs/release-readiness.md
@scripts/release-smoke-local.sh
@Makefile
@AGENTS.md
@.cursor/rules/serial-builds-only.mdc
</context>

<tasks>

<task type="auto">
  <name>Confirm packaging smoke script still matches AGENTS.md serial contract</name>
  <files>scripts/release-smoke-local.sh, docs/release-readiness.md</files>
  <action>
Before asking the human to burn a heavy build, confirm the existing gate path is still the only allowed local packaging smoke (per D-03, AGENTS.md).

Read scripts/release-smoke-local.sh and verify it still runs `goreleaser check` then `goreleaser build --snapshot --clean --single-target --parallelism=1` under `GOMAXPROCS=1` and `GOFLAGS=-p=1`, asserts a host magelift binary, LICENSE/NOTICE presence, and prints a `release smoke ok binary=…` line. Do not raise parallelism. Do not add multi-platform targets. Do not run the smoke under Cursor in this task.

Optionally add a one-line note under docs/release-readiness.md Local packaging smoke that Phase 2 closes this gate only via plain Terminal `make release-smoke` when the machine is idle — if that sentence is already present, leave the prose alone.

Do not flip Packaging smoke to Closed yet — that waits for human evidence in the next tasks.
  </action>
  <verify>
    <automated>rg -n 'single-target' scripts/release-smoke-local.sh &amp;&amp; rg -n 'parallelism=1' scripts/release-smoke-local.sh &amp;&amp; rg -n 'GOMAXPROCS' scripts/release-smoke-local.sh &amp;&amp; rg -n 'release smoke ok' scripts/release-smoke-local.sh &amp;&amp; rg -n 'Packaging smoke' docs/release-readiness.md | rg -q 'Partial'</automated>
  </verify>
  <done>Script still enforces serial single-target; board packaging row still Partial pending human run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>HUMAN_GATE: plain Terminal make release-smoke</name>
  <what-built>
    Packaging smoke path is already scripted (`make release-smoke` → `./scripts/release-smoke-local.sh`). Agents must not run this heavy host build under Cursor (prior abort ~2.3 GB swap on 2026-07-22; AGENTS.md / serial-builds-only).
  </what-built>
  <how-to-verify>
    1. Quit or fully idle Cursor (and other heavy IDEs) on the 16 GB Mac.
    2. Open a plain Terminal.app (or similar) outside Cursor.
    3. `cd` to the magelift repo at the commit that contains plan 02-01 docs (and this plan's script check).
    4. Run exactly: `make release-smoke`
    5. Expect exit 0 and a final line matching `release smoke ok binary=… (serial single-target)`.
    6. If swap climbs or the machine crawls: abort immediately (Ctrl-C), reap orphan `go tool compile` only if no intentional build remains, do not retry under Cursor — retry later in plain Terminal when idle.
    7. Paste the final `release smoke ok binary=…` line (and date) back into the chat as the resume signal payload.
  </how-to-verify>
  <resume-signal>Paste: approved — plus the exact `release smoke ok binary=…` line and UTC date. Or describe failure/abort.</resume-signal>
</task>

<task type="auto">
  <name>Close Packaging smoke row with date and output evidence</name>
  <files>docs/release-readiness.md</files>
  <precondition>Human resume signal includes an exit-0 `release smoke ok binary=…` line from a plain Terminal run — do not Close from goreleaser check alone.</precondition>
  <action>
Per D-03 / RELEASE-04 and research discretion (board table row sufficient; no committed `dist/` artifacts):

Update docs/release-readiness.md gate board Packaging smoke row from Partial to **Closed**. Evidence column: UTC run date + the one-line `release smoke ok binary=…` text from the human resume signal.

Append a bullet under Packaging smoke record for this successful run (keep the 2026-07-22 abort bullet as history).

Do not Close or Defer other gate-board rows (RELEASE-05 is Phase 8 / D-06 — out of scope). Do not commit anything under `dist/`. Do not claim hosted release CI ran.
  </action>
  <verify>
    <automated>rg -n '\| Packaging smoke \| \*\*Closed\*\*' docs/release-readiness.md &amp;&amp; rg -n 'release smoke ok binary=' docs/release-readiness.md &amp;&amp; test "$(rg -n '\| Packaging smoke \| \*\*Partial\*\*' docs/release-readiness.md | wc -l | tr -d ' ')" -eq 0</automated>
  </verify>
  <done>Packaging smoke is Closed with date + smoke ok line recorded; other board rows untouched for RELEASE-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer machine → public gate board | Smoke evidence becomes the published Closed claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Spoofing | Packaging smoke Closed claim | high | mitigate | Require human plain-Terminal ok line before flipping Partial→Closed; forbid check-only Close |
| T-02-08 | Denial of service | goreleaser on 16GB Mac | high | mitigate | HUMAN_GATE outside Cursor; single-target parallelism=1; abort-on-swap protocol |
| T-02-09 | Information disclosure | smoke evidence in git | low | mitigate | Record command output only; never commit dist/ binaries or credentials |
| T-02-SC | Tampering | package installs | low | accept | Existing pinned goreleaser via go run; no new deps |
</threat_model>

<verification>
Board Packaging smoke Closed with recorded ok line; script still serial; RELEASE-05 rows not bulk-closed.
</verification>

<success_criteria>
RELEASE-04 truths hold; Phase 2 packaging gate is Closed on evidence.
</success_criteria>

<output>
Create `.planning/phases/02-tag-ready-release-surface/02-04-SUMMARY.md` when done
</output>
