---
phase: 02-tag-ready-release-surface
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - docs/adding-a-provider.md
  - examples/custom-cli/README.md
  - examples/custom-cli/main.go
  - examples/custom-cli/stub_module.go
autonomous: true
requirements:
  - RELEASE-03

must_haves:
  truths:
    - "docs/adding-a-provider.md plus examples/custom-cli document a clean-GOMODCACHE build that registers a community StackModule without reading core source (D-05, RELEASE-03)"
    - "Docs state honestly that registration uses this module's internal/platform + internal/cli (compile-time custom binary) — separate Go modules cannot import internal/ (research A3 / Pitfall 4)"
    - "A clean empty GOMODCACHE (and empty GOCACHE) build of ./examples/custom-cli succeeds and the binary runs version"
  artifacts:
    - "docs/adding-a-provider.md Community section with clean-cache verification steps"
    - "examples/custom-cli/README.md matching verification recipe"
    - "examples/custom-cli stub StackModule registered from main (buildable community slot)"
  key_links:
    - "adding-a-provider.md → examples/custom-cli main RegisterModule → platform.ModuleRegistry"
    - "Clean GOMODCACHE env → go build ./examples/custom-cli proves docs-only path"
---

<objective>
Prove a third party can follow `docs/adding-a-provider.md` + `examples/custom-cli` to register an out-of-tree (custom-binary) provider from a clean module cache without consulting core source (D-05, RELEASE-03).

Purpose: Community registration is the public extensibility story; it must be documentable and buildable, without overclaiming a separate-module import of `internal/`.
Output: Doc verification recipe, optional stub module, and clean-cache build evidence in SUMMARY.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tag-ready-release-surface/02-CONTEXT.md
@.planning/phases/02-tag-ready-release-surface/02-RESEARCH.md
@docs/adding-a-provider.md
@examples/custom-cli/README.md
@examples/custom-cli/main.go
@internal/platform/module.go
@docs/adr/0007-multi-provider-community-targets.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end clean-GOMODCACHE custom-cli build via documented recipe</name>
  <files>docs/adding-a-provider.md, examples/custom-cli/README.md</files>
  <action>
Add a verification recipe to both docs/adding-a-provider.md (Community binary section) and examples/custom-cli/README.md so RELEASE-03 can be checked without reading core packages (per D-05).

Recipe must: create empty `GOMODCACHE` and `GOCACHE` temp dirs; run `GOMAXPROCS=1 GOFLAGS=-p=1 go build -o /tmp/magelift-ext ./examples/custom-cli` from the repo root; run `/tmp/magelift-ext version`. State that the verifier should use only these docs + the example tree — not browse `internal/cloud/**` for how to register.

Honesty (research A3): document that community providers for MageLift today are compile-time custom binaries of this module (or a fork). Go's `internal/` rule means a separate module path cannot import `internal/platform` or `internal/cli`. Do not claim publish-to-proxy.golang.org of an external module that imports those packages. Future public export of platform is out of Phase 2 scope.

Keep ADR 0007 compile-time RegisterModule rule; no dynamic plugins.

Then execute the recipe once (serial) and record exit status + `version` output line in SUMMARY.
  </action>
  <verify>
    <automated>rg -n 'GOMODCACHE' docs/adding-a-provider.md examples/custom-cli/README.md &amp;&amp; rg -ni 'internal/' docs/adding-a-provider.md examples/custom-cli/README.md &amp;&amp; export GOMODCACHE="$(mktemp -d /tmp/magelift-modcache.XXXXXX)" GOCACHE="$(mktemp -d /tmp/magelift-gocache.XXXXXX)" &amp;&amp; GOMAXPROCS=1 GOFLAGS=-p=1 go build -o /tmp/magelift-ext-phase2 ./examples/custom-cli &amp;&amp; /tmp/magelift-ext-phase2 version</automated>
  </verify>
  <done>Both docs contain GOMODCACHE clean-cache steps and internal/ honesty; clean-cache build + version succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Add buildable community stub StackModule in examples/custom-cli</name>
  <files>examples/custom-cli/stub_module.go, examples/custom-cli/main.go, examples/custom-cli/README.md</files>
  <behavior>
    - Registering stub Module{} alongside first-party modules succeeds (RegisterModule returns nil)
    - Descriptor() returns a community/experimental-labeled target id that does not collide with aws/gcp first-party ids
    - Plan() returns a clear error (or ErrNotSupported-shaped message) so the stub is not a fake deployable provider
    - Clean GOMODCACHE build of ./examples/custom-cli still succeeds after stub is wired
  </behavior>
  <action>
Implement research open-question recommendation: a minimal stub `StackModule` in package main under examples/custom-cli (e.g. stub_module.go) so the community registration slot is literal, not only a comment.

Implement Descriptor, CertificationTier (community or experimental), Plan (refuse with a clear message — not a real Pulumi program), Program, and OutputKeys per internal/platform.StackModule. Choose provider/runtime ids that cannot collide with registered first-party modules.

Wire `stubModule{}` (or equivalent) into the RegisterModule loop in main.go; remove the commented-only community slot.

Update README to say the stub demonstrates registration; real providers replace Plan/Program with a real stack.

Do not export platform from the core module. Do not add new third-party Go dependencies. Keep builds serial (`GOMAXPROCS=1 GOFLAGS=-p=1`).
  </action>
  <verify>
    <automated>export GOMODCACHE="$(mktemp -d /tmp/magelift-modcache2.XXXXXX)" GOCACHE="$(mktemp -d /tmp/magelift-gocache2.XXXXXX)" &amp;&amp; GOMAXPROCS=1 GOFLAGS=-p=1 go build -o /tmp/magelift-ext-stub ./examples/custom-cli &amp;&amp; /tmp/magelift-ext-stub version &amp;&amp; rg -n 'RegisterModule' examples/custom-cli/main.go &amp;&amp; test -f examples/custom-cli/stub_module.go</automated>
  </verify>
  <done>stub_module.go exists, is registered from main, clean-cache build+version pass, Plan refuses real deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Community docs → external developer | Docs define what can be registered and how trust works |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | community provider loading | high | mitigate | Keep compile-time RegisterModule only (ADR 0007); no dynamic plugin loader |
| T-02-06 | Spoofing | adding-a-provider.md claims | medium | mitigate | Document internal/ import boundary so separate-module overclaim is impossible |
| T-02-SC | Tampering | package installs | low | accept | No new module dependencies |
</threat_model>

<verification>
Clean GOMODCACHE build of examples/custom-cli succeeds; docs include recipe + internal/ honesty; stub registered.
</verification>

<success_criteria>
RELEASE-03 observable truths hold without exporting platform or adding plugins.
</success_criteria>

<output>
Create `.planning/phases/02-tag-ready-release-surface/02-03-SUMMARY.md` when done
</output>
