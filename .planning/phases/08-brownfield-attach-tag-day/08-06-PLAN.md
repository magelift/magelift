---
phase: 08-brownfield-attach-tag-day
plan: 06
type: execute
wave: 5
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - docs/release-readiness.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-brownfield-attach-tag-day/scratch/08-06-offline-evidence.md
  - .planning/phases/08-brownfield-attach-tag-day/scratch/08-06-aws-adopt-confirm.md
autonomous: false
requirements:
  - RELEASE-05
  - ATTACH-01
  - ATTACH-02
  - ATTACH-03
  - ATTACH-04
user_setup:
  - service: aws
    why: "Free-tier VPC+RDS adopt confirmation (spend map pass 3/3) when ADC available"
    env_vars: []
    dashboard_config:
      - task: "aws login / aws sso login / refresh ADC until aws sts get-caller-identity succeeds"
        location: "Local terminal (interactive)"

must_haves:
  truths:
    - "Offline Floci-or-mock evidence for import/adopt mechanics is recorded before any paid AWS claim (D-04)"
    - "When AWS ADC works, free-tier VPC+RDS adopt confirm runs once and records describe-after-destroy intact resources; if ADC unavailable, HUMAN_GATE blocks only the paid cell (D-04)"
    - "Every docs/release-readiness.md row is Closed with evidence or Deferred with reason; GCP certify / Magento deploy Ops rows stay Pending→07 or Deferred→07 until 07-06/07 live evidence — never fake-certified (D-05)"
    - "Hosted CI / GitHub Actions minutes row is Deferred (Act-only) per maintainer lock (D-05)"
    - "Every REQUIREMENTS row is Complete or has a recorded deferral; Cloud SQL attach deferred explicitly; ATTACH-01..04 marked Complete when offline+docs done (paid confirm evidence attached or gate noted)"
  artifacts:
    - "scratch/08-06-offline-evidence.md with mock/Floci commands + outcomes"
    - "scratch/08-06-aws-adopt-confirm.md when ADC available (or HUMAN_GATE hold note)"
    - "docs/release-readiness.md fully Closed/Deferred/Pending→07"
    - ".planning/REQUIREMENTS.md ATTACH/RELEASE rows settled"
  key_links:
    - "offline evidence → RELEASE-05 attach rows Closed"
    - "GCP board rows → Phase 7 evidence only"
    - "Act-only CI → Deferred reason"
---

<objective>
Close tag day honesty: record D-04 offline (+ optional paid AWS) evidence and settle every RELEASE-05 / REQUIREMENTS row without fake-certifying GCP (D-04, D-05, RELEASE-05).

Purpose: SC5 — no undetermined gate or requirement on tag day.
Output: evidence scratch files, updated release-readiness board, REQUIREMENTS settlements.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-brownfield-attach-tag-day/08-CONTEXT.md
@.planning/phases/08-brownfield-attach-tag-day/08-RESEARCH.md
@.planning/phases/08-brownfield-attach-tag-day/08-01-SUMMARY.md
@.planning/phases/08-brownfield-attach-tag-day/08-02-SUMMARY.md
@.planning/phases/08-brownfield-attach-tag-day/08-03-SUMMARY.md
@.planning/phases/08-brownfield-attach-tag-day/08-04-SUMMARY.md
@.planning/phases/08-brownfield-attach-tag-day/08-05-SUMMARY.md
@docs/release-readiness.md
@docs/brownfield-attach.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Record offline Floci-or-mock adopt evidence (D-04)</name>
  <files>.planning/phases/08-brownfield-attach-tag-day/scratch/08-06-offline-evidence.md</files>
  <action>
Per D-04: run serial offline proof first — at minimum the package tests from 08-01..04 (`GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/network/ ./internal/cloud/aws/database/ ./internal/cloud/aws/stack/ ./internal/cli/ -count=1 -run 'Existing|Adopt|Refuse|Detach'`). Optionally run `make floci-test` only if Docker is healthy and memory allows; Floci is emulator evidence only, not AWS certification. Write scratch/08-06-offline-evidence.md with commands, exit codes, and which ATTACH behaviors they cover. Do not open paid AWS in this task.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/network/ ./internal/cloud/aws/database/ ./internal/cloud/aws/stack/ ./internal/cli/ -count=1 -run 'Existing|Adopt|Refuse|Detach' && test -f .planning/phases/08-brownfield-attach-tag-day/scratch/08-06-offline-evidence.md && rg -n 'pass|PASS|exit' .planning/phases/08-brownfield-attach-tag-day/scratch/08-06-offline-evidence.md | grep -v '^#' | grep -cE 'pass|PASS|exit' | awk '{if($1+0==0){print "evidence incomplete"; exit 1} print "offline evidence recorded"}'</automated>
  </verify>
  <done>Offline adopt evidence file exists with real command outcomes (D-04 mocks-first).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Free-tier AWS VPC+RDS adopt confirm when ADC available</name>
  <what-built>Offline adopt path (08-01..05) + offline evidence file. Paid confirm is spend map pass 3 of 3.</what-built>
  <how-to-verify>
1. Run `aws sts get-caller-identity`. If it fails, leave scratch/08-06-aws-adopt-confirm.md noting HUMAN_GATE blocked on ADC and resume when login works — do not invent PASS.
2. If identity works: adopt a pre-existing free-tier VPC + RDS MySQL via magelift.yaml existing.network + existing.database; run preview (expect ADOPT lines); apply; destroy MageLift stack; run `aws ec2 describe-vpcs` and `aws rds describe-db-instances` and confirm adopted resources still exist.
3. Record commands + outputs in scratch/08-06-aws-adopt-confirm.md. Serial only; abort if swap climbs (AGENTS.md).
4. Type approved when evidence file is honest (PASS or blocked-on-ADC).
  </how-to-verify>
  <resume-signal>Type "approved" when confirm file is written (PASS or ADC-blocked), or describe issues</resume-signal>
</task>

<task type="auto">
  <name>RELEASE-05 gate board + REQUIREMENTS settlement</name>
  <files>docs/release-readiness.md, .planning/REQUIREMENTS.md</files>
  <action>
Per D-05 / RELEASE-05: audit every docs/release-readiness.md gate row — set Closed with named evidence, Deferred with reason, or Pending→07 / Deferred→07 for GCP Magento deploy Ops / GCP certify / Cloudflare DNS cutover until Phase 7 live evidence exists. Never mark GCP certified without .magelift/gcp-matrix live PASS rows. Mark hosted CI / GitHub Actions minutes as Deferred (Act-only until minutes return) — do not reopen as Closed. Add brownfield-attach / ATTACH rows Closed citing 08-01..05 + offline evidence (and paid confirm if PASS). In .planning/REQUIREMENTS.md: mark ATTACH-01..04 Complete when offline+docs criteria met; RELEASE-05 Complete when board has no undetermined rows; record explicit deferral for Cloud SQL / multi-cloud attach and any unpaid AWS confirm if still ADC-blocked. Update REQUIREMENTS traceability table statuses to match.
  </action>
  <verify>
    <automated>rg -n '^\| ' docs/release-readiness.md | grep -v '^#' | grep -viE 'Closed|Deferred|Pending|Offline closed|Gate \| Status' | grep -c '|' | awk '{if($1+0!=0){print "undetermined gate rows:"; exit 1} print "board settled"}' && rg -n 'GCP|Act-only|hosted CI|GitHub Actions' docs/release-readiness.md | grep -v '^#' | grep -cE 'Pending → 07|Pending→07|Deferred → Phase 7|Deferred.*Act-only|Act-only' | awk '{if($1+0==0){print "GCP/CI honesty missing"; exit 1} print "GCP/CI honest"}' && rg -n 'ATTACH-0|RELEASE-05|Cloud SQL' .planning/REQUIREMENTS.md | grep -v '^#' | head -20</automated>
  </verify>
  <done>No undetermined gate or requirement; GCP Pending→07; Act-only CI Deferred; ATTACH/RELEASE rows settled (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag board → public claim | False Closed on GCP/CI would poison release honesty |
| Paid AWS confirm → account | Spend + destroy discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-15 | Repudiation | release-readiness.md | high | mitigate | D-05: GCP stays Pending→07; Act-only CI Deferred; every row Closed/Deferred/Pending with reason |
| T-08-16 | Tampering | fake certify | critical | mitigate | Forbid Closed on GCP without 07-06/07 matrix evidence |
| T-08-17 | Elevation of Privilege | paid AWS confirm | medium | mitigate | HUMAN_GATE; destroy MageLift stack; verify adopted resources intact |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
Offline tests green; evidence files present; board and REQUIREMENTS have no undetermined ATTACH/RELEASE/GCP/CI rows.
</verification>

<success_criteria>
SC5 + D-04 + D-05: tag board honest; offline evidence recorded; paid AWS confirm PASS or ADC HUMAN_GATE noted.
</success_criteria>

<output>
Create `.planning/phases/08-brownfield-attach-tag-day/08-06-SUMMARY.md` when done
</output>
