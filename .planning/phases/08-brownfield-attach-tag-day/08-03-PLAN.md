---
phase: 08-brownfield-attach-tag-day
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - internal/cloud/aws/database/database.go
  - internal/cloud/aws/database/database_test.go
  - internal/cloud/aws/stack/component.go
  - internal/cloud/aws/stack/component_test.go
autonomous: true
requirements:
  - ATTACH-02

must_haves:
  truths:
    - "database.New with Existing set registers the component and creates zero rds.Instance / rds.Cluster / subnet-group children (D-01, ATTACH-02)"
    - "WriterEndpoint and MasterSecretARN outputs come from Existing.Endpoint and Existing.SecretARN refs"
    - "stack.New wires Spec.Existing.Database into database.Args.Existing and still grants execution role access to the adopted secret ARN only"
  artifacts:
    - "database.ExistingDatabase + Existing branch early-return in database.go"
    - "TestExistingDatabaseUsesRefsWithoutCreatingRDSResources (or equivalent)"
    - "stack component branch when Spec.Existing.Database != nil"
  key_links:
    - "Spec.Existing.Database → database.Args.Existing → component outputs → ECS execution policy"
---

<objective>
Wire the database Existing reference-without-own path and stack composition so apply adopts RDS without creating or replacing it (ATTACH-02 apply, D-01).

Purpose: Close the real capability gap — stacks always called database.New create path today.
Output: Existing branch in database package + stack.New wiring + mock graph tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brownfield-attach-tag-day/08-CONTEXT.md
@.planning/phases/08-brownfield-attach-tag-day/08-RESEARCH.md
@.planning/phases/08-brownfield-attach-tag-day/08-02-SUMMARY.md
@internal/cloud/aws/network/network.go
@internal/cloud/aws/database/database.go
@internal/cloud/aws/database/database_test.go
@internal/cloud/aws/stack/component.go
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>database.Existing early-return (zero RDS creates)</name>
  <files>internal/cloud/aws/database/database.go, internal/cloud/aws/database/database_test.go</files>
  <behavior>
    - When Args.Existing != nil, New validates identifier/endpoint/secretArn then registers component outputs from refs
    - Mock graph: zero aws:rds/instance:Instance, aws:rds/cluster:Cluster, aws:rds/subnetGroup:SubnetGroup
    - Invalid Existing (empty endpoint or secret) fails before RegisterResourceOutputs / child creates
  </behavior>
  <action>
Per D-01 / ATTACH-02: mirror network.Existing — add ExistingDatabase struct (Identifier, Endpoint, SecretARN) and Args.Existing. When set, validate then set WriterEndpoint/ReaderEndpoint/MasterSecretARN/ClusterARN-or-equivalent from refs and return with no rds.New* children. Prefer reference-without-own over Pulumi Import (research). Reuse secret ARN regex patterns from the package or stack. Add pulumi.WithMocks test asserting zero RDS creates and outputs match refs. Greenfield Aurora/RDS paths must stay unchanged.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/database/ -count=1 -run 'Existing|PreviewGraph|Reject' && rg -n 'Args\.Existing|ExistingDatabase' internal/cloud/aws/database/database.go | grep -v '^#' | grep -c Existing | awk '{if($1+0==0){print "Existing branch missing"; exit 1} print "Existing branch present"}'</automated>
  </verify>
  <done>Existing DB path creates no RDS custom resources; outputs come from operator refs.</done>
</task>

<task type="auto" tdd="true">
  <name>Stack wires Existing.Database into database.New</name>
  <files>internal/cloud/aws/stack/component.go, internal/cloud/aws/stack/component_test.go</files>
  <behavior>
    - When Spec.Existing.Database != nil, database.Args.Existing is populated from Spec (externalId, endpoint, secretArn)
    - Execution role policy still scopes to MasterSecretARN from the adopted secret
    - Stack composition test or focused unit proves Existing branch is selected (no RDS creates in mock when possible)
  </behavior>
  <action>
Per D-01 / ATTACH-02: in stack/component.go where database.New is called, if Spec.Existing.Database != nil set database.Args.Existing from Spec fields introduced in 08-02. Keep IAM execution policy grant on MasterSecretARN so ECS can inject the adopted secret — policy must remain ARN-scoped (no broaden-to-*). Prefer extending an existing component mock test if cheap; otherwise add a focused wiring test. Do not invent Cloud SQL paths. Serial builds only.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/database/ -count=1 -run 'Existing' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'ExistingDatabase|ComposesPreview|CatalogCell'</automated>
  </verify>
  <done>Stack adopt path feeds Existing into database.New; secret IAM grant remains single-ARN scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec Existing refs → database component | Operator-supplied endpoint/secret become Magento connection material |
| ECS execution role → Secrets Manager | Privilege boundary for adopted secret access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Elevation of Privilege | databaseExecutionPolicy | high | mitigate | Policy JSON lists only the adopted secret ARN (+ existing KMS key pattern); no wildcards |
| T-08-08 | Tampering | database.Existing | high | mitigate | Never create rds.Instance/Cluster when Existing set; mock asserts zero creates |
| T-08-09 | Denial of Service | missing endpoint | medium | mitigate | Validate endpoint+secretArn before register |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new registry packages |
</threat_model>

<verification>
database Existing mock tests + stack wiring tests pass under GOMAXPROCS=1.
</verification>

<success_criteria>
ATTACH-02 AWS RDS adopt applies without creating RDS; outputs and IAM use operator-supplied secret/endpoint (D-01).
</success_criteria>

<output>
Create `.planning/phases/08-brownfield-attach-tag-day/08-03-SUMMARY.md` when done
</output>
