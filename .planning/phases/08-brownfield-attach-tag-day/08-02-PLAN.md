---
phase: 08-brownfield-attach-tag-day
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/model.go
  - internal/config/schema.go
  - docs/configuration.md
  - schema/magelift.schema.json
  - internal/cloud/aws/stack/spec.go
  - internal/cloud/aws/stack/config.go
  - internal/cloud/aws/stack/config_test.go
  - internal/cloud/aws/stack/spec_test.go
autonomous: true
requirements:
  - ATTACH-02

must_haves:
  truths:
    - "magelift.yaml can declare target.aws.existing.database with provider/kind/externalId/secretArn/endpoint per D-01 discretion"
    - "PlanFromConfig maps existing.database into Spec.Existing.Database and rejects incomplete refs (missing secretArn or endpoint)"
    - "make generate / generate-check stays clean after model+schema prose updates"
  artifacts:
    - "AWSExistingDatabase (or equivalent) on AWSExistingResources"
    - "Spec.Existing.Database *sdk.ExistingResourceRef plus secret/endpoint fields on ExistingResources"
    - "PlanFromConfig existing-database unit tests"
  key_links:
    - "config.AWSExistingResources.Database → PlanFromConfig → Spec.Existing.Database"
    - "Spec.Validate rejects incomplete database adopt refs"
---

<objective>
Ship the config/schema/Spec surface for adopting an existing AWS RDS MySQL instance (ATTACH-02 surface, D-01). No Pulumi component work yet — that is 08-03.

Purpose: Lock the YAML contract operators will learn before wiring the database Existing branch.
Output: Nested `existing.database` fields, Spec mapping, validation tests, regenerated configuration reference.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brownfield-attach-tag-day/08-CONTEXT.md
@.planning/phases/08-brownfield-attach-tag-day/08-RESEARCH.md
@internal/config/model.go
@internal/config/schema.go
@internal/cloud/aws/stack/config.go
@internal/cloud/aws/stack/spec.go
@sdk/v1/types.go
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Add existing.database config model + schema prose</name>
  <files>internal/config/model.go, internal/config/schema.go, docs/configuration.md, schemas/magelift.schema.json</files>
  <behavior>
    - AWSExistingResources accepts nested database with provider=aws, kind=database, externalId, secretArn, endpoint
    - SchemaJSON / ReferenceMarkdown include existing.database fields after generate
    - Network existing shape unchanged
  </behavior>
  <action>
Per D-01 (discretion): introduce a dedicated struct for adopted DB (prefer AWSExistingDatabase with Provider/Kind/ExternalID/SecretARN/Endpoint) under AWSExistingResources.Database — do not overload AWSExistingResource with DB-only fields. Update schema.go prose next to the existing.network paragraph to document required secretArn + endpoint and that MageLift does not create RDS when set. Run `GOMAXPROCS=1 GOFLAGS=-p=1 go generate ./internal/config` (or `make generate`) so docs/configuration.md and schemas/magelift.schema.json regenerate. Cloud SQL is not a config key this milestone.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go generate ./internal/config && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/config/ -count=1 && rg -n 'existing\.database|SecretARN|secretArn' internal/config/model.go docs/configuration.md | grep -v '^#' | grep -c database | awk '{if($1+0==0){print "database field missing"; exit 1} print "database config present"}'</automated>
  </verify>
  <done>Config model + generated configuration reference document existing.database with secretArn and endpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Map PlanFromConfig + Spec validation for existing database</name>
  <files>internal/cloud/aws/stack/spec.go, internal/cloud/aws/stack/config.go, internal/cloud/aws/stack/config_test.go, internal/cloud/aws/stack/spec_test.go</files>
  <behavior>
    - PlanFromConfig maps complete existing.database into Spec.Existing.Database (kind ExistingDatabase) plus secret/endpoint on ExistingResources
    - Incomplete database (missing secretArn or endpoint) fails PlanFromConfig/Validate with a clear error
    - Wrong kind/provider rejected
    - Existing network-only configs still plan successfully
  </behavior>
  <action>
Per D-01 / ATTACH-02: extend ExistingResources with Database *sdk.ExistingResourceRef and SecretARN + Endpoint fields needed by the later component (names at discretion; keep them colocated with Database). Map from config in PlanFromConfig; validate kind=database, provider=aws, non-empty externalId, secretArn ARN shape (reuse secretARN regex if present), and non-empty endpoint hostname. Add TestPlanFromConfigMapsExistingDatabaseInputs and rejection tests mirroring existing-network coverage. Do not wire database.New Existing branch here (08-03). Avoid file overlap with adopt.go from 08-01.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'ExistingDatabase|PlanFromConfigMapsExisting|PlanFromConfigRejectsIncompleteExisting' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/config/ -count=1</automated>
  </verify>
  <done>PlanFromConfig accepts complete existing.database and rejects incomplete refs; Spec validates kind/provider.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator YAML → Spec | External DB ID, secret ARN, endpoint cross into plan |
| Spec → later ECS injection | Secret ARN must stay ARN-shaped (no plaintext password in YAML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | existing.database YAML | high | mitigate | Accept Secrets Manager ARN only; validate ARN regex; never accept inline DB passwords |
| T-08-05 | Tampering | PlanFromConfig | medium | mitigate | Reject incomplete/wrong-kind refs before any Pulumi program runs |
| T-08-06 | Spoofing | externalId typo | medium | mitigate | Strict ID/ARN validation; preview (08-04) will echo externalId |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new registry packages |
</threat_model>

<verification>
Config package tests + stack PlanFromConfig existing-database tests pass; generate-check clean.
</verification>

<success_criteria>
Operators can express ATTACH-02 AWS RDS adopt refs in YAML and have them validated into Spec without creating cloud resources yet (D-01).
</success_criteria>

<output>
Create `.planning/phases/08-brownfield-attach-tag-day/08-02-SUMMARY.md` when done
</output>
