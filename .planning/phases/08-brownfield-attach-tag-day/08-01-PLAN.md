---
phase: 08-brownfield-attach-tag-day
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cloud/aws/stack/adopt.go
  - internal/cloud/aws/stack/adopt_test.go
  - internal/cli/lifecycle.go
  - internal/cli/lifecycle_test.go
  - internal/cloud/aws/network/network_test.go
autonomous: true
requirements:
  - ATTACH-01
  - ATTACH-03

must_haves:
  truths:
    - "preview against existing.network emits an ADOPT line naming the VPC externalId (not silent omission) per D-01 / ATTACH-01 SC1"
    - "Existing network mock graph still creates zero aws:ec2/vpc:Vpc / subnet / NAT children (ATTACH-01)"
    - "Refuse gate fails before mutate when an operation would destroy or replace an adopted network, with a message naming the resource and externalId (D-02 / ATTACH-03)"
  artifacts:
    - "internal/cloud/aws/stack/adopt.go with AdoptReport + RefuseAdoptedMutation for network"
    - "CLI preview/deploy/destroy result surfaces adopted network entries"
    - "adopt_test.go covering report lines and refuse-before-mutate naming"
  key_links:
    - "Spec.Existing.Network → AdoptReport → infrastructureResult / stderr"
    - "RefuseAdoptedMutation → executeInfrastructure Update/Destroy path before backend call"
---

<objective>
Prove the attach honesty path for an existing VPC end-to-end: operator-visible ADOPT reporting, zero managed VPC creates, and test-asserted refuse-before-mutate naming the resource (D-01, D-02, ATTACH-01, ATTACH-03 network slice).

Purpose: Close SC1 and the network half of SC3 before database attach lands.
Output: `stack/adopt.go` helpers, CLI wiring, regression + refuse tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brownfield-attach-tag-day/08-CONTEXT.md
@.planning/phases/08-brownfield-attach-tag-day/08-RESEARCH.md
@internal/cloud/aws/network/network.go
@internal/cloud/aws/network/network_test.go
@internal/cloud/aws/stack/spec.go
@internal/cloud/aws/stack/config.go
@internal/cli/lifecycle.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end VPC ADOPT report + refuse gate</name>
  <files>internal/cloud/aws/stack/adopt.go, internal/cloud/aws/stack/adopt_test.go, internal/cli/lifecycle.go, internal/cli/lifecycle_test.go</files>
  <reversibility rating="costly">ADOPT preview wording and refuse error shape are locked by D-01/D-02 in CONTEXT; changing them later rewrites operator contracts.</reversibility>
  <behavior>
    - AdoptReport(spec) with Existing.Network set returns one entry kind=network including externalId
    - AdoptReport with no Existing.Network returns empty
    - RefuseAdoptedMutation for destroy/replace intent against adopted network returns error containing externalId and ownership phrase
    - Preview CLI result (or stderr) includes ADOPT network line when planned Spec has Existing.Network
  </behavior>
  <action>
Per D-01 / D-02 / ATTACH-01 / ATTACH-03 (network): add `internal/cloud/aws/stack/adopt.go` that builds an adopt report from Spec.Existing.Network (kind + externalId + human label) and a RefuseAdoptedMutation helper that fails before any provider mutate when the caller intent is destroy or replace of an adopted network ID MageLift does not own. Error must name the resource and include the externalId (research wording: adopted resource … MageLift does not own this resource). Wire adopt report into CLI preview/deploy/destroy so operators see ADOPT lines — extend infrastructureResult JSON/YAML and/or write human lines to stderr before Pulumi runs. Call refuse gate on Update and Destroy paths in executeInfrastructure (or equivalent) when Spec has Existing.Network. Do not Pulumi-Import the VPC; keep reference-without-own. Serial only: GOMAXPROCS=1 GOFLAGS=-p=1. Database adopt fields are out of this plan (08-02/03).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'Adopt|Refuse|ExistingNetwork' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ -count=1 -run 'Preview|Adopt|Infrastructure' && rg -n 'AdoptReport|RefuseAdoptedMutation' internal/cloud/aws/stack/adopt.go internal/cli/lifecycle.go | grep -v '^#' | grep -c AdoptReport | awk '{if($1+0==0){print "AdoptReport missing"; exit 1} print "adopt wired"}'</automated>
  </verify>
  <done>Preview surfaces ADOPT for existing VPC; refuse gate errors name the network externalId before mutate; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Regress Existing network zero-create graph</name>
  <files>internal/cloud/aws/network/network_test.go</files>
  <behavior>
    - TestExistingNetworkUsesImportedSubnetsWithoutCreatingVPCResources still asserts zero Vpc/Subnet/NatGateway creates
    - Optional: assert AdoptReport-compatible externalId format already validated by validateExistingNetwork
  </behavior>
  <action>
Per D-01 / ATTACH-01: keep the existing early-return path in network.New unchanged unless a bug blocks adopt reporting. Strengthen or reaffirm TestExistingNetworkUsesImportedSubnetsWithoutCreatingVPCResources so Phase 8 cannot regress silent VPC creation. Do not add Pulumi Import options. Do not touch database package.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/network/ -count=1 -run 'ExistingNetworkUsesImported' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'Adopt|Refuse'</automated>
  </verify>
  <done>Existing-network mock graph still creates zero managed VPC/subnet/NAT resources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator YAML → PlanFromConfig | Untrusted external VPC ID / subnet IDs enter Spec |
| CLI preview/apply → AWS API | Mutate boundary; refuse must fire before provider calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | adopt.go RefuseAdoptedMutation | high | mitigate | Fail closed naming resource before Update/Destroy when intent would mutate adopted VPC |
| T-08-02 | Elevation of Privilege | network Existing path | medium | mitigate | Never register aws:ec2/vpc:Vpc custom resources for Existing; tests assert zero creates |
| T-08-03 | Information Disclosure | adopt report | low | accept | External IDs are operator-supplied; no secrets in ADOPT lines |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new registry packages this plan |
</threat_model>

<verification>
Package tests for stack adopt + network Existing + CLI adopt wiring pass under serial GOMAXPROCS=1.
</verification>

<success_criteria>
SC1 network path: preview reports ADOPT for existing VPC; apply path cannot replace/destroy it without a named refuse error (D-01, D-02).
</success_criteria>

<output>
Create `.planning/phases/08-brownfield-attach-tag-day/08-01-SUMMARY.md` when done
</output>
