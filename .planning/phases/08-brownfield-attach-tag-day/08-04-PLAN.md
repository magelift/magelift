---
phase: 08-brownfield-attach-tag-day
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-03
files_modified:
  - internal/cloud/aws/stack/adopt.go
  - internal/cloud/aws/stack/adopt_test.go
  - internal/cli/lifecycle.go
  - internal/cli/lifecycle_test.go
autonomous: true
requirements:
  - ATTACH-03

must_haves:
  truths:
    - "preview reports ADOPT lines for both network and database when both Existing refs are set, distinct from CREATE counts (D-01, ATTACH-03 SC2/SC3)"
    - "RefuseAdoptedMutation covers database as well as network — replace/destroy intent fails naming the DB externalId (D-02)"
    - "Offline destroy/detach path with Existing set produces no Delete calls for adopted VPC/RDS IDs (ATTACH-04 offline half)"
  artifacts:
    - "Unified AdoptReport entries for network + database"
    - "Refuse tests AdoptedRefuse / naming both resource kinds"
    - "Detach/destroy mock assertion: no provider Delete for adopted external IDs"
  key_links:
    - "AdoptReport(network+database) → CLI preview result"
    - "RefuseAdoptedMutation → Update/Destroy before backend"
---

<objective>
Unify ATTACH-03 honesty: import-vs-create preview for network+DB and refuse-before-mutate naming every adopted resource (D-01, D-02, ATTACH-03).

Purpose: Close SC2 (no destructive change against adopted DB on preview) and SC3 (test-asserted refuse).
Output: Extended adopt helpers + CLI report + destroy-no-delete mock proof.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brownfield-attach-tag-day/08-CONTEXT.md
@.planning/phases/08-brownfield-attach-tag-day/08-RESEARCH.md
@.planning/phases/08-brownfield-attach-tag-day/08-01-SUMMARY.md
@.planning/phases/08-brownfield-attach-tag-day/08-03-SUMMARY.md
@internal/cloud/aws/stack/adopt.go
@internal/cli/lifecycle.go
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Unified ADOPT preview for network + database</name>
  <files>internal/cloud/aws/stack/adopt.go, internal/cloud/aws/stack/adopt_test.go, internal/cli/lifecycle.go, internal/cli/lifecycle_test.go</files>
  <behavior>
    - AdoptReport includes database entry when Spec.Existing.Database set
    - Preview result lists ADOPT network and ADOPT database lines with externalIds
    - When only one of network/database is set, only that line appears
  </behavior>
  <action>
Per D-01 / ATTACH-03: extend AdoptReport from 08-01 to include Spec.Existing.Database (and secret/endpoint only as needed for operator clarity — do not print secret values, only ARNs/IDs already in config). Ensure CLI preview/deploy surfaces both. Keep CREATE summary from Pulumi ChangeSummary separate so operators can tell adopt vs create. Serial GOMAXPROCS=1.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'Adopt|Refuse|Adopted' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ -count=1 -run 'Adopt|Preview|Infrastructure'</automated>
  </verify>
  <done>Preview shows ADOPT for network and database independently and together.</done>
</task>

<task type="auto" tdd="true">
  <name>Refuse destroy/replace for adopted DB + detach mock</name>
  <files>internal/cloud/aws/stack/adopt.go, internal/cloud/aws/stack/adopt_test.go</files>
  <behavior>
    - RefuseAdoptedMutation on destroy/replace intent for adopted database returns error containing externalId and ownership phrase
    - Same for network still passes (08-01 regression)
    - Detach/destroy simulation: with Existing set, mocked destroy path records no Delete against adopted VPC/RDS external IDs
  </behavior>
  <action>
Per D-02 / ATTACH-03 / ATTACH-04 offline: expand RefuseAdoptedMutation to cover database adopt refs. Add tests named for AdoptedRefuse (or equivalent) that assert the error names the resource. Add an offline detach proof: because adopted resources were never in Pulumi state, a destroy of MageLift-owned children must not emit Delete for adopted external IDs — assert via mock counters or by verifying Refuse blocks any explicit mutate-of-adopted intent. Document in test comments that live describe-after-destroy is 08-06 HUMAN_GATE. Do not add a detach CLI (discretion: docs in 08-05).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'Adopt|Refuse|Adopted|Detach' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/database/ -count=1 -run 'Existing' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/network/ -count=1 -run 'ExistingNetwork'</automated>
  </verify>
  <done>Refuse gate names adopted DB and network; offline destroy leaves adopted IDs without Delete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Update/Destroy → provider | Last chance to refuse mutate of non-owned resources |
| Preview output → operator | Honesty surface for import-vs-create |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Tampering | RefuseAdoptedMutation | high | mitigate | Cover network+database; tests assert named failure before backend |
| T-08-11 | Repudiation | AdoptReport | medium | mitigate | Stable ADOPT lines in structured CLI result for audit |
| T-08-12 | Elevation of Privilege | destroy path | high | mitigate | No Delete of adopted IDs; reference-without-own keeps them out of state |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | No new registry packages |
</threat_model>

<verification>
Stack adopt/refuse/detach tests + network/database Existing regressions pass.
</verification>

<success_criteria>
ATTACH-03 satisfied offline: preview distinguishes ADOPT vs CREATE; refuse-before-mutate is test-asserted for network and DB (D-02).
</success_criteria>

<output>
Create `.planning/phases/08-brownfield-attach-tag-day/08-04-SUMMARY.md` when done
</output>
