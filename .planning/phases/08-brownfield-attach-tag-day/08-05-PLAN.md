---
phase: 08-brownfield-attach-tag-day
plan: 05
type: execute
wave: 4
depends_on:
  - 08-03
  - 08-04
files_modified:
  - docs/brownfield-attach.md
  - docs/configuration.md
  - docs/migrating-from-paas.md
  - docs/adr/0010-database-dump-seed.md
  - docs/adr/README.md
  - docs/index.md
  - docs/post-beta-roadmap.md
  - internal/cloud/aws/stack/adopt_test.go
autonomous: true
requirements:
  - ATTACH-04

must_haves:
  truths:
    - "Documented attach limits state what can be attached (AWS VPC + RDS MySQL), what cannot (Cloud SQL this milestone; multi-cloud), and egress ownership when using existing.network (D-03, ATTACH-04)"
    - "Detach path is documented as manual un-adopt (remove existing refs / destroy MageLift-owned stack) and offline proof shows adopted cloud resources are not deleted (D-03)"
    - "ADR 0010 no longer claims brownfield attach DB is out of scope — superseded by Phase 8 / ATTACH-02"
  artifacts:
    - "docs/brownfield-attach.md runbook"
    - "ADR 0010 supersede note + index cross-links"
    - "migrating-from-paas.md / post-beta-roadmap.md point at Phase 8 attach docs"
  key_links:
    - "configuration.md existing.database fields ↔ brownfield-attach.md limits"
    - "offline detach test ↔ detach section in brownfield-attach.md"
---

<objective>
Document attach limits and the detach path, and supersede ADR 0010’s out-of-scope attach claim (D-03, ATTACH-04).

Purpose: SC4 requires operators to know what attaches, what does not, and how to leave cloud resources intact.
Output: brownfield-attach docs, ADR update, cross-links, offline detach verification retained.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-brownfield-attach-tag-day/08-CONTEXT.md
@.planning/phases/08-brownfield-attach-tag-day/08-RESEARCH.md
@.planning/phases/08-brownfield-attach-tag-day/08-04-SUMMARY.md
@docs/adr/0010-database-dump-seed.md
@docs/migrating-from-paas.md
@docs/configuration.md
@docs/release-readiness.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Write brownfield-attach runbook + ADR 0010 supersede</name>
  <files>docs/brownfield-attach.md, docs/adr/0010-database-dump-seed.md, docs/adr/README.md, docs/index.md, docs/migrating-from-paas.md, docs/post-beta-roadmap.md, docs/configuration.md</files>
  <action>
Per D-03 / ATTACH-04: create docs/brownfield-attach.md covering (1) attachable: AWS existing.network + existing.database (RDS MySQL) with required subnet IDs / secretArn / endpoint; (2) not attachable this milestone: Cloud SQL, non-AWS VPC/DB, importing into Pulumi state for destroy ownership; (3) limits: MageLift does not create NAT/endpoints in existing network mode — operator owns egress; (4) detach: documented manual un-adopt — remove existing.* from a new stack or destroy the MageLift stack while adopted resources were never in state; verify with aws ec2 describe-vpcs / aws rds describe-db-instances after destroy (live steps belong to 08-06). Supersede ADR 0010 consequence that claimed attach existing DB out of scope — state Phase 8 ATTACH-02 delivers AWS RDS adopt; dump-seed decision otherwise unchanged. Update migrating-from-paas.md and post-beta-roadmap.md so they no longer imply attach is only post-beta. Link from docs/index.md and adr README. If configuration.md needs hand-edited attach prose beyond generate, keep it consistent with schema.go. Discretion: no magelift detach CLI.
  </action>
  <verify>
    <automated>test -f docs/brownfield-attach.md && rg -n 'detach|existing\.database|egress|Cloud SQL' docs/brownfield-attach.md | grep -v '^#' | grep -c detach | awk '{if($1+0==0){print "detach section missing"; exit 1} print "detach documented"}' && rg -n 'out of scope|ATTACH-02|Phase 8|supersed' docs/adr/0010-database-dump-seed.md | grep -v '^#' | grep -cE 'ATTACH-02|Phase 8|supersed' | awk '{if($1+0==0){print "ADR not superseded"; exit 1} print "ADR updated"}' && rg -n 'brownfield-attach' docs/index.md docs/migrating-from-paas.md docs/post-beta-roadmap.md | grep -v '^#' | grep -c brownfield-attach | awk '{if($1+0==0){print "cross-links missing"; exit 1} print "cross-links ok"}'</automated>
  </verify>
  <done>Attach limits + detach path documented; ADR 0010 superseded for DB attach; docs cross-linked.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep offline detach proof aligned with docs</name>
  <files>internal/cloud/aws/stack/adopt_test.go, docs/brownfield-attach.md</files>
  <behavior>
    - Existing AdoptedRefuse / detach mock from 08-04 still green
    - Doc section references the offline proof (test name or package path)
  </behavior>
  <action>
Per D-03 / ATTACH-04: ensure docs/brownfield-attach.md cites the offline test(s) that prove destroy does not Delete adopted IDs. If a small test comment or helper rename improves discoverability, do it without changing refuse semantics. Do not claim live AWS describe-after-destroy until 08-06 evidence exists.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/aws/stack/ -count=1 -run 'Adopt|Refuse|Adopted|Detach' && rg -n 'adopt_test|RefuseAdopted|Detach' docs/brownfield-attach.md | grep -v '^#' | grep -cE 'adopt_test|Refuse|Detach' | awk '{if($1+0==0){print "doc missing offline proof cite"; exit 1} print "offline proof cited"}'</automated>
  </verify>
  <done>Docs cite offline detach proof; stack adopt/refuse tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator action | Incorrect detach docs could cause intentional cloud deletes |
| ADR public record | Honesty about attach scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Repudiation | docs/brownfield-attach.md | medium | mitigate | Explicit detach steps + offline proof citation; no claim of live confirm here |
| T-08-14 | Tampering | ADR 0010 | low | mitigate | Supersede attach-out-of-scope bullet with ATTACH-02 reality |
| T-08-SC | Tampering | npm/pip/cargo installs | high | accept | Docs-only; no packages |
</threat_model>

<verification>
Doc presence gates + stack adopt/refuse tests pass.
</verification>

<success_criteria>
ATTACH-04 docs complete with limits + detach; ADR 0010 honest (D-03).
</success_criteria>

<output>
Create `.planning/phases/08-brownfield-attach-tag-day/08-05-SUMMARY.md` when done
</output>
