---
phase: 01-publishable-baseline-honest-fallbacks
plan: 10
type: execute
wave: 5
depends_on: ["01-07", "01-08"]
files_modified:
  - internal/cloud/aws/runtime/runtime.go
  - internal/cloud/aws/runtime/types.go
  - internal/cloud/aws/runtime/identity.go
  - internal/cloud/aws/runtime/component.go
  - internal/cloud/aws/runtime/validate.go
  - internal/cloud/aws/runtime/containers.go
  - internal/cloud/aws/runtime/sidecars.go
  - internal/cloud/aws/runtime/env.go
  - internal/cloud/ovh/stack/ops.go
  - internal/cloud/ovh/stack/ops_test.go
  - internal/cloud/scaleway/stack/ops.go
  - internal/cloud/scaleway/stack/ops_test.go
  - internal/cloud/aws/eksops/ops.go
  - internal/cloud/aws/eksops/ops_test.go
  - internal/platform/ops.go
autonomous: true
gap_closure: true
requirements: [QUALITY-07, TRUST-02]

must_haves:
  truths:
    - "No non-test file under internal/cloud/aws/runtime/ exceeds 400 lines (QUALITY-07, criterion 2)"
    - "runtime.go is gone; AWS runtime wiring is split by concern (task definition / service / sidecar injection plus supporting types/identity/validate/env) inside the same package"
    - "The pre-existing runtime_test.go suite — including 01-07 combination rows and TestRuntimeAddsSigV4ProxyForMagentoOpenSearch — passes after the split with no intentional test edits"
    - "No-op AcquireLock on ovh, scaleway, and eksops warns on stderr that no DIY lock was taken, then returns a working noop release (TRUST-02 / criterion 5 AcquireLock clause)"
    - "The 01-08 AST sentinel guards no longer carve AcquireLock out as a silent-success exception"
    - "Tests prove the lock-absence warning is emitted for each affected adapter"
  artifacts:
    - "Seven non-test files under internal/cloud/aws/runtime/ replacing the 991-line runtime.go (types, identity, component, validate, containers, sidecars, env)"
    - "ovh/scaleway/eksops AcquireLock implementations that warn then return noopRelease"
    - "Updated ops tests (and new eksops ops_test.go) asserting stderr warning; AST carve-out comments and exception branches removed"
  key_links:
    - "Intra-package move only — runtime_test.go keeps compiling against unexported symbols; do not cross a package boundary"
    - "01-07 SUMMARY: recapture verbose baseline from final tree before moving; do not edit SigV4 sentinel tests"
    - "ROADMAP criterion 5 prefers warn that no lock was taken over returning ErrNotSupported (ErrNotSupported would break preview/apply that rely on noop release today)"
    - "CLI root.go newLock → ops.AcquireLock — ops stderr write is enough; no CLI-layer duplicate warn required if stderr flows"
    - "F-01-07-1 search-proxy DependsOn gap stays deferred — do not fix during this split"
---

<objective>
Close the two offline Phase 1 verification gaps that later phases do not absorb: split the 991-line AWS runtime construction path (QUALITY-07 / criterion 2), and make experimental no-op `AcquireLock` honest about taking no DIY lock (TRUST-02 remainder / criterion 5).

Purpose: Verification scored 2/5. Hosted CI green stays DEFERRED (Actions minutes). QUALITY-07 was never planned (`01-10` referenced but missing). AcquireLock still returns `(noopRelease, nil)` with no warning on ovh/scaleway/eksops, and the 01-08 AST guards name that as an intentional carve-out.

Output: Behaviour-preserving intra-package file split under the 400-line non-test cap, and warn-then-noop `AcquireLock` with carve-outs gone and tests proving the warning.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-publishable-baseline-honest-fallbacks/01-VERIFICATION.md
@.planning/phases/01-publishable-baseline-honest-fallbacks/01-RESEARCH.md
@.planning/phases/01-publishable-baseline-honest-fallbacks/01-07-SUMMARY.md
@.planning/phases/01-publishable-baseline-honest-fallbacks/01-08-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@internal/cloud/aws/runtime/runtime.go
@internal/cloud/aws/runtime/runtime_test.go
@internal/cloud/ovh/stack/ops.go
@internal/cloud/ovh/stack/ops_test.go
@internal/cloud/scaleway/stack/ops.go
@internal/cloud/scaleway/stack/ops_test.go
@internal/cloud/aws/eksops/ops.go
@internal/platform/ops.go
@internal/cli/root.go
</context>

## Precondition (Task A — already done)

`gofmt -l` on `internal/cli/lifecycle_test.go` and `internal/cloud/aws/stack/component_test.go` is clean (commit `5666051` — style: gofmt drifted Phase 1 test files). **Do not re-gofmt or re-touch those files** unless drift returns. Skip any gofmt task for them.

Hosted CI / QUALITY-06 outcome / A1 lint parity remain **DEFERRED** — out of scope for this plan.

<tasks>

<task type="auto">
  <name>Task 1: Split aws/runtime by concern (QUALITY-07)</name>
  <files>internal/cloud/aws/runtime/runtime.go, internal/cloud/aws/runtime/types.go, internal/cloud/aws/runtime/identity.go, internal/cloud/aws/runtime/component.go, internal/cloud/aws/runtime/validate.go, internal/cloud/aws/runtime/containers.go, internal/cloud/aws/runtime/sidecars.go, internal/cloud/aws/runtime/env.go</files>
  <behavior>
    - After the split, every non-test `*.go` file under `internal/cloud/aws/runtime/` has ≤400 lines (`wc -l`).
    - `runtime.go` no longer exists; the Pulumi `New` constructor lives in `component.go` (repo convention).
    - Concerns map to REQUIREMENTS QUALITY-07 wording: task-definition / container graph → `containers.go`; ECS service + component construction → `component.go`; sidecar injection (search proxy, Varnish) → `sidecars.go`; supporting pieces in `types.go`, `identity.go`, `validate.go`, `env.go` per 01-RESEARCH §Q7.
    - `GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/aws/runtime/ -count=1` passes, including 01-07 combination coverage and `TestRuntimeAddsSigV4ProxyForMagentoOpenSearch`.
    - No intentional edits to `runtime_test.go` (gofmt-only if the tool forces alignment — prefer zero diff).
    - Finding F-01-07-1 is not fixed in this plan.
  </behavior>
  <action>
Behaviour-preserving intra-package split only. Do not rename symbols, change visibility, reorder parameters, "tidy" logic, or fix F-01-07-1.

1. Record baseline (serial Go only):
   `GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/aws/runtime/ -count=1 -v > .planning/phases/01-publishable-baseline-honest-fallbacks/scratch/01-10-runtime-baseline.txt`
   (create `scratch/` if needed; do not commit the scratch file unless useful for the SUMMARY).

2. Move declarations into the seven files from 01-RESEARCH §Q7 (line ranges were for the 991-line monolith — use current symbol boundaries, not stale line numbers):

   | File | Contents |
   |---|---|
   | `types.go` | package doc, consts, regexes, SecretReference, CapabilityConfig, Identity*/Args/Component types |
   | `identity.go` | NewIdentity, createIdentity, provisionIdentity, role, executionPolicy, attachExecutionLogPolicy, executionLogPolicy |
   | `component.go` | New, targetGroupInput, tags |
   | `validate.go` | validate, validateSecretReferences, sameSecretReferences, hasSecretReference |
   | `containers.go` | container struct types + containerDefinitions* / FrontendPort / baseContainer / nginxHealthCheck / awslogsConfig / searchEndpointInput |
   | `sidecars.go` | appendSearchProxy, appendVarnish, searchProxyTarget |
   | `env.go` | appendDatabaseSecret, appendEncryptionSecret, deploymentCommand, capabilityEnvironment, containerEnvFromBindings, amqpSettings |

3. Prefer gopls Move / Extract (or Serena symbolic moves) over cut-paste. Prefer one logical file per commit during execution so a bad move is bisectable.

4. After each move batch: `gofmt -l` on touched files; `GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/aws/runtime/ -count=1`. Final gate: no non-test file &gt;400 lines; `runtime.go` deleted; verbose PASS list matches baseline for test names (import-block-only net delta is expected).

5. Do not import cloud packages into `internal/cli`. Do not parallelize builds.
  </action>
  <verify>
    <automated>! test -f internal/cloud/aws/runtime/runtime.go &amp;&amp; for f in internal/cloud/aws/runtime/*.go; do case "$f" in *_test.go) continue;; esac; lines=$(wc -l &lt;"$f" | awk '{print $1}'); test "$lines" -le 400 || { echo "$f has $lines lines"; exit 1; }; done &amp;&amp; GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/aws/runtime/ -count=1 -run 'TestRuntimeAddsSigV4ProxyForMagentoOpenSearch|TestRuntime' -v</automated>
  </verify>
  <done>Package has no runtime.go; every non-test file ≤400 lines; full runtime test suite including SigV4 and 01-07 combination rows passes under serial Go. QUALITY-07 is ready to mark Complete in REQUIREMENTS.md only after this task lands (executor updates REQUIREMENTS — not this planning commit).</done>
  <reversibility rating="reversible">Pure file split inside one package; revert by reconstituting runtime.go from the seven files.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warn on no-op AcquireLock (TRUST-02 remainder)</name>
  <files>internal/cloud/ovh/stack/ops.go, internal/cloud/ovh/stack/ops_test.go, internal/cloud/scaleway/stack/ops.go, internal/cloud/scaleway/stack/ops_test.go, internal/cloud/aws/eksops/ops.go, internal/cloud/aws/eksops/ops_test.go, internal/platform/ops.go</files>
  <behavior>
    - `Ops.AcquireLock` on ovh stack, scaleway stack, and aws eksops writes a clear stderr warning that no DIY deployment lock was taken, then returns `(noopRelease, nil)` — not silent success, and not `ErrNotSupported` (prefer warn+noop so preview/apply keep working; matches ROADMAP "warn that no lock was taken").
    - Warning text is stable enough to assert (name "lock" / "DIY" / "not taken" or equivalent; include provider/runtime or target context if cheap from PlannedStack).
    - 01-08 AST sentinel guards no longer list AcquireLock as a named silent-success exception; comments pointing at "plan 01-10 revisits" are gone.
    - Because warn+noop still returns a nil error, the ErrNotSupported AST walk must not require AcquireLock to return the sentinel — replace the carve-out with a positive requirement: either (a) drop Ops from that walk and keep only `unsupported` receivers under ErrNotSupported, while a dedicated test asserts AcquireLock warns; or (b) AST-assert AcquireLock calls the shared warn helper / writes to stderr. Do not leave a comment that silent success is OK.
    - Tests capture stderr (or a package-level warn sink defaulting to os.Stderr) and fail if the warning is absent.
    - `internal/cli/root.go` `newLock` → `ops.AcquireLock` needs no change if the adapter writes stderr; optional CLI test only if wiring is unclear.
  </behavior>
  <action>
Close criterion 5's AcquireLock clause — the last silent-success path left after 01-08/01-09.

1. Update `platform.Ops` doc comment on `AcquireLock`: experimental targets may still return a no-op release when they have no DIY lock, but they **must warn** (stderr) that no lock was taken. Do not change the interface signature.

2. In `internal/cloud/ovh/stack/ops.go`, `internal/cloud/scaleway/stack/ops.go`, and `internal/cloud/aws/eksops/ops.go`: before returning the noop release, emit the warning. Prefer a tiny shared pattern (package-level `warnNoDIYLock` writing to `os.Stderr`, or a testable `io.Writer` defaulting to stderr) so tests can redirect without changing the Ops interface. Same message shape across the three adapters.

3. Drop the AcquireLock carve-out from ovh and scaleway `ops_test.go` AST guards (`acquireLockException`, `acquireLockReason`, `t.Logf("exception …")`, and comments that plan 01-10 will revisit). Replace with the positive warning test + narrowed sentinel guard as above.

4. Add `internal/cloud/aws/eksops/ops_test.go` (or extend an existing test file) covering the same warning behaviour for the experimental AWS Kubernetes path.

5. Do not return `ErrNotSupported` from AcquireLock in this plan unless warn+noop proves impossible — ROADMAP and 01-RESEARCH recommend warn so OVH/Scaleway preview/apply keep working until Phase 6 real locks.

6. Do not mark QUALITY-07 or re-touch REQUIREMENTS TRUST-02 checkbox in the planning commit; after execution, executor may leave TRUST-02 Complete (now fully true) and check QUALITY-07.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ ./internal/cloud/aws/eksops/ -count=1 -v</automated>
  </verify>
  <done>All three no-op AcquireLock sites warn on stderr; AST carve-outs removed; tests fail if the warning disappears; criterion 5 AcquireLock clause and TRUST-02 remainder are closed offline.</done>
  <reversibility rating="reversible">Warning + test changes; behaviour for callers still returns a release function — only honesty of the call changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → cloud mutation | AcquireLock sits on the path before Pulumi mutate; a silent no-op implies a DIY lock that does not exist |
| Test suite → runtime construction | Split must not change container/sidecar/IAM wiring that production ECS depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-38 | Repudiation | silent no-op AcquireLock | critical | mitigate | Task 2 warns on stderr that no DIY lock was taken; tests assert the warning; AST carve-out removed |
| T-01-39 | Tampering | runtime.go split drift | high | mitigate | Task 1: baseline verbose test capture; no symbol renames; SigV4 + 01-07 suite must pass unchanged |
| T-01-40 | Elevation of Privilege | "fix" F-01-07-1 during split | medium | accept (deferred) | Explicitly out of scope; fixing DependsOn mid-split would poison the behaviour-preservation claim |
| T-01-41 | Information Disclosure | lock warning text | low | mitigate | Warning names absence of DIY lock / target identity only — no ARNs, credentials, or backend URLs |
| T-01-SC | Tampering | package-manager installs | high | mitigate | No installs; serial Go only (`GOMAXPROCS=1 GOFLAGS=-p=1`) |
</threat_model>

<verification>
1. `! test -f internal/cloud/aws/runtime/runtime.go` and every non-test file in that package ≤400 lines.
2. `GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/aws/runtime/ -count=1` passes (SigV4 + combination rows).
3. `GOMAXPROCS=1 GOFLAGS=-p=1 go test -race ./internal/cloud/ovh/stack/ ./internal/cloud/scaleway/stack/ ./internal/cloud/aws/eksops/ -count=1` passes with AcquireLock warning assertions.
4. `rg -n 'plan 01-10 revisits|acquireLockException' internal/cloud/ovh/stack/ops_test.go internal/cloud/scaleway/stack/ops_test.go` returns no matches.
5. Hosted CI / force-all / A1 remain deferred — not required to close this plan.
</verification>

<success_criteria>
- QUALITY-07 and ROADMAP criterion 2 are achievable by execution of Task 1.
- TRUST-02 AcquireLock remainder and ROADMAP criterion 5's third clause are achievable by execution of Task 2.
- Criterion 1 (hosted CI green) stays deferred; F-01-07-1 stays deferred.
</success_criteria>

<output>
Create `.planning/phases/01-publishable-baseline-honest-fallbacks/01-10-SUMMARY.md` when done. Record: final non-test file list + line counts; AcquireLock warning text verbatim; how the AST guard was narrowed; confirm F-01-07-1 untouched. After execution, check QUALITY-07 in REQUIREMENTS.md; leave TRUST-02 Complete (now accurate).
</output>
