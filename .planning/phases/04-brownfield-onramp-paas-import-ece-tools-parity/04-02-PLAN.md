---
phase: 04-brownfield-onramp-paas-import-ece-tools-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/cli/root.go
  - internal/cli/init_import_test.go
  - docs/cli-reference.md
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02

must_haves:
  truths:
    - "init exposes mutually exclusive --from-acc and --from-upsun flags; both set or neither with a conflicting mode yields exit 2 (D-03)"
    - "Existing magelift.yaml (or --config path) refuses overwrite with exit 2 unless persistent --yes is set (D-04)"
    - "Optional --config-out PATH writes a side-file for review without clobbering the default config path when the default is left untouched (D-04 intent; flag name confirmed at checkpoint)"
  artifacts:
    - "init local flags --from-acc, --from-upsun, --config-out on initCommand"
    - "CLI tests covering mutual exclusion, refuse-without-yes, overwrite-with-yes, and --config-out side write"
    - "Generated CLI help / docs/cli-reference.md reflecting the locked flag names"
  key_links:
    - "init RunE → mutual exclusion check → exists&&!yes → exit 2 → paasimport → write configPath or config-out"
    - "Persistent --yes (not a new overwrite flag) gates destructive rewrite of an existing config path"
---

<objective>
Lock the published init CLI contract for PaaS import: D-03 flag pair, D-04 refuse/`--yes` overwrite, and side-file write via `--config-out` (avoiding root `--output`/`-o` format collision).

Purpose: Operator muscle memory and CI scripts learn these flags once (one-way / costly doors).
Output: Complete init import CLI surface wired to paasimport; Upsun flag callable (mapper may still be thin until 04-03).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-CONTEXT.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-RESEARCH.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-01-SUMMARY.md
@internal/cli/root.go
@internal/cli/releases.go
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm published init import flags: mutually exclusive --from-acc and --from-upsun (D-03), not a unified --from enum and not a new import verb</decision>
  <context>
D-03 is one-way: REQUIREMENTS, docs/migrating-from-paas.md, and CLI help become the contract operators learn. Research also verified promote already owns --from for environment selection — unifying under --from would collide. CONTEXT already locked D-03; this gate records explicit execute-time confirmation before the contract ships in help text.
  </context>
  <options>
    <option id="d03-literal">
      <name>Ship --from-acc and --from-upsun on init (locked D-03)</name>
      <pros>Matches IMPORT-01/02 wording; avoids promote --from collision; no new top-level verb</pros>
      <cons>Two flags instead of one enum; mutual-exclusion logic required</cons>
    </option>
    <option id="reject-reopen">
      <name>Reopen discuss (only if D-03 must change)</name>
      <pros>Allows alternate UX</pros>
      <cons>Breaks locked CONTEXT and REQUIREMENTS literals; delays phase</cons>
    </option>
  </options>
  <resume-signal>Select: d03-literal (expected) or reject-reopen</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm side-file flag name --config-out PATH for D-04 review-without-clobber intent</decision>
  <context>
CONTEXT D-04 named optional --output PATH, but root already defines persistent --output/-o as table|json|yaml (research Pitfall 1). Local --output on init would collide or shadow format. Research recommends --config-out PATH preserving D-04 side-file semantics. This is costly for published help once gendocs ships.
  </context>
  <options>
    <option id="config-out">
      <name>Use init-local --config-out PATH (research recommendation)</name>
      <pros>No collision with global -o; clear operator meaning; D-04 intent preserved</pros>
      <cons>Wording differs from CONTEXT literal --output</cons>
    </option>
    <option id="write-flag">
      <name>Use init-local --write PATH instead</name>
      <pros>Also avoids -o collision</pros>
      <cons>Less explicit; still diverges from CONTEXT --output wording</cons>
    </option>
  </options>
  <resume-signal>Select: config-out (expected) or write-flag</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Init CLI: Upsun flag, mutual exclusion, refuse/--yes, --config-out</name>
  <reversibility rating="one-way">D-03 flag names and help text become the published operator contract.</reversibility>
  <files>internal/cli/root.go, internal/cli/init_import_test.go, docs/cli-reference.md</files>
  <behavior>
    - --from-acc and --from-upsun together → exit 2 / invalid
    - Target config exists and --yes unset → exit 2 with already-exists message
    - Target exists and --yes set → overwrite succeeds for import write
    - --config-out PATH writes only that path when used for review side-file; default magelift.yaml left unchanged when it did not exist as the write target
    - --from-upsun is accepted as a flag (calls paasimport Upsun entry; stub OK if 04-03 completes mapping)
    - Bare init without --from-* keeps prior starter behavior; define --yes on bare init consistently (document: --yes enables overwrite of starter path when rewriting starterConfig)
  </behavior>
  <action>
Per D-03 and D-04 (and checkpoint outcomes): finish initCommand local flags `--from-upsun` and `--config-out` (or checkpoint-chosen alias). Enforce mutual exclusion of the two --from-* flags. Reuse persistent `--yes` for overwrite of an existing write target — do not add a separate overwrite flag name. When `--config-out` is set, write the generated YAML there for review; refuse/overwrite rules apply to the chosen write path. Wire `--from-upsun` to `paasimport` Upsun entrypoint (thin adapter stub returning error or minimal map until 04-03 is fine if CLI tests cover flag plumbing). Refresh `docs/cli-reference.md` via existing gendocs path if that is how the repo updates CLI docs. Serial Go tests only (AGENTS.md). Do not implement unmapped sidecar here (04-03 / D-05).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ -count=1 -run 'Init|FromAcc|FromUpsun|ConfigOut|Yes|Mutual'</automated>
  </verify>
  <done>CLI contract tests green for exclusion, refuse/--yes, and --config-out; help/docs list --from-acc/--from-upsun.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator flags → filesystem write | Overwrite of existing magelift.yaml is destructive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | init overwrite | medium | mitigate | Refuse exists unless persistent --yes (D-04) |
| T-04-05 | Elevation of privilege | --config-out path | medium | mitigate | Clean + write only under operator-chosen path; no follow of unexpected symlinks beyond os.WriteFile norms |
| T-04-SC | Tampering | package installs | low | accept | No new modules |
</threat_model>

<verification>
Focused serial CLI tests cover D-03/D-04 surfaces; docs list locked flag names.
</verification>

<success_criteria>
Published init import flags and overwrite semantics match locked decisions; --config-out avoids -o collision.
</success_criteria>

<output>
Create `.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-02-SUMMARY.md` when done
</output>
