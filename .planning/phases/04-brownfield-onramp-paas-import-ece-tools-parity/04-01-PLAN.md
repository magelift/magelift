---
phase: 04-brownfield-onramp-paas-import-ece-tools-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/fixtures/acc/supported/.magento.app.yaml
  - testdata/fixtures/acc/supported/.magento/services.yaml
  - testdata/fixtures/acc/supported/.magento/routes.yaml
  - testdata/fixtures/acc/supported/.magento.env.yaml
  - internal/paasimport/doc.go
  - internal/paasimport/source.go
  - internal/paasimport/map.go
  - internal/paasimport/acc.go
  - internal/paasimport/map_test.go
  - internal/cli/root.go
  - internal/cli/init_import_test.go
  - internal/cli/foreign_config_test.go
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-04
  - IMPORT-05
  - IMPORT-06

must_haves:
  truths:
    - "Operator can run magelift init --from-acc against the supported ACC fixture and get a magelift.yaml that config.Load accepts with zero hand edits (D-01, D-03, IMPORT-01, IMPORT-04)"
    - "Passing a PaaS app YAML path as --config to deploy/validate paths fails loudly — foreign schema is never treated as MageLift input (IMPORT-05)"
    - "CI fixtures under testdata/fixtures/acc/ are synthetic config-only trees; no sibling ece-tools or ACC product source is copied into the repo (D-01, D-02, IMPORT-06)"
  artifacts:
    - "testdata/fixtures/acc/supported/ with .magento* config files sufficient for a fully mapped happy path"
    - "internal/paasimport package with ACC Map entrypoint returning MageLift YAML bytes"
    - "init --from-acc wired on existing initCommand writing the default config path"
    - "Automated tests proving Load/validate on generated YAML and rejection of foreign schema as --config"
  key_links:
    - "init --from-acc → paasimport.MapACC(fixture root) → WriteFile(configPath) → config.Load"
    - "CLI/config path that receives --config=.magento.app.yaml → error (not deploy proceed)"
---

<objective>
Prove the PaaS-import architecture end-to-end: one supported ACC fixture through `magelift init --from-acc` to a Load-valid `magelift.yaml`, plus foreign-schema rejection (D-01, D-03, IMPORT-01/04/05/06).

Purpose: Catch CLI↔mapper↔schema dead-ends before Upsun, unmapped sidecars, and overwrite semantics expand the surface.
Output: Synthetic ACC fixture, `internal/paasimport` skeleton with real ACC happy-path map, init flag wiring, and offline tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-CONTEXT.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-RESEARCH.md
@internal/cli/root.go
@internal/config/config.go
@docs/provenance.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end ACC init --from-acc → Load-valid magelift.yaml</name>
  <files>testdata/fixtures/acc/supported/.magento.app.yaml, testdata/fixtures/acc/supported/.magento/services.yaml, testdata/fixtures/acc/supported/.magento/routes.yaml, testdata/fixtures/acc/supported/.magento.env.yaml, internal/paasimport/doc.go, internal/paasimport/source.go, internal/paasimport/map.go, internal/paasimport/acc.go, internal/paasimport/map_test.go, internal/cli/root.go, internal/cli/init_import_test.go</files>
  <action>
Per D-01 and D-03: ship the thinnest production-quality vertical slice for ACC import.

1. Add config-only fixture tree `testdata/fixtures/acc/supported/` with synthetic `.magento.app.yaml`, `.magento/services.yaml`, `.magento/routes.yaml`, and `.magento.env.yaml` (or equivalent env file) covering app name, one DB/Redis-style relationship, one route/domain intent, and one D-07 allowlist env (e.g. crypt or SCD_STRATEGY). No Magento application source; no vendored sibling trees (IMPORT-06 / D-02).
2. Create `internal/paasimport` (pure library, no cobra): detect ACC config root, parse YAML via existing `go.yaml.in/yaml/v4`, map structural fields into a MageLift document with `schemaVersion: 1` and fields that already exist in `starterConfig` / schema (project.name, application/target/capability seams, `build.staticContent` locales/themes if present). For this tracer, the fixture MUST be fully mappable (zero unmapped keys) so exit 0 is possible without the sidecar contract yet (sidecar lands in 04-03 / D-05).
3. Extend existing `initCommand` in `internal/cli/root.go` with local bool flag `--from-acc`. When set: resolve cwd (or tested root) as ACC fixture root, call paasimport, write bytes to `o.configPath` (default magelift.yaml). Keep refuse-if-exists (exit 2) behavior for this plan — overwrite/`--yes` is 04-02. Do not add `magelift import`, do not add `--from` enum (D-03). Do not touch Upsun yet.
4. Add `internal/cli/init_import_test.go` that runs init with `--from-acc` against the supported fixture in a temp cwd, asserts the written file `config.Load`s and is acceptable to the same validation path `magelift config validate` uses. Serial Go only: `GOMAXPROCS=1 GOFLAGS=-p=1` (AGENTS.md).

Do not implement unmapped sidecar, `--from-upsun`, `--config-out`, or dump/media (Phase 5). Do not invent APIs beyond CONTEXT.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/paasimport/ ./internal/cli/ -count=1 -run 'Acc|FromAcc|MapACC'</automated>
  </verify>
  <done>Supported ACC fixture imports via init --from-acc to a Load-valid magelift.yaml; paasimport package exists; tests green under serial GOMAXPROCS=1.</done>
</task>

<task type="auto" tdd="true">
  <name>Reject foreign PaaS YAML as --config deploy input</name>
  <files>internal/cli/foreign_config_test.go, internal/cli/root.go, internal/config/config.go</files>
  <behavior>
    - Loading or validating with --config pointing at .magento.app.yaml (or fixture copy) fails with a clear error
    - Loading or validating with --config pointing at .platform.app.yaml fails similarly
    - A normal magelift.yaml from starter/import still Loads successfully
  </behavior>
  <action>
Per IMPORT-05 and ROADMAP SC#3: ensure deploy/validate paths never accept ACC/Upsun app YAML as MageLift config. Prefer detecting missing `schemaVersion: 1` / KnownFields failure already produced by `config.Load`, and add an explicit CLI or Load guard if the error is too opaque. Wire a focused test that uses fixture bytes (or temp copies of foreign filenames) and asserts non-zero / error — not silent proceed. Do not change importer output to write foreign schemas. Serial tests only (AGENTS.md).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ ./internal/config/ -count=1 -run 'Foreign|MagentoApp|PlatformApp|schemaVersion'</automated>
  </verify>
  <done>Foreign .magento.app.yaml / .platform.app.yaml rejected as --config; MageLift YAML still Loads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator cwd fixture YAML → paasimport | Untrusted PaaS YAML parsed offline |
| Generated magelift.yaml → config.Load / deploy | Only MageLift schema may cross into deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | paasimport path inputs | medium | mitigate | Clean filepath; confine reads to config-root files named by source detector; reject `..` escapes in soak paths later (04-03) |
| T-04-02 | Information Disclosure | mapped crypt env | high | mitigate | Tracer maps crypt to secret-ref placeholder fields only — never plaintext key material in YAML (D-07) |
| T-04-03 | Tampering | --config foreign schema | medium | mitigate | Load/CLI reject non-MageLift documents before deploy (IMPORT-05) |
| T-04-SC | Tampering | package installs | low | accept | No new Go modules in this plan |
</threat_model>

<verification>
Serial `go test` on paasimport + cli packages green; supported ACC fixture imports without hand edits; foreign schema rejected.
</verification>

<success_criteria>
Tracer happy path works end-to-end for ACC; IMPORT-05 fail-loud proven; fixtures are synthetic-only.
</success_criteria>

<output>
Create `.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-01-SUMMARY.md` when done
</output>
