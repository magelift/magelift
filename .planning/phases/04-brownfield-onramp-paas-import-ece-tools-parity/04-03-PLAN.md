---
phase: 04-brownfield-onramp-paas-import-ece-tools-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-04
files_modified:
  - testdata/fixtures/acc/unmapped/
  - testdata/fixtures/upsun/supported/
  - testdata/fixtures/upsun/unmapped/
  - internal/paasimport/map.go
  - internal/paasimport/unmapped.go
  - internal/paasimport/acc.go
  - internal/paasimport/upsun.go
  - internal/paasimport/allowlist.go
  - internal/paasimport/map_test.go
  - internal/paasimport/soak_test.go
  - internal/cli/root.go
  - internal/cli/init_import_test.go
  - internal/config/model.go
  - schema/magelift.schema.json
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-04
  - ECE-04

must_haves:
  truths:
    - "Supported ACC and Upsun fixtures each produce magelift.yaml that passes config.Load and schema validation with zero hand edits (IMPORT-01/02/04)"
    - "Any unmapped key writes the mapped YAML plus adjacent magelift.unmapped.md (or stem.unmapped.md for --config-out) and exits non-zero — never success with warnings only (D-05, IMPORT-03)"
    - "Structural app/services/routes/cron plus D-07 allowlist env vars map into magelift.yaml; everything else appears in the unmapped report or ECE-04 intentional-gap docs later (D-07)"
    - "MAGELIFT_IMPORT_FIXTURE_ACC / MAGELIFT_IMPORT_FIXTURE_UPSUN soak tests skip cleanly when unset (D-02)"
  artifacts:
    - "testdata/fixtures/{acc,upsun}/{supported,unmapped}/ config-only trees"
    - "internal/paasimport unmapped report renderer + allowlist table"
    - "Minimal application.cron (or equivalent) schema fields for Magento cron:run entries"
    - "CLI integration asserting non-zero + sidecar on unmapped fixture"
  key_links:
    - "Map → Result{YAML, Unmapped[]} → write YAML; if Unmapped non-empty write sidecar + invalid/exit non-zero"
    - "D-07 allowlist keys → existing magelift.yaml fields / secret refs / build.staticContent — never raw MAGENTO_DC_* host dumps"
---

<objective>
Complete shared ACC/Upsun mapping, D-07 allowlist, fail-loud unmapped sidecar (D-05), cron destination schema, validate gate, and optional soak skips (D-02) — IMPORT-01..04 + ECE-04 code surface.

Purpose: Brownfield operators get reviewable YAML and an automation-safe non-zero contract when keys cannot map.
Output: Full paasimport mappers, fixtures, sidecar convention `magelift.unmapped.md`, schema cron list, tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-CONTEXT.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-RESEARCH.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-01-SUMMARY.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-02-SUMMARY.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-04-SUMMARY.md
@schema/magelift.schema.json
@internal/config/model.go
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm D-05 fail-loud unmapped contract: write YAML + durable sidecar + non-zero exit; soft path only via future explicit opt-out, not default success-with-warnings</decision>
  <context>
D-05 is one-way: exit codes and sidecar path become CI/automation contract. CONTEXT locked write-mapped-YAML + sidecar + refuse success. Discretion (planner): adjacent magelift.unmapped.md next to the written YAML (stem.unmapped.md when --config-out uses another basename). Soft --allow-unmapped is explicitly out of default and not required this phase unless needed for tests — do not ship default-lenient behavior.
  </context>
  <options>
    <option id="d05-loud">
      <name>Ship D-05 as locked (YAML + magelift.unmapped.md + non-zero)</name>
      <pros>Honesty-first; CI can assert sidecar; matches TRUST-02 lineage</pros>
      <cons>Partial maps never exit 0 — operators must triage or edit</cons>
    </option>
    <option id="reject-reopen">
      <name>Reopen discuss (only if D-05 must change)</name>
      <pros>Allows soft-default UX</pros>
      <cons>Breaks locked CONTEXT; risk of silent success</cons>
    </option>
  </options>
  <resume-signal>Select: d05-loud (expected) or reject-reopen</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Shared mapper: ACC + Upsun + D-07 allowlist + unmapped sidecar</name>
  <reversibility rating="one-way">D-05 exit code + sidecar path and D-07 allowlist become published automation/docs contracts.</reversibility>
  <files>testdata/fixtures/acc/unmapped/, testdata/fixtures/upsun/supported/, testdata/fixtures/upsun/unmapped/, internal/paasimport/map.go, internal/paasimport/unmapped.go, internal/paasimport/acc.go, internal/paasimport/upsun.go, internal/paasimport/allowlist.go, internal/paasimport/map_test.go, internal/cli/root.go, internal/cli/init_import_test.go</files>
  <behavior>
    - Supported Upsun fixture → Load-valid magelift.yaml, exit 0, no sidecar
    - Unmapped ACC and Upsun fixtures → YAML written, magelift.unmapped.md lists every residual key (app/services/routes/cron/env), process exits non-zero
    - Fully mapped run never writes sidecar
    - Allowlist table covers D-07: crypt→secret ref; routes/UPDATE_URLS→domain/base-URL; DB/Redis/OpenSearch/AMQP relationships→capability seams; SCD_*→build.staticContent
    - Free-form shell hooks/crons that are not bin/magento cron:run appear as unmapped (not silently dropped)
  </behavior>
  <action>
Per D-05, D-07, IMPORT-03, and research Pattern 2: implement shared structural walk for ACC (`.magento*`) and Upsun/Platform (`.platform*` / Upsun equiv). Collect every source key; map known structure + frozen allowlist; residual keys → unmapped report. Discretion: write sidecar adjacent to output as `magelift.unmapped.md`, or `{basename without .yaml}.unmapped.md` when `--config-out` uses another name. Init RunE: always write best-effort YAML; if unmapped non-empty also write sidecar and return invalid/non-zero (exit 2 family). Never exit 0 with stderr-only warnings. Do not dump plaintext crypt keys or raw DB hosts into YAML — secret refs and capability intent only (research Pitfall 8). Expand fixtures per D-01. Wire real Upsun mapper (replace 04-02 stub). Serial Go tests (AGENTS.md). Docs matrix wording for long-tail gaps is 04-06; this plan owns the code allowlist + report.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/paasimport/ ./internal/cli/ -count=1 -run 'Unmapped|Upsun|Allowlist|Acc|Import'</automated>
  </verify>
  <done>ACC/Upsun supported+unmapped fixtures pass assertions; sidecar+non-zero on residuals; allowlist covered by table tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Cron schema destination + soak skip + validate gate</name>
  <reversibility rating="costly">Minimal application.cron schema becomes part of magelift.yaml contract.</reversibility>
  <files>internal/config/model.go, schema/magelift.schema.json, internal/paasimport/map.go, internal/paasimport/soak_test.go, internal/paasimport/map_test.go</files>
  <behavior>
    - Magento cron:run-style entries map into application.cron (or chosen portable list field) and appear in generated YAML that schema-validates
    - Non-Magento free-form cron shells remain unmapped
    - Tests using MAGELIFT_IMPORT_FIXTURE_ACC / MAGELIFT_IMPORT_FIXTURE_UPSUN skip when unset; when set to a config-root, run Map without vendoring that tree into the repo
    - Generated supported YAML passes schema + config.Load (IMPORT-04)
  </behavior>
  <action>
Per research Pitfall 4 / Open Q2 and D-07: add a minimal portable cron schedule list under `application` (planner pick: `application.cron` as list of {schedule, command} or equivalent matching existing model style). Map ACC/Upsun Magento cron entries into it; leave exotic shell crons unmapped. Regenerate or hand-update `schema/magelift.schema.json` consistently with model tags/generators used in-repo. Add soak tests gated on D-02 env vars that skip cleanly when unset; when set, confine to cleaned config-root paths (reject `..`). Assert IMPORT-04 validate gate in mapper/CLI tests. Depends on 04-04 so `build.staticContent.strategy/threads` keys already exist for SCD_* allowlist mapping. Serial tests (AGENTS.md). No Phase 5 dump/media.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/paasimport/ ./internal/config/ -count=1 -run 'Cron|Soak|Validate|Schema|Allowlist'</automated>
  </verify>
  <done>Cron maps into schema-valid field; soak skips when unset; supported imports pass Load/schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PaaS YAML keys → magelift.yaml | Untrusted mapping; residuals must not vanish |
| Soak env path → filesystem | Maintainer-provided path must stay config-root confined |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information Disclosure | crypt / DB mapping | high | mitigate | Secret-ref placeholders only; no plaintext secrets in YAML (D-07) |
| T-04-07 | Tampering | soak fixture paths | medium | mitigate | Clean + reject path escapes; skip if unset (D-02) |
| T-04-08 | Repudiation | unmapped report | medium | mitigate | Durable sidecar on disk listing every residual key (D-05) |
| T-04-SC | Tampering | package installs | low | accept | No new modules |
</threat_model>

<verification>
Serial importer+cli+config tests green for supported/unmapped ACC/Upsun, sidecar contract, cron schema, soak skip.
</verification>

<success_criteria>
IMPORT-01..04 satisfied for fixtures; D-05/D-07 code contracts shipped; ECE-04 allowlist implemented in mapper.
</success_criteria>

<output>
Create `.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-03-SUMMARY.md` when done
</output>
