---
phase: 04-brownfield-onramp-paas-import-ece-tools-parity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/build/plan/plan.go
  - internal/build/plan/plan_test.go
  - internal/build/runner/protocol.go
  - internal/build/runner/codec_test.go
  - build/src/Magento/LifecyclePlan.php
  - build/tests/
  - schema/magelift.schema.json
  - internal/config/model.go
autonomous: true
requirements:
  - ECE-03

must_haves:
  truths:
    - "build.staticContent strategy and threads flow Go plan → runner protocol → PHP LifecyclePlan into setup:static-content:deploy argv as -s and -j alongside locale×theme (D-06, ECE-03)"
    - "Schema/docs-facing keys for locales, themes, strategy, threads are explicit (not silent ignore of strategy/threads)"
    - "make php-test (or focused PHPUnit) asserts -s/-j present for configured strategy/threads"
  artifacts:
    - "StaticContent protocol fields for strategy/threads (or equivalent top-level prepare fields applied per command)"
    - "plan.staticContent reading strategy/threads from magelift.yaml build.staticContent"
    - "LifecyclePlan.php emitting -s / -j when set"
    - "Go + PHP tests covering the vertical slice"
  key_links:
    - "magelift.yaml build.staticContent → plan.staticContent → PrepareRequest.StaticContent → LifecyclePlan::staticContentCommands → bin/magento … -s … -j …"
---

<objective>
Close ECE-03 half-done SCD gap: wire strategy and threads through Go→PHP end-to-end (D-06), keeping locale×theme cartesian product.

Purpose: Stores depending on SCD_STRATEGY / SCD_THREADS get honest build behavior, not locale/theme-only.
Output: Protocol + plan + LifecyclePlan + schema key pin + Go/PHP tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-CONTEXT.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-RESEARCH.md
@internal/build/plan/plan.go
@internal/build/runner/protocol.go
@build/src/Magento/LifecyclePlan.php
@schema/magelift.schema.json
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end SCD strategy/threads Go→PHP argv</name>
  <reversibility rating="costly">Protocol and schema keys for strategy/threads become build-contract honesty commitments (D-06).</reversibility>
  <files>internal/build/plan/plan.go, internal/build/plan/plan_test.go, internal/build/runner/protocol.go, internal/build/runner/codec_test.go, build/src/Magento/LifecyclePlan.php, build/tests/</files>
  <action>
Per D-06 / ECE-03 and research Pattern 3: one vertical slice only.

1. Extend `buildrunner.StaticContent` (or prepare-level fields applied to each SCD command) with optional Strategy string and Threads int. Keep Locale/Theme required as today.
2. Update `plan.staticContent` to read `strategy` (enum quick|standard|compact) and `threads` (positive int) from `build.staticContent` map alongside locales/themes; apply the same strategy/threads to each locale×theme pair. Invalid enum/threads → clear error.
3. Update `LifecyclePlan::staticContentCommands` to append `-s` + strategy and `-j` + threads when set (Adobe public SCD CLI). Preserve `--language` / `--theme` / `--no-interaction`.
4. Add/extend Go plan tests and PHPUnit (under existing `build/tests/`) asserting argv contains strategy/threads. Run PHP tests via `make php-test` or the focused phpunit filter the Makefile already supports. Go tests serial: `GOMAXPROCS=1 GOFLAGS=-p=1` (AGENTS.md).

Do not implement patch apply (04-05). Do not claim full ece-tools clone.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/build/plan/ ./internal/build/runner/ -count=1 -run 'Static|Strategy|Thread' && make php-test</automated>
  </verify>
  <done>strategy/threads appear in prepare protocol and PHP SCD argv tests; locale×theme still works.</done>
</task>

<task type="auto">
  <name>Pin build.staticContent schema keys for strategy/threads</name>
  <files>schema/magelift.schema.json, internal/config/model.go</files>
  <action>
Per research schema opacity note: tighten `build.staticContent` schema to document allowed keys `locales`, `themes`, `strategy`, `threads` (strategy enum quick|standard|compact; threads integer ≥1). Keep backward compatibility for existing configs that only set locales/themes. Align model comments/tags if the repo generates schema from them; otherwise edit schema and model docs consistently. Do not invent unrelated build keys. No new Go module deps.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/config/ ./internal/build/plan/ -count=1 -run 'Static|Schema|Build'</automated>
  </verify>
  <done>Schema documents strategy/threads; existing locale/theme configs still validate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| magelift.yaml build.staticContent → PHP argv | Config crosses into process arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering | strategy/threads values | low | mitigate | Enum + positive int validation in plan.staticContent before protocol encode |
| T-04-SC | Tampering | package installs | low | accept | No new modules |
</threat_model>

<verification>
Serial Go build tests + make php-test cover SCD -s/-j emission.
</verification>

<success_criteria>
ECE-03 satisfied for locales, themes, strategy, threads on the supported path.
</success_criteria>

<output>
Create `.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-04-SUMMARY.md` when done
</output>
