---
phase: 04-brownfield-onramp-paas-import-ece-tools-parity
plan: 05
type: execute
wave: 2
depends_on:
  - 04-04
files_modified:
  - build/src/Magento/PatchApplier.php
  - build/src/Magento/LifecyclePlan.php
  - build/tests/
  - docs/ece-parity.md
autonomous: true
requirements:
  - ECE-02

must_haves:
  truths:
    - "During prepare/build after composer install, MageLift applies m2-hotfixes/*.patch in deterministic (alpha) order via clean-room PatchApplier (D-06, ECE-02)"
    - "PHPUnit covers successful apply and fail-loud on bad patch; no Adobe patch DB or ece-tools/magento-cloud-patches source is vendored (IMPORT-06)"
    - "QUALITY_PATCHES / cloud-required quality patch IDs are not silently claimed as implemented — intentional gap deferred to docs plan if not trivial"
  artifacts:
    - "build/src/Magento/PatchApplier.php (or equivalent) invoking host patch -p1 / documented tool"
    - "LifecyclePlan or build phase hook calling PatchApplier after composer install"
    - "PHPUnit fixtures with tiny synthetic .patch files under build/tests"
  key_links:
    - "composer install → PatchApplier(m2-hotfixes) → subsequent Magento build steps"
---

<objective>
Ship clean-room `m2-hotfixes`-style patch application in the PHP build system (ECE-02 / D-06) without vendoring Adobe patch tooling.

Purpose: Brownfield stores with custom hotfixes get the same apply step operators expect from cloud PaaS, under MageLift provenance rules.
Output: PatchApplier + lifecycle wiring + php-test coverage; QUALITY_PATCHES left as intentional gap for 04-06 matrix unless already trivial.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-CONTEXT.md
@.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-RESEARCH.md
@build/src/Magento/LifecyclePlan.php
@docs/provenance.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end m2-hotfixes apply after composer in PHP build</name>
  <reversibility rating="costly">Patch apply becomes a php-test honesty gate for ECE-02 (D-06).</reversibility>
  <files>build/src/Magento/PatchApplier.php, build/src/Magento/LifecyclePlan.php, build/tests/</files>
  <action>
Per D-06 / ECE-02 and research Open Q3: implement clean-room hotfix apply only.

1. Add `PatchApplier` (name may match existing Magento/build naming) that discovers `m2-hotfixes/*.patch` (or project-relative path used by ACC public docs), sorts alphabetically, and applies each with host `patch -p1` via proc_open (or documented equivalent). Fail loud on non-zero apply. Empty directory = no-op success.
2. Wire into the prepare/build command sequence after composer install and before SCD/deploy steps that assume patched code — extend LifecyclePlan or the existing build orchestrator entry that already emits composer. Prefer a dedicated stage so SCD plan 04-04 file touches stay conflict-free: if LifecyclePlan must change, touch only the composer-adjacent method, not staticContentCommands.
3. PHPUnit: temp dir with a synthetic file + minimal unified diff patch; assert file content changes after apply; assert corrupt patch fails. Do not copy source from sibling `magento-cloud-patches` / `ece-tools` trees (IMPORT-06). QUALITY_PATCHES ID lists / Adobe quality patch DB are intentional gaps (document in 04-06), not silent success claims.

Assumption A1: CI/dev images provide `patch`; if missing, fail with an actionable error naming the required tool.
  </action>
  <verify>
    <automated>make php-test</automated>
  </verify>
  <done>m2-hotfixes apply runs in build sequence; PHPUnit covers success and failure; no vendored Adobe patch packages.</done>
</task>

<task type="auto">
  <name>Record ECE-02 closed vs QUALITY_PATCHES gap stub for docs plan</name>
  <files>docs/ece-parity.md</files>
  <action>
If `docs/ece-parity.md` does not yet exist, create a minimal stub section for patch rows only: m2-hotfixes apply = closed (points at PatchApplier); QUALITY_PATCHES / cloud-required quality patches = intentional gap with reason (no Adobe patch DB vendored). Full matrix with no blank rows is owned by 04-06 — this task only ensures ECE-02 claims are not undocumented when php-test lands. Do not rewrite migrating-from-paas here.
  </action>
  <verify>
    <automated>test -f docs/ece-parity.md && grep -E 'm2-hotfixes|QUALITY_PATCHES' docs/ece-parity.md</automated>
  </verify>
  <done>ece-parity stub names m2-hotfixes closed and QUALITY_PATCHES intentional gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project m2-hotfixes/*.patch → patch(1) | Untrusted diffs mutate application tree during build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | patch content | medium | mitigate | Apply only under project hotfix dir; fail on patch nonzero; do not fetch remote patch DBs |
| T-04-11 | Elevation of privilege | patch path traversal | medium | mitigate | Resolve hotfix dir under project root; reject paths escaping root |
| T-04-SC | Tampering | package installs | low | accept | No new Composer packages required for apply |
</threat_model>

<verification>
make php-test green with patch apply cases; provenance preserved (no vendored sibling patch tooling).
</verification>

<success_criteria>
ECE-02 m2-hotfixes path closed under clean-room policy; quality-patch IDs explicitly not claimed.
</success_criteria>

<output>
Create `.planning/phases/04-brownfield-onramp-paas-import-ece-tools-parity/04-05-SUMMARY.md` when done
</output>
