---
phase: 03-credit-efficient-acceptance-harness-evidence-tiering
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/acceptance/lib-assert-clean-aws.sh
  - scripts/aws-acceptance-local.sh
  - tests/acceptance/assert_clean_stub_test.sh
  - docs/aws-acceptance.md
autonomous: true
requirements:
  - ACCEPT-04

must_haves:
  truths:
    - "Shared assert_clean returns 0 when stubbed aws describe queries report zero leftovers (D-02, ACCEPT-04)"
    - "Shared assert_clean returns non-zero when a stub reports ≥1 leftover resource (D-02, ACCEPT-04)"
    - "AWS acceptance EXIT path sources the shared helper (destroy-on-exit still gated by KEEP) so live runs reuse the same assert_clean (ACCEPT-04)"
  artifacts:
    - "scripts/acceptance/lib-assert-clean-aws.sh extracted from the existing aws-acceptance-local assert_clean body"
    - "tests/acceptance/assert_clean_stub_test.sh covering clean→0 and leftover→non-zero"
    - "scripts/aws-acceptance-local.sh sourcing the shared helper on EXIT"
  key_links:
    - "EXIT trap → destroy (unless KEEP) → lib-assert-clean-aws.sh"
    - "MAGELIFT_ACCEPTANCE_AWS_STUB=1 PATH-isolated stub used only in offline tests"
---

<objective>
Extract shared AWS `assert_clean` and prove both exit outcomes offline with stubs so ACCEPT-04 is not assumed (D-02). Live leftover proof stays for 03-06 HUMAN_GATE (D-01).

Purpose: Teardown honesty is the credit-safety net; dual-outcome must be demonstrated before paid create.
Output: Shared assert_clean lib, EXIT wiring, offline stub tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-CONTEXT.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-RESEARCH.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-01-PLAN.md
@scripts/aws-acceptance-local.sh
@scripts/acceptance/lib-checkpoint.sh
@scripts/acceptance/lib-evidence.sh
</context>

<tasks>

<task type="tracer">
  <name>End-to-end stubbed assert_clean clean outcome via shared lib</name>
  <files>scripts/acceptance/lib-assert-clean-aws.sh, tests/acceptance/assert_clean_stub_test.sh, scripts/aws-acceptance-local.sh</files>
  <action>
Per D-02 / ACCEPT-04 and research Pattern 3: extract the existing `assert_clean` function body from `scripts/aws-acceptance-local.sh` (lines covering VPC/RDS/ElastiCache/ALB/ECS/logs/SG queries) into `scripts/acceptance/lib-assert-clean-aws.sh` without changing query semantics or project-tag filters.

Wire `scripts/aws-acceptance-local.sh` to source the lib and call the shared function from the EXIT cleanup path (preserve KEEP gate: destroy + assert_clean only when created and KEEP is not true).

Add `tests/acceptance/assert_clean_stub_test.sh` that installs a PATH-isolated fake `aws` when `MAGELIFT_ACCEPTANCE_AWS_STUB=1`, returns zero counts for all describe/list queries, sources the lib, and asserts exit 0. Do not point the stub at a real account. Do not run live destroy.
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_AWS_STUB=1 bash tests/acceptance/assert_clean_stub_test.sh --clean</automated>
  </verify>
  <done>Shared lib sourced; stubbed clean path exits 0; aws script no longer embeds a duplicate assert_clean body.</done>
</task>

<task type="auto" tdd="true">
  <name>Leftover stub fails loud + document dual-outcome offline half</name>
  <files>tests/acceptance/assert_clean_stub_test.sh, docs/aws-acceptance.md</files>
  <behavior>
    - Stub reporting length 1 for any watched resource class yields assert_clean non-zero and FAILED message on stderr
    - Clean stub still exits 0
    - Stub mode never calls the real aws binary (PATH isolation)
  </behavior>
  <action>
Extend the stub test with a leftover mode (`--leftover`) that makes at least one describe query report a non-zero count and asserts non-zero exit (ACCEPT-04 offline half / ROADMAP SC#3). Keep stub activation behind `MAGELIFT_ACCEPTANCE_AWS_STUB=1` and PATH isolation so maintainers cannot accidentally stub a real session (research Security Domain).

Document in `docs/aws-acceptance.md` that offline dual-outcome is proven by the stub test, and that the live leftover demonstration requires 03-06 HUMAN_GATE (D-01) after deliberate keep/orphan. Do not spend cloud money. Do not claim live leftover proof closed.
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_AWS_STUB=1 bash tests/acceptance/assert_clean_stub_test.sh --clean &amp;&amp; MAGELIFT_ACCEPTANCE_AWS_STUB=1 bash tests/acceptance/assert_clean_stub_test.sh --leftover; test $? -ne 0</automated>
  </verify>
  <done>Both stub outcomes demonstrated; docs distinguish offline vs live leftover proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stub aws wrapper → real AWS account | Stub must only run under explicit env + PATH isolation in tests |
| EXIT cleanup → account mutate | destroy only when created and KEEP false |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | MAGELIFT_ACCEPTANCE_AWS_STUB | high | mitigate | Stub only when env set; PATH-isolated fake aws in tests; document never export stub in live sessions |
| T-03-05 | Abuse / Denial of service | leftover resources after crash | high | mitigate | EXIT destroy + assert_clean fail-loud; KEEP documented for long-lived proof only |
| T-03-06 | Information disclosure | assert_clean stderr | low | accept | Leftover counts/names only; no credentials printed |
| T-03-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Stub clean→0 and leftover→non-zero; aws script sources shared lib.
</verification>

<success_criteria>
ACCEPT-04 offline dual-outcome holds; live leftover half deferred to 03-06.
</success_criteria>

<output>
Create `.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-02-SUMMARY.md` when done
</output>
