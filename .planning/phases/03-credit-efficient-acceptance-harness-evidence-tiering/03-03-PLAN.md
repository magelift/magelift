---
phase: 03-credit-efficient-acceptance-harness-evidence-tiering
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/capability-matrix.md
  - tests/acceptance/matrix_tier_guard_test.sh
autonomous: true
requirements:
  - TRUST-03
  - TRUST-04
  - ACCEPT-06

must_haves:
  truths:
    - "docs/capability-matrix.md records an evidence tier per AWS ECS catalog cell and never claims a tier above stated evidence (D-03, TRUST-03)"
    - "Aurora CreateDBCluster, amazon-mq × preview, and OpenSearch SigV4 data-plane each carry an explicit unverifiable reason (D-03, TRUST-04)"
    - "A checked-in port-coverage table maps every AWS day-2 port (Bootstrap, State, Secrets, RuntimeObserve TailLogs/CheckRuntime/PrepareExec, Ops) to Pulumi mocks / Floci / unit-fake / paid-only (ACCEPT-06 foundation, ROADMAP SC#5)"
  artifacts:
    - "docs/capability-matrix.md Evidence tier column (or equivalent per-cell tier table) for catalog cells"
    - "Unverifiable section/rows naming Aurora CreateDBCluster, amazon-mq×preview, OpenSearch SigV4 with specific reasons"
    - "Port-coverage table in docs/capability-matrix.md"
    - "tests/acceptance/matrix_tier_guard_test.sh grepping required unverifiable reason anchors"
  key_links:
    - "capability-matrix tier ≤ recorded evidence → harness/Floci/paid claims"
    - "port-coverage mockable rows → plan 03-04 Floci/unit fill"
---

<objective>
Make the capability matrix honest: per-cell evidence tiers, explicit unverifiable reasons (D-03), and a checked-in day-2 port-coverage map that ACCEPT-06 can close against.

Purpose: TRUST-03/04 are the honesty surface; Floci work in 03-04 must not invent coverage claims the matrix does not list.
Output: Updated `docs/capability-matrix.md` + a lightweight grep guard test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-CONTEXT.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-RESEARCH.md
@docs/capability-matrix.md
@internal/platform/account.go
@internal/platform/observe.go
@internal/platform/ops.go
@tests/floci/
</context>

<tasks>

<task type="tracer">
  <name>End-to-end honesty: one catalog cell tier + one unverifiable reason row</name>
  <files>docs/capability-matrix.md, tests/acceptance/matrix_tier_guard_test.sh</files>
  <action>
Per D-03 / TRUST-03 / TRUST-04: expand `docs/capability-matrix.md` so evidence is not a thin glossary only.

1. Add a per-cell **evidence tier** column (or dedicated table keyed by catalog cell) using tiers Pulumi mocks / Floci / real-account acceptance — no cell may claim real-account acceptance without citing harness evidence. Keep existing certified/experimental honesty; tier must not exceed evidence (TRUST-03).
2. For the tracer, ensure at least one free-tier cell (e.g. `queueMode: db`) has an explicit tier, and add a clear **Unverifiable on maintainer accounts** subsection that includes Aurora `CreateDBCluster` with the free-tier API block reason (TRUST-04 / D-03).
3. Add `tests/acceptance/matrix_tier_guard_test.sh` that fails if `docs/capability-matrix.md` lacks the Aurora CreateDBCluster unverifiable anchor text. Use region-scoped checks on the doc file only.

Do not mark Floci gaps closed yet (03-04). Do not spend cloud money. Do not reopen Phase 1 CI (D-04).
  </action>
  <verify>
    <automated>bash tests/acceptance/matrix_tier_guard_test.sh</automated>
  </verify>
  <done>Matrix has tier labeling started; Aurora unverifiable reason present; guard test exits 0.</done>
</task>

<task type="auto">
  <name>Complete TRUST-04 triad + port-coverage table for all AWS day-2 ports</name>
  <files>docs/capability-matrix.md, tests/acceptance/matrix_tier_guard_test.sh</files>
  <action>
Per D-03 / TRUST-04 / ACCEPT-06 / ROADMAP SC#5:

1. Finish unverifiable reasons for all three locked cells: Aurora `CreateDBCluster` (free-tier block), `amazon-mq` × `preview` (2-AZ preview vs CLUSTER_MULTI_AZ 3-AZ incompatibility — cite existing matrix notes), OpenSearch SigV4 data-plane (deferred paid / not free-tier certifiable). Optionally note AOSS OCU honesty from Phase 1 where it clarifies cost unverifiability — do not invent new AWS policy claims.
2. Ensure remaining AWS ECS catalog cells in the matrix carry an evidence tier consistent with current proof (Pulumi mocks vs Floci vs prior real-account notes). Prefer under-claim over over-claim (TRUST-03).
3. Add a **Day-2 port coverage** table mapping at least: Bootstrap, State (lock/snapshot), Secrets, RuntimeObserve.TailLogs, RuntimeObserve.CheckRuntime, RuntimeObserve.PrepareExec (and SelectTask if listed separately), Ops.AcquireLock / NewDeploySteps if exposed as day-2 surfaces, plus media/storage if exercised by Floci today. Each row: port | evidence (Pulumi mocks / Floci / unit-fake / paid-only) | notes. Per research A2/Q3: mark live ECS ExecuteCommand / OIDC bootstrap as paid-only unless Floci already proves them; unit-fake SelectTask/PrepareExec is allowed for mockable rows.
4. Extend `matrix_tier_guard_test.sh` to require presence of all three unverifiable anchors (Aurora CreateDBCluster, amazon-mq preview, OpenSearch SigV4) and a port-coverage heading. Do not put the guard's forbidden over-claim phrases as instructional prose inside actions that the guard would false-positive on.

ACCEPT-06 closure of Floci gaps is plan 03-04 — this plan only publishes the map.
  </action>
  <verify>
    <automated>bash tests/acceptance/matrix_tier_guard_test.sh &amp;&amp; rg -n 'CreateDBCluster' docs/capability-matrix.md &amp;&amp; rg -ni 'amazon-mq' docs/capability-matrix.md &amp;&amp; rg -ni 'SigV4' docs/capability-matrix.md &amp;&amp; rg -ni 'port coverage|Day-2 port' docs/capability-matrix.md</automated>
  </verify>
  <done>TRUST-03/04 doc surface complete; port-coverage table checked in; guard covers the three unverifiable anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator trust | Matrix claims become product honesty; over-claim is a trust failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Spoofing / Repudiation | capability-matrix evidence tiers | high | mitigate | Tier ≤ evidence rule; unverifiable reasons required; grep guard for locked cells |
| T-03-08 | Information disclosure | port-coverage notes | low | accept | No credentials; public architecture only |
| T-03-SC | Tampering | package installs | low | accept | Docs/tests only; no new packages |
</threat_model>

<verification>
matrix_tier_guard_test.sh green; three unverifiable reasons + port-coverage table present.
</verification>

<success_criteria>
TRUST-03 and TRUST-04 hold in docs; ACCEPT-06 has a checked-in map for 03-04 to close.
</success_criteria>

<output>
Create `.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-03-SUMMARY.md` when done
</output>
