---
phase: 03-credit-efficient-acceptance-harness-evidence-tiering
plan: 06
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - docs/aws-acceptance.md
  - docs/capability-matrix.md
  - docs/release-readiness.md
  - .planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md
autonomous: false
requirements:
  - ACCEPT-01
  - ACCEPT-02
  - ACCEPT-03
  - ACCEPT-04

must_haves:
  truths:
    - "After HUMAN_GATE spend approval, one AWS free-tier preview stack iterates ≥3 catalog cells with one create then updates only — no re-create between cells (D-01, ACCEPT-01)"
    - "Killing the run mid-matrix then re-invoking resumes at the first incomplete cell (D-01, ACCEPT-02)"
    - "Harness-written .magelift/matrix-results.md rows include cell, result, duration, provider, account, date for each cell (ACCEPT-03)"
    - "Destroy on exit plus assert_clean demonstrates clean→0 and leftover→non-zero on the real account (D-01, ACCEPT-04)"
  artifacts:
    - ".planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md capturing create-once log proof, resume proof, evidence sample, assert_clean dual outcomes"
    - "docs/capability-matrix.md / aws-acceptance.md / release-readiness citations updated from harness evidence (not hand-invented cell results)"
  key_links:
    - "HUMAN_GATE spend OK → MAGELIFT_AWS_ACCEPTANCE_KEEP long-lived create → cell updates → kill+resume → destroy → assert_clean"
user_setup:
  - service: aws
    why: "Paid pass 1 of 3 — free-tier preview stack create and day-2 cells require real AWS credentials, region, and a signed image digest"
    env_vars:
      - name: AWS_REGION
        source: "Maintainer AWS account / free-tier region (e.g. eu-north-1)"
      - name: MAGELIFT_BIN
        source: "Built magelift binary on PATH or absolute path"
      - name: MAGELIFT_CONFIG
        source: "Acceptance magelift.yaml for preview free-tier cells"
      - name: MAGELIFT_AWS_ACCEPTANCE_DIGEST
        source: "Signed immutable image reference for promote/deploy"
      - name: MAGELIFT_AWS_CERTIFICATE_IDENTITY
        source: "Expected Sigstore certificate identity"
    dashboard_config:
      - task: "Confirm free-tier / credit budget OK for one preview stack (~10m+ create plus cell updates)"
        location: "AWS account billing + maintainer spend approval in chat"
---

<objective>
Prove the harness on one long-lived AWS free-tier preview stack after explicit HUMAN_GATE spend approval (D-01): ≥3 cells, kill+resume, auto evidence, destroy + assert_clean both outcomes.

Purpose: Offline plans 03-01…03-05 bought everything mocks can; this is paid pass 1 of 3.
Output: Scratch proof note + docs citations from harness evidence. No create before gate approval.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-CONTEXT.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-RESEARCH.md
@scripts/aws-acceptance-local.sh
@scripts/acceptance/cells-aws-preview.txt
@docs/aws-acceptance.md
@docs/capability-matrix.md
@docs/release-readiness.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Preflight: offline suite green and paid prerequisites listed</name>
  <files>docs/aws-acceptance.md, .planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md</files>
  <precondition>Plans 03-01…03-05 SUMMARY.md exist and offline acceptance/Floci gates were green in those plans — do not open spend if offline suite is red.</precondition>
  <action>
Per D-01 / D-02: before any AWS create, confirm offline gates still pass (acceptance shell tests + matrix guard + port-coverage gate; floci-test if Docker up). Write `scratch/03-06-paid-proof.md` template listing: required env vars, cell catalog (≥3), KEEP=true create-once procedure, kill+resume steps, leftover assert_clean procedure, and destroy cleanup. Document that evidence stays under gitignored `.magelift/` and the scratch/docs cite harness output (research open Q1).

Do not set AWS credentials in git. Do not run preview/promote/deploy yet. Do not reopen Phase 1 hosted CI (D-04).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md &amp;&amp; MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/checkpoint_resume_test.sh &amp;&amp; bash tests/acceptance/evidence_append_test.sh &amp;&amp; MAGELIFT_ACCEPTANCE_AWS_STUB=1 bash tests/acceptance/assert_clean_stub_test.sh --clean &amp;&amp; bash tests/acceptance/matrix_tier_guard_test.sh</automated>
  </verify>
  <done>Offline preflight green; paid proof scratch template ready; no cloud create yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>HUMAN_GATE: approve paid AWS free-tier stack create</name>
  <what-built>
    Offline harness (resume, evidence, assert_clean stubs), matrix honesty, Floci/mock port coverage, and GCP dry-run shape are complete. Live AWS free-tier preview create is the only remaining spend for Phase 3 (paid pass 1 of 3 per ROADMAP).
  </what-built>
  <how-to-verify>
    1. Confirm AWS account, region, free-tier/credit headroom, MAGELIFT_BIN, MAGELIFT_CONFIG, signed digest + certificate identity env vars are ready (see scratch/03-06-paid-proof.md).
    2. Confirm you approve **one** long-lived preview stack create (KEEP=true) iterating ≥3 free-tier cells from scripts/acceptance/cells-aws-preview.txt — no Aurora apply, no amazon-mq apply, no OpenSearch apply.
    3. Confirm the agent may run `scripts/aws-acceptance-local.sh` / cell driver against real AWS after this approval (destroy at end unless you explicitly keep for leftover assert).
    4. Reply with spend approval wording below. If credits are exhausted or you refuse spend, say so — agent must stop with no create.
  </how-to-verify>
  <resume-signal>Type: approved — AWS free-tier spend OK for Phase 3 harness proof. Or: denied — do not create.</resume-signal>
</task>

<task type="auto">
  <name>Run live harness proof and record dual assert_clean + docs citations</name>
  <files>docs/aws-acceptance.md, docs/capability-matrix.md, docs/release-readiness.md, .planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md</files>
  <precondition>Human resume signal explicitly approves AWS free-tier spend — if denied or missing, halt with no create (D-01).</precondition>
  <action>
Per D-01 and ROADMAP SC#1–3 / ACCEPT-01..04:

1. With KEEP=true (or equivalent long-lived flag), create **one** preview stack and iterate ≥3 catalog cells via updates only. Capture log proof of one create-once and subsequent cell-update lines (ACCEPT-01). Prefer dynamic cluster/service lookup from magelift outputs each cell (research Pitfall 3).
2. Kill the run mid-matrix (after ≥1 cell recorded), re-invoke, confirm resume skips completed cells (ACCEPT-02). Mind Pulumi DIY lock hygiene (Pitfall 4).
3. Confirm `.magelift/matrix-results.md` rows were appended by the harness with the six columns (ACCEPT-03). Copy a redacted sample into scratch/03-06-paid-proof.md (account id OK; no digests/secrets).
4. Demonstrate assert_clean dual outcome on the real account (ACCEPT-04): clean→0 after destroy; leftover→non-zero when a resource is deliberately left then cleaned. Prefer the safest leftover (tagged resource you can destroy immediately). Always finish with destroy + clean account.
5. Update docs citations (aws-acceptance.md, capability-matrix evidence notes, release-readiness if it tracks acceptance) from harness evidence dates — do not invent cell PASS rows by hand.

Serial host commands only; abort if the machine is under memory pressure (AGENTS.md). Profile must remain preview unless ALLOW_COSTLY is explicitly set (do not set it in this plan).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md &amp;&amp; rg -n 'create-once|cell-update|resume|assert_clean' .planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/scratch/03-06-paid-proof.md</automated>
  </verify>
  <done>Scratch proof records create-once, resume, evidence sample, and both assert_clean outcomes; docs cite harness evidence; account left clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HUMAN_GATE → AWS billing | Spend only after explicit approval |
| Harness → AWS account | Real mutate APIs; tag-scoped cleanup required |
| Evidence files → git/docs | Cite samples; do not commit secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Elevation of privilege / Abuse | live aws-acceptance-local.sh | critical | mitigate | Blocking HUMAN_GATE before create; preview profile; free-tier cell allowlist; KEEP documented; destroy+assert_clean at end |
| T-03-14 | Repudiation | matrix-results / scratch proof | medium | mitigate | Harness append only; scratch cites harness output; forbid hand-typed PASS invention for SC proof |
| T-03-15 | Information disclosure | digest / cert identity in docs | medium | mitigate | Evidence columns exclude digests; scratch redacts secrets |
| T-03-16 | Denial of service | orphan spend after kill | high | mitigate | Resume+lock hygiene; mandatory destroy+assert_clean before plan close |
| T-03-SC | Tampering | package installs | low | accept | No new packages on paid path |
</threat_model>

<verification>
HUMAN_GATE recorded; scratch proof complete; account assert_clean ok after final destroy.
</verification>

<success_criteria>
Live ACCEPT-01..04 truths hold on one free-tier preview stack; Phase 3 paid pass closed.
</success_criteria>

<output>
Create `.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-06-SUMMARY.md` when done
</output>
