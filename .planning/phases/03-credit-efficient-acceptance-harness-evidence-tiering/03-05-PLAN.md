---
phase: 03-credit-efficient-acceptance-harness-evidence-tiering
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - scripts/gcp-acceptance-local.sh
  - scripts/acceptance/lib-checkpoint.sh
  - scripts/acceptance/lib-evidence.sh
  - docs/gcp-acceptance.md
  - tests/acceptance/gcp_harness_shape_test.sh
autonomous: true
requirements:
  - ACCEPT-05

must_haves:
  truths:
    - "GCP acceptance harness shares checkpoint/resume and evidence-append APIs with AWS (D-02, ACCEPT-05)"
    - "Phase 3 exercises GCP via preview/dry-run (or structural force_clean/assert_clean shape tests) without gcloud/Pulumi resource create that spends credits (D-02, CONTEXT deferred live GCP)"
    - "Docs describe PSA soak + force_clean teardown path as part of the same harness shape, verified structurally offline (ACCEPT-05, ROADMAP SC#4)"
  artifacts:
    - "scripts/gcp-acceptance-local.sh sourcing shared checkpoint/evidence libs (and dry-run/preview-only Phase 3 path)"
    - "tests/acceptance/gcp_harness_shape_test.sh proving resume/evidence + force_clean/assert_clean structure without up"
    - "docs/gcp-acceptance.md updated for shared harness shape and Phase 3 spend fence"
  key_links:
    - "gcp-acceptance-local.sh → lib-checkpoint + lib-evidence → preview mode"
    - "force_clean_orphans + assert_clean remain EXIT path; live up deferred to Phase 7"
---

<objective>
Give GCP the same acceptance harness shape as AWS — resume, evidence, destroy/assert_clean/force_clean — and exercise it offline via preview/dry-run only (ACCEPT-05, D-02). Live GCP spend stays Phase 7.

Purpose: Phase 7 certification must not invent harness UX; the shape is proven here without credits.
Output: Shared-lib wiring in gcp script, offline shape tests, docs spend fence.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-CONTEXT.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-RESEARCH.md
@scripts/gcp-acceptance-local.sh
@scripts/acceptance/lib-checkpoint.sh
@scripts/acceptance/lib-evidence.sh
@docs/gcp-acceptance.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end GCP dry-run/preview path sources shared checkpoint + evidence</name>
  <files>scripts/gcp-acceptance-local.sh, docs/gcp-acceptance.md, tests/acceptance/gcp_harness_shape_test.sh</files>
  <action>
Per ACCEPT-05 / D-02 / research A4: evolve `scripts/gcp-acceptance-local.sh` so the maintainer-facing control flow matches AWS harness shape for resume and evidence without performing a spending `up`.

1. Source `scripts/acceptance/lib-checkpoint.sh` and `lib-evidence.sh`. Use a GCP-scoped checkpoint/evidence path under `.magelift/gcp-matrix/` (existing evidence location cited in capability-matrix) so AWS and GCP runs do not clobber each other.
2. Ensure `preview` mode (existing script mode) remains the Phase 3 default exercise path: validate/doctor/login/preview structure may run when credentials exist, but this plan's automated verify must not require a paid create. Prefer a `MAGELIFT_ACCEPTANCE_DRY_RUN=1` (or GCP-specific dry-run) branch that walks cell/shape steps, records checkpoint + evidence rows, and exits 0 without `up`.
3. Add `tests/acceptance/gcp_harness_shape_test.sh` that runs dry-run, asserts shared helpers were used (checkpoint file + evidence row written), and asserts the script still defines `force_clean_orphans`, PSA soak messaging, and `assert_clean` (structural grep/source checks).
4. Update `docs/gcp-acceptance.md`: shared harness shape, checkpoint/evidence paths, Phase 3 = preview/dry-run only, live create + PSA soak timing = Phase 7. Do not run `up` in this plan.

Do not spend GCP credits. Do not implement kube day-2 beyond harness needs (CONTEXT OUT).
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/gcp_harness_shape_test.sh</automated>
  </verify>
  <done>GCP dry-run writes checkpoint+evidence via shared libs; force_clean/assert_clean symbols still present.</done>
</task>

<task type="auto">
  <name>Document and test force_clean/assert_clean shape parity with AWS EXIT contract</name>
  <files>scripts/gcp-acceptance-local.sh, tests/acceptance/gcp_harness_shape_test.sh, docs/gcp-acceptance.md</files>
  <action>
Per ACCEPT-05 / ROADMAP SC#4: harden the shape test so EXIT cleanup still calls destroy (when created) then force_clean_orphans fallback then assert_clean — without executing live gcloud mutates in dry-run. Assert dry-run never sets `created=1` via a real up.

Keep PSA soak seconds / FORCE_CLEAN_TIMEOUT behavior intact for live Phase 7; dry-run may short-circuit soaks. Align evidence columns with AWS (cell, result, duration, provider, account, date) using provider=gcp.

If lib-checkpoint/evidence need a small provider-agnostic tweak (e.g. configurable paths), edit those shared libs carefully without breaking 03-01 AWS dry-run tests — re-run AWS acceptance shell tests after.
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/gcp_harness_shape_test.sh &amp;&amp; MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/checkpoint_resume_test.sh &amp;&amp; bash tests/acceptance/evidence_append_test.sh</automated>
  </verify>
  <done>GCP shape test green; AWS dry-run tests still green after any shared-lib tweaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 GCP harness → billing account | preview/dry-run must not cross into spending up |
| Shared libs → AWS/GCP evidence files | Paths must not clobber across providers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Elevation of privilege / Abuse | gcp-acceptance-local.sh up | high | mitigate | Phase 3 dry-run/preview only; docs + test assert no created=1 via up; live spend = Phase 7 |
| T-03-12 | Tampering | shared evidence paths | medium | mitigate | GCP evidence under .magelift/gcp-matrix/; AWS under .magelift/matrix-results.md |
| T-03-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
GCP harness shape test green offline; AWS dry-run regressions green.
</verification>

<success_criteria>
ACCEPT-05 shape holds without GCP spend; Phase 7 can reuse the same script for live proof.
</success_criteria>

<output>
Create `.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-05-SUMMARY.md` when done
</output>
