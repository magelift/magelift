---
phase: 03-credit-efficient-acceptance-harness-evidence-tiering
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - tests/floci/ecs_test.go
  - tests/floci/operations_test.go
  - tests/floci/state_test.go
  - tests/floci/storage_test.go
  - internal/cloud/aws/ops/day2_test.go
  - docs/capability-matrix.md
  - tests/acceptance/port_coverage_floci_gate_test.sh
autonomous: true
requirements:
  - ACCEPT-06

must_haves:
  truths:
    - "Every day-2 port marked mockable (Floci or unit-fake) in the port-coverage table has an automated test that passes (D-02, ACCEPT-06)"
    - "make floci-test is green for all Floci-marked rows after gap fill (ACCEPT-06, ROADMAP SC#5)"
    - "Ports marked paid-only (e.g. live ExecuteCommand / OIDC) remain unpaid and are not claimed as Floci-certified (D-03)"
  artifacts:
    - "Floci and/or unit-fake tests covering every mockable port-coverage row"
    - "tests/acceptance/port_coverage_floci_gate_test.sh (or equivalent) asserting mockable rows map to test symbols"
    - "Updated port-coverage notes if a gap is closed or confirmed paid-only"
  key_links:
    - "port-coverage mockable row → Floci test or day2 unit fake → make floci-test / go test"
---

<objective>
Close ACCEPT-06 offline: every port the matrix marks mockable is covered by Floci or unit fakes; paid-only stays honest (D-02, D-03).

Purpose: Paid AWS pass must buy only what mocks cannot prove.
Output: Gap-fill tests + green `make floci-test` for Floci rows + gate that ties the table to tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-CONTEXT.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-RESEARCH.md
@docs/capability-matrix.md
@tests/floci/
@internal/cloud/aws/ops/day2.go
@internal/cloud/aws/ops/day2_test.go
@scripts/floci-test.sh
@Makefile
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end: one previously under-covered mockable port → failing gate then green test</name>
  <files>docs/capability-matrix.md, tests/acceptance/port_coverage_floci_gate_test.sh, tests/floci/ecs_test.go, internal/cloud/aws/ops/day2_test.go</files>
  <action>
Per ACCEPT-06 / D-02: read the port-coverage table from plan 03-03. Identify the first mockable row that lacks a corresponding Floci or unit-fake test (likely PrepareExec/SelectTask unit-fake per research A2, or any Floci gap vs Bootstrap/State/Secrets/Logs/Health already present).

1. Add `tests/acceptance/port_coverage_floci_gate_test.sh` that parses or explicitly lists mockable rows from the matrix and asserts each maps to a known test function name (fail closed on missing). Start with at least the gap row plus existing covered ports.
2. Implement the missing coverage for that one port: prefer Floci if the API is supported on pin 1.5.33; otherwise unit-fake SelectTask/PrepareExec in `internal/cloud/aws/ops/day2_test.go` and keep the matrix row as unit-fake (not Floci). Do not invent Floci support for IAM OIDC / live ExecuteCommand — mark/retain paid-only (D-03, research Q3).
3. Run serially only (`GOMAXPROCS=1 GOFLAGS=-p=1`); never parallel builds under Cursor (AGENTS.md).

Do not start AWS create (D-01). Do not change paid-only rows to Floci without proof.
  </action>
  <verify>
    <automated>bash tests/acceptance/port_coverage_floci_gate_test.sh</automated>
  </verify>
  <done>Gate exists; at least one prior gap row now maps to a passing test symbol.</done>
</task>

<task type="auto" tdd="true">
  <name>Fill remaining mockable Floci/unit gaps; make floci-test green</name>
  <files>tests/floci/ecs_test.go, tests/floci/operations_test.go, tests/floci/state_test.go, tests/floci/storage_test.go, internal/cloud/aws/ops/day2_test.go, docs/capability-matrix.md, tests/acceptance/port_coverage_floci_gate_test.sh</files>
  <behavior>
    - Every port-coverage row with evidence Floci or unit-fake has a matching automated test
    - make floci-test exits 0 for Floci-tagged coverage
    - paid-only rows are listed in the gate as exempt, not as Floci
  </behavior>
  <action>
Close all remaining mockable gaps called out by the port-coverage table and gate. Reuse existing Floci patterns in `tests/floci/*_test.go` (compose via `make floci-test` / `scripts/floci-test.sh`). For graph-only composition claims already covered by Pulumi WithMocks elsewhere, cite them in the matrix notes rather than duplicating live Floci.

Update port-coverage notes only when evidence class changes (gap closed or confirmed paid-only). Expand the gate script so it fails if any mockable row lacks a test mapping.

Serial execution only. If Docker/Floci is unavailable, stop and report — do not silently drop ACCEPT-06.
  </action>
  <verify>
    <automated>bash tests/acceptance/port_coverage_floci_gate_test.sh &amp;&amp; make floci-test</automated>
  </verify>
  <done>All mockable ports covered; floci-test green; paid-only not over-claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Floci emulator → AWS API contract claims | Emulator proof ≠ IAM/OIDC/live exec certification |
| Unit fakes → production Observe path | Fakes prove control flow only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Spoofing | Floci coverage claims | high | mitigate | Port table + gate; paid-only for ExecuteCommand/OIDC until proven; under-claim preferred |
| T-03-10 | Denial of service | parallel floci/go test on 16GB | high | mitigate | Serial make floci-test; GOMAXPROCS=1; AGENTS.md |
| T-03-SC | Tampering | package installs | low | accept | No new modules; existing Floci digest pin |
</threat_model>

<verification>
port_coverage_floci_gate_test.sh and make floci-test both green.
</verification>

<success_criteria>
ACCEPT-06 offline coverage holds; paid pass only needed for paid-only ports.
</success_criteria>

<output>
Create `.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-04-SUMMARY.md` when done
</output>
