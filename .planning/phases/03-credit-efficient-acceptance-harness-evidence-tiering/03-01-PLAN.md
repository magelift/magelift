---
phase: 03-credit-efficient-acceptance-harness-evidence-tiering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/acceptance/lib-checkpoint.sh
  - scripts/acceptance/lib-evidence.sh
  - scripts/acceptance/cells-aws-preview.txt
  - scripts/aws-acceptance-local.sh
  - tests/acceptance/checkpoint_resume_test.sh
  - tests/acceptance/evidence_append_test.sh
  - docs/aws-acceptance.md
  - Makefile
autonomous: true
requirements:
  - ACCEPT-01
  - ACCEPT-02
  - ACCEPT-03

must_haves:
  truths:
    - "Offline dry-run iterates an ordered cell catalog against one logical stack and logs create-once then update-only cell steps — never a re-create between cells (D-02, ACCEPT-01)"
    - "Re-invoking the harness with a checkpoint that already recorded earlier cells skips those IDs and continues at the first incomplete cell (D-02, ACCEPT-02)"
    - "After each cell, the harness appends one markdown table row with cell, result, duration, provider, account, and date — no maintainer transcription (D-02, ACCEPT-03)"
  artifacts:
    - "scripts/acceptance/lib-checkpoint.sh with load/save/skip helpers and JSON under .magelift/acceptance-checkpoint.json"
    - "scripts/acceptance/lib-evidence.sh with append_row writing the six ROADMAP SC#2 columns"
    - "scripts/acceptance/cells-aws-preview.txt listing ≥3 free-tier preview cells (queueMode db, ecs-rabbitmq, ecs-artemis per research open Q2)"
    - "tests/acceptance/checkpoint_resume_test.sh and evidence_append_test.sh exiting 0"
  key_links:
    - "aws-acceptance-local.sh cell loop → lib-checkpoint skip → lib-evidence append → DRY_RUN fixture path"
    - "MAGELIFT_ACCEPTANCE_DRY_RUN=1 (or equivalent) prevents any Pulumi/AWS create in this plan"
---

<objective>
Ship the offline harness core: cell catalog, checkpoint/resume, and automatic evidence append with a fixture/dry-run mode so ACCEPT-01..03 are proven without cloud spend (D-02).

Purpose: Paid AWS proof (03-06 / D-01) is worthless if resume and evidence do not already work offline.
Output: Shared `scripts/acceptance/` libs, cell list, dry-run-capable AWS acceptance script, and automated offline tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-CONTEXT.md
@.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-RESEARCH.md
@scripts/aws-acceptance-local.sh
@docs/aws-acceptance.md
@Makefile
</context>

<tasks>

<task type="tracer">
  <name>End-to-end dry-run: one cell through checkpoint skip + evidence append</name>
  <files>scripts/acceptance/lib-checkpoint.sh, scripts/acceptance/lib-evidence.sh, scripts/acceptance/cells-aws-preview.txt, scripts/aws-acceptance-local.sh, tests/acceptance/checkpoint_resume_test.sh</files>
  <action>
Per D-02 and research Pattern 1/2: create the thinnest vertical slice that proves harness plumbing without AWS spend.

1. Add `scripts/acceptance/lib-checkpoint.sh` exporting helpers to load/save `.magelift/acceptance-checkpoint.json` (JSON map of cell ID → result/at per research A1) and `cell_done` / `record_cell` used by the driver.
2. Add `scripts/acceptance/lib-evidence.sh` exporting `append_row` that appends one markdown table row to `.magelift/matrix-results.md` with columns exactly: cell | result | duration | provider | account | date (ACCEPT-03 / ROADMAP SC#2). Create the file + header if missing. Do not write secrets or digests into evidence columns.
3. Add `scripts/acceptance/cells-aws-preview.txt` with at least three free-tier preview cell IDs (lock research Q2 set: `queueMode:db`, `queueMode:ecs-rabbitmq`, `queueMode:ecs-artemis` — exclude Aurora apply, amazon-mq apply, OpenSearch apply).
4. Evolve `scripts/aws-acceptance-local.sh` so when `MAGELIFT_ACCEPTANCE_DRY_RUN=1` (name may match research `MAGELIFT_*_DRY_RUN`; pick one and document it): source the libs, iterate the cell catalog, skip completed checkpoint cells, simulate create-once then per-cell update steps in the log (printf markers such as `acceptance create-once` then `acceptance cell-update`), call `append_row` and `record_cell` after each cell, and never invoke real `magelift preview/promote/deploy/destroy` or aws mutate APIs. KEEP semantics stay for live mode later; dry-run must not spend.
5. Add `tests/acceptance/checkpoint_resume_test.sh` that seeds a checkpoint with cell 1 complete, runs the dry-run harness, and asserts cell 1 was skipped and cell 2 executed (ACCEPT-02).

Do not extract assert_clean (plan 03-02). Do not call live AWS. Do not reopen Phase 1 hosted CI (D-04).
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/checkpoint_resume_test.sh</automated>
  </verify>
  <done>Dry-run path sources checkpoint+evidence libs, skips a recorded cell, and exits 0 under the resume test.</done>
</task>

<task type="auto" tdd="true">
  <name>Evidence append unit test + multi-cell dry-run create-once log contract</name>
  <files>tests/acceptance/evidence_append_test.sh, scripts/aws-acceptance-local.sh, docs/aws-acceptance.md, Makefile</files>
  <behavior>
    - append_row writes a row containing all six columns (cell, result, duration, provider, account, date)
    - Two successive appends yield two data rows (header once)
    - Dry-run over ≥3 cells logs one create-once marker and ≥2 cell-update markers with zero destroy-between-cells
  </behavior>
  <action>
Per ACCEPT-01 and ACCEPT-03: expand the dry-run driver so a full catalog pass (≥3 cells) logs a single create-once followed by update-only cell steps (no stack destroy between cells). KEEP remains the live long-lived pattern; dry-run must assert the same control flow in log markers.

Add `tests/acceptance/evidence_append_test.sh` covering the `<behavior>` cases against `lib-evidence.sh` (temp dir for `.magelift/`). Wire a Makefile target (e.g. `acceptance-harness-test`) that runs both acceptance shell tests serially — no parallel go builds (AGENTS.md).

Update `docs/aws-acceptance.md` with: dry-run env var, checkpoint path, evidence path, cell catalog location, and resume instructions (including lock hygiene note from research Pitfall 4 — unlock only when account empty). Do not claim live multi-cell proof yet (that is 03-06 / D-01).
  </action>
  <verify>
    <automated>bash tests/acceptance/evidence_append_test.sh &amp;&amp; MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/checkpoint_resume_test.sh</automated>
  </verify>
  <done>Evidence test green; dry-run multi-cell log shows create-once then updates; docs describe resume + dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer shell → `.magelift/` evidence/checkpoint | Local untrusted filesystem; harness is sole writer for SC proof |
| Dry-run env → real AWS/Pulumi | Must not cross into mutate when DRY_RUN set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of privilege | aws-acceptance-local.sh | high | mitigate | DRY_RUN short-circuits before preview/promote/deploy; refuse costly profile without ALLOW_COSTLY (existing) |
| T-03-02 | Repudiation | matrix-results.md | medium | mitigate | Only append_row writes new evidence rows; columns exclude secrets/digests |
| T-03-03 | Tampering | acceptance-checkpoint.json | medium | mitigate | Checkpoint is local gitignored state; resume skips only recorded IDs; docs warn against hand-editing for SC proof |
| T-03-SC | Tampering | package installs | low | accept | No new Go modules or npm/pip/cargo packages in this plan |
</threat_model>

<verification>
Offline dry-run + both acceptance shell tests pass; no AWS create invoked.
</verification>

<success_criteria>
ACCEPT-01..03 offline halves hold under dry-run; live create-once proof deferred to 03-06.
</success_criteria>

<output>
Create `.planning/phases/03-credit-efficient-acceptance-harness-evidence-tiering/03-01-SUMMARY.md` when done
</output>
