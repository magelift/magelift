---
phase: 07-gcp-certification
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/cutover-dns-cloudflare.sh
  - scripts/acceptance/cutover-dns-cloudflare_test.sh
  - docs/migrating-from-paas.md
  - .planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md
autonomous: true
requirements:
  - MIGRATE-04

must_haves:
  truths:
    - "scripts/cutover-dns-cloudflare.sh upserts CNAME or A for MAGELIFT_CUTOVER_HOST via Cloudflare API using CLOUDFLARE_API_TOKEN or CF_API_TOKEN (D-04)"
    - "Script --cleanup deletes the preview record; --dry-run / offline self-test works without stack spend"
    - "Docs forbid Wrangler OAuth for DNS writes (zone:read insufficient)"
  artifacts:
    - "scripts/cutover-dns-cloudflare.sh"
    - "scripts/acceptance/cutover-dns-cloudflare_test.sh (mocked curl or dry-run)"
    - "Runbook pointer to preferred host magelift-preview.alexandrecourtiol.com"
  key_links:
    - "Harness cutover:dns cell → script with applicationURL / LB export"
    - "Token Zone.DNS Edit → API v4 DNS records CRUD"
---

<objective>
Deliver the Cloudflare DNS cutover script for MIGRATE-04 Phase 7 half (D-04) with offline-testable dry-run/self-test.

Purpose: Wrangler OAuth cannot write DNS; rehearse cutover reproducibly and clean up after the paid pass.
Output: cutover-dns-cloudflare.sh + shell test + docs pointer.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gcp-certification/07-CONTEXT.md
@.planning/phases/07-gcp-certification/07-RESEARCH.md
@.planning/phases/06-shared-kubernetes-day-2/06-PHASE7-HANDOFF.md
@docs/migrating-from-paas.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end Cloudflare DNS upsert + cleanup script</name>
  <files>scripts/cutover-dns-cloudflare.sh, docs/migrating-from-paas.md</files>
  <action>
Per D-04 / MIGRATE-04 DNS: implement scripts/cutover-dns-cloudflare.sh.

1. Auth: require CLOUDFLARE_API_TOKEN or CF_API_TOKEN (Bearer). Resolve zone ID for alexandrecourtiol.com (fallback acourtiol.com). Default MAGELIFT_CUTOVER_HOST=magelift-preview.alexandrecourtiol.com.
2. Upsert: if TARGET looks like hostname → CNAME; if IPv4 → A; TTL short (e.g. 120); proxied false unless flagged. Support --dry-run that prints intended API calls without mutating. Support --cleanup to DELETE the record by name.
3. Document in migrating-from-paas.md cutover DNS section: API token Zone.DNS Edit required; Wrangler OAuth insufficient. Cross-link 06-PHASE7-HANDOFF.md. No Go SDK. No cloud spend in this plan (live API only if operator already exported token for optional manual self-test — not required for verify).
  </action>
  <verify>
    <automated>test -x scripts/cutover-dns-cloudflare.sh || chmod +x scripts/cutover-dns-cloudflare.sh; MAGELIFT_CUTOVER_HOST=magelift-preview.alexandrecourtiol.com TARGET=example.invalid ./scripts/cutover-dns-cloudflare.sh --dry-run && rg -n 'CLOUDFLARE_API_TOKEN|CF_API_TOKEN|Zone.DNS|Wrangler|magelift-preview.alexandrecourtiol.com' scripts/cutover-dns-cloudflare.sh docs/migrating-from-paas.md</automated>
  </verify>
  <done>Dry-run upsert path works; docs name token scope and preferred host.</done>
</task>

<task type="auto">
  <name>Offline shell self-test with mocked curl</name>
  <files>scripts/acceptance/cutover-dns-cloudflare_test.sh</files>
  <action>
Per D-04: add a shell test that stubs curl (PATH wrapper or CURL_BIN) to capture create/update/delete payloads and asserts zone resolve + upsert + --cleanup sequences without calling production Cloudflare. Exit non-zero if token env missing when not in dry-run/mock mode. Serial bash only.
  </action>
  <verify>
    <automated>bash scripts/acceptance/cutover-dns-cloudflare_test.sh</automated>
  </verify>
  <done>Mocked DNS script test green without real token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare API token → DNS zone | Token can mutate customer DNS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Tampering | DNS upsert | high | mitigate | Zone-scoped token; short TTL; --cleanup on harness EXIT |
| T-07-10 | Elevation of Privilege | Over-broad token | medium | mitigate | Document Zone.DNS Edit only; refuse Wrangler OAuth |
| T-07-SC | Tampering | package installs | low | accept | curl + shell only |
</threat_model>

<verification>
Dry-run + mocked shell test green.
</verification>

<success_criteria>
MIGRATE-04 DNS tooling ready for live cell; offline verifiable.
</success_criteria>

<output>
Create `.planning/phases/07-gcp-certification/07-05-SUMMARY.md` when done
</output>
