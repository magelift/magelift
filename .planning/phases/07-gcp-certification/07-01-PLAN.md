---
phase: 07-gcp-certification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cloud/gcp/bootstrap/bootstrap.go
  - internal/cloud/gcp/bootstrap/identity.go
  - internal/cloud/gcp/bootstrap/identity_test.go
  - internal/cloud/gcp/bootstrap/wif_client.go
  - internal/cloud/gcp/secrets/secrets.go
  - internal/cloud/gcp/secrets/secrets_test.go
  - internal/cloud/gcp/ops/day2.go
  - internal/cli/build.go
  - internal/cli/build_test.go
  - .github/workflows/gcp-wif-act-smoke.yml
  - docs/gcp-experimental.md
autonomous: true
requirements:
  - GCP-01
  - GCP-02

must_haves:
  truths:
    - "Bootstrap BuildPlan/Ensure provisions WIF pool + GitHub OIDC provider + CI SA binding and no longer reports wif deferred (D-05, GCP-01)"
    - "Composer gcp-secret-manager:// references resolve via Secret Manager AccessSecretVersion through the same loud-failure path as AWS (D-06 honesty, GCP-02)"
    - "Act-targeted workflow or documented gcloud WIF exchange proves token exchange without SA keys in repo secrets (D-05)"
  artifacts:
    - "internal/cloud/gcp/bootstrap/identity.go (or equivalent) with WIF Ensure + fake-client unit tests"
    - "secrets Store GetSecretValue / AccessSecretVersion adapter + CLI Composer GCP case"
    - ".github/workflows/gcp-wif-act-smoke.yml (Act-only) or docs section for gcloud STS exchange"
  key_links:
    - "ops Module.Bootstrap Details.wif → real provider resource name (not deferred)"
    - "loadComposerCredentials GCPSecretManager → secretref.Resolver.GCPSecretManager.GetSecretValue"
    - "WIF attribute condition repository == acourtiol/magelift"
---

<objective>
Ship offline WIF bootstrap and Composer Secret Manager resolution so the live pass never invents identity or secrets mid-spend (D-01, D-05, GCP-01, GCP-02).

Purpose: Close the two cloud-shaped gaps that block SC1–SC2 before any create-once.
Output: WIF Ensure + unit tests, SM Get wired for Composer, Act/docs CI proof path, Bootstrap Details without deferred WIF.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gcp-certification/07-CONTEXT.md
@.planning/phases/07-gcp-certification/07-RESEARCH.md
@internal/cloud/aws/bootstrap/identity.go
@internal/cloud/gcp/bootstrap/bootstrap.go
@internal/cloud/gcp/secrets/secrets.go
@internal/cli/build.go
@internal/secretref/secretref.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end WIF plan+Ensure + Composer SM Get</name>
  <files>internal/cloud/gcp/bootstrap/identity.go, internal/cloud/gcp/bootstrap/wif_client.go, internal/cloud/gcp/bootstrap/identity_test.go, internal/cloud/gcp/bootstrap/bootstrap.go, internal/cloud/gcp/secrets/secrets.go, internal/cloud/gcp/secrets/secrets_test.go, internal/cloud/gcp/ops/day2.go, internal/cli/build.go, internal/cli/build_test.go</files>
  <reversibility rating="costly">WIF pools persist until cleaned; Composer SM read becomes part of certified build path (D-05, D-06).</reversibility>
  <action>
Per D-05 / GCP-01 and D-06 honesty / GCP-02: extend GCP bootstrap beyond GCS DIY.

1. Add WIF BuildPlan fields + Ensure (mirror AWS identity shape): workload identity pool, OIDC provider for https://token.actions.githubusercontent.com, attribute mapping attribute.repository=assertion.repository, attribute condition assertion.repository == 'acourtiol/magelift', CI service account + roles/iam.workloadIdentityUser binding. Keep GCS DIY Ensure intact (D-01 project digital-lab-341608 is live-pass config only — code stays project-parameterized). Use injectable IAM/WIF client interfaces for unit tests; no live Ensure in this plan.
2. Wire Module.Bootstrap Details so wif is the provider resource name (or structured map with pool/provider/sa), never the string deferred. Update package comment that claimed WIF deferred.
3. Add secrets API GetSecretValue via AccessSecretVersion on the existing SM client; implement secretref.GCPSecretManagerProvider adapter. In loadComposerCredentials case GCPSecretManager, construct provider and call resolveComposerCredentials through Resolver with GCPSecretManager set — same validation as AWS (empty/malformed fail with error). Add unit tests: fake SM returns payload; missing secret/errors propagate; Composer GCP path no longer returns the not-implemented message for a working provider injection.
4. Serial only: GOMAXPROCS=1 GOFLAGS=-p=1. No new registry packages (use cloud.google.com/go/secretmanager already in go.mod; IAM via google.golang.org/api/iam or existing google APIs already depended — prefer existing modules; if a new Go module is required, stop and re-run Package Legitimacy before go get).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/gcp/bootstrap/ ./internal/cloud/gcp/secrets/ ./internal/cli/ -count=1 -run 'WIF|Identity|Bootstrap|Composer|Secret|GCP' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/gcp/ops/ -count=1 -run 'Bootstrap|Cost|Unsupported' && (rg -n 'wif.*deferred|"wif": "deferred"' internal/cloud/gcp/ops/day2.go internal/cloud/gcp/bootstrap/ | grep -v '^#' | grep -c deferred || true) | awk 'NR==1{if($1+0>0){print "deferred still present"; exit 1} print "no deferred wif in ops/bootstrap"}'</automated>
  </verify>
  <done>WIF Ensure unit-proven; Composer GCP SM resolves via AccessSecretVersion; Bootstrap Details do not report deferred WIF.</done>
</task>

<task type="auto">
  <name>Act-only WIF smoke workflow + docs exchange path</name>
  <files>.github/workflows/gcp-wif-act-smoke.yml, docs/gcp-experimental.md, docs/gcp-acceptance.md</files>
  <action>
Per D-05: add an Act-targeted workflow using google-github-actions/auth@v3 with workload_identity_provider and service_account placeholders filled from bootstrap outputs / env (document required secrets as WIF resource names, never credentials_json / SA JSON keys). permissions: contents: read, id-token: write. Document in docs/gcp-experimental.md or gcp-acceptance.md: (1) how to run via act locally, (2) fallback documented gcloud IAM Credentials / STS token exchange proving federation without hosted GH minutes, (3) matrix records Act-only until minutes return. Do not enable a hosted required check. Do not commit SA keys.
  </action>
  <verify>
    <automated>test -f .github/workflows/gcp-wif-act-smoke.yml && rg -n 'google-github-actions/auth@v3|id-token:\s*write|workload_identity_provider' .github/workflows/gcp-wif-act-smoke.yml && rg -n 'Act-only|act |WIF|workload.identity' docs/gcp-experimental.md docs/gcp-acceptance.md && ! rg -n 'credentials_json|private_key|BEGIN PRIVATE KEY' .github/workflows/gcp-wif-act-smoke.yml</automated>
  </verify>
  <done>Act smoke workflow + docs prove keyless path; no SA key material in workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub OIDC → GCP WIF | Untrusted CI claims cross into GCP IAM |
| Composer secretref → Secret Manager | Build process reads credential material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | WIF attribute condition | high | mitigate | Condition on exact repo acourtiol/magelift; no wildcard org |
| T-07-02 | Information Disclosure | SA keys in secrets | critical | mitigate | Refuse credentials_json for certification evidence; WIF only |
| T-07-03 | Information Disclosure | Composer SM payload | medium | mitigate | Never print auth JSON in CLI logs; reuse resolveComposerCredentials |
| T-07-04 | Spoofing | Bootstrap Ensure failures | medium | mitigate | Loud errors from fake/real clients; no silent skip of WIF |
| T-07-SC | Tampering | package installs | low | accept | No new registry packages on recommended path |
</threat_model>

<verification>
Serial bootstrap/secrets/cli tests green; deferred WIF gone from Bootstrap Details; Act workflow present without key material.
</verification>

<success_criteria>
GCP-01 and GCP-02 offline foundations ready for live exercise; zero cloud spend in this plan.
</success_criteria>

<output>
Create `.planning/phases/07-gcp-certification/07-01-SUMMARY.md` when done
</output>
