---
phase: 07-gcp-certification
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - internal/cloud/gcp/cost/estimate.go
  - internal/cloud/gcp/cost/estimate_test.go
  - internal/cloud/gcp/ops/day2.go
autonomous: true
requirements:
  - GCP-05

must_haves:
  truths:
    - "magelift cost against a GCP planned stack returns a CostReport with non-empty Estimated items in account-free mode (GCP-05, SC5)"
    - "Module.CostEstimator no longer returns unsupportedCost / ErrNotSupported for gke-autopilot (D-06 honesty)"
    - "Account-free notice states Catalog live is optional; no false live prices without --live"
  artifacts:
    - "internal/cloud/gcp/cost/estimate.go mirroring AWS account-free shape for GKE Autopilot cells"
    - "internal/cloud/gcp/cost/estimate_test.go"
    - "ops day2 CostEstimator() → gcp/cost.Estimator"
  key_links:
    - "platform.ModuleCostEstimator(gcp Module) → cost.Estimator.Estimate"
    - "CLI cost command → per-cell Estimated rows for preset/catalog capacity"
---

<objective>
Replace GCP unsupportedCost with an AWS-shaped CostEstimator so SC5 can be proven offline and on the live pass (GCP-05).

Purpose: Cost must not be ErrNotSupported when certification claims per-cell estimates.
Output: gcp/cost package + Module wire + unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gcp-certification/07-CONTEXT.md
@.planning/phases/07-gcp-certification/07-RESEARCH.md
@internal/cloud/aws/cost/estimate.go
@internal/cloud/gcp/ops/day2.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end account-free GCP CostEstimator</name>
  <files>internal/cloud/gcp/cost/estimate.go, internal/cloud/gcp/cost/estimate_test.go, internal/cloud/gcp/ops/day2.go</files>
  <action>
Per GCP-05 / RESEARCH A2: create internal/cloud/gcp/cost with Estimator.Estimate mirroring AWS account-free honesty for runtime gke-autopilot (and reject other runtimes loudly).

1. Account-free mode (opts.Live false): populate Estimated from magelift.yaml GCP catalog / preset capacity (GKE Autopilot web, Cloud SQL, Memorystore Valkey, edge/LB as configured); set Mode account-free; Notice that Catalog API is not called; Unsupported for usage-based SKUs. Do not claim current Google list prices.
2. Optional Live path: if implementing Catalog API, gate behind opts.Live and inject a PricingClient interface; otherwise return a clear error that --live Catalog is not wired yet (prefer account-free satisfying GCP-05). Do not add google.golang.org/api/cloudbilling without Package Legitimacy re-audit.
3. Wire Module.CostEstimator() to return cost.Estimator{}; delete or stop using unsupportedCost for Estimate. Keep serial GOMAXPROCS=1 GOFLAGS=-p=1. Depends on 07-01 only because both touch day2.go — preserve 07-01 Bootstrap Details when editing CostEstimator.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/gcp/cost/ ./internal/cloud/gcp/ops/ -count=1 -run 'Cost|Estimate|Unsupported' && GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cli/ -count=1 -run 'Cost' -short</automated>
  </verify>
  <done>Account-free CostReport has Estimated items for gke-autopilot; Module.CostEstimator is concrete.</done>
</task>

<task type="auto" tdd="true">
  <name>Pin account-free table + ErrNotSupported gone for GCP cost</name>
  <files>internal/cloud/gcp/cost/estimate_test.go, internal/cloud/gcp/ops/day2.go</files>
  <behavior>
    - Estimate with nil planned stack returns error
    - Estimate for gke-autopilot account-free returns Mode account-free and len(Estimated) greater than 0
    - Estimate for unsupported runtime returns error mentioning runtime
    - Module.CostEstimator().Estimate does not return platform.ErrNotSupported
  </behavior>
  <action>
Per GCP-05: expand table-driven tests covering preset empty vs configured catalog cells; assert Notice non-empty in account-free; assert unsupportedCost type removed or unused. Serial tests only.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/cloud/gcp/cost/ ./internal/cloud/gcp/ops/ -count=1 && ! rg -n 'unsupportedCost' internal/cloud/gcp/ops/day2.go</automated>
  </verify>
  <done>Cost tests green; unsupportedCost removed from day2.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cost report → operator spend decisions | Misleading prices could cause overspend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering / Repudiation | CostReport Mode | medium | mitigate | Explicit account-free Notice; no silent live prices |
| T-07-06 | Information Disclosure | Catalog API keys | low | accept | Account-free default; ADC only if live added later |
| T-07-SC | Tampering | package installs | low | accept | No new modules on account-free path |
</threat_model>

<verification>
gcp/cost + ops Cost tests green; CLI cost short path still healthy.
</verification>

<success_criteria>
GCP-05 offline: per-cell account-free estimates available for gke-autopilot.
</success_criteria>

<output>
Create `.planning/phases/07-gcp-certification/07-02-SUMMARY.md` when done
</output>
