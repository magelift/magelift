---
phase: 07-gcp-certification
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-04
  - 07-05
files_modified:
  - scripts/acceptance/cells-gcp-preview.txt
  - scripts/gcp-acceptance-local.sh
  - scripts/acceptance/lib-checkpoint.sh
  - scripts/acceptance/lib-evidence.sh
  - tests/acceptance/gcp_harness_shape_test.sh
  - docs/gcp-acceptance.md
autonomous: true
requirements:
  - GCP-03
  - GCP-04
  - GCP-05

must_haves:
  truths:
    - "cells-gcp-preview.txt lists resume-friendly cells for day2 logs/exec/secrets/state/health, deploy candidate, migrate dump, cost estimate, cutover dns (D-02, D-03, D-04)"
    - "Dry-run harness loops all cells with append_row and zero Pulumi create (ACCEPT shape)"
    - "Live path shape documents create-once then updates between cells; teardown via EXIT destroy + force_clean (D-02)"
  artifacts:
    - "Expanded scripts/acceptance/cells-gcp-preview.txt"
    - "live_cell_loop (or equivalent) in gcp-acceptance-local.sh"
    - "gcp_harness_shape_test.sh green in dry-run"
  key_links:
    - "checkpoint skip → append_row → .magelift/gcp-matrix/matrix-results.md"
    - "deploy cell → kube.Steps migrate→cutover→health→record"
    - "migrate:dump → kube dumpimport runner from 07-04"
    - "cutover:dns → scripts/cutover-dns-cloudflare.sh from 07-05"
---

<objective>
Expand the GCP acceptance harness cell catalog and live_cell_loop shape so one create-once pass can record SC3–SC5 evidence (D-02).

Purpose: Phase 3 dry-run catalog is only preset:preview; Phase 7 needs ordered cells before paid spend.
Output: cell catalog + live loop wiring + dry-run green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gcp-certification/07-CONTEXT.md
@.planning/phases/07-gcp-certification/07-RESEARCH.md
@scripts/gcp-acceptance-local.sh
@scripts/acceptance/cells-gcp-preview.txt
@scripts/acceptance/lib-checkpoint.sh
@scripts/acceptance/lib-evidence.sh
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end dry-run cell loop with full catalog</name>
  <files>scripts/acceptance/cells-gcp-preview.txt, scripts/gcp-acceptance-local.sh, tests/acceptance/gcp_harness_shape_test.sh, docs/gcp-acceptance.md</files>
  <action>
Per D-02 / RESEARCH Pattern 3 recommended cell order (discretion): expand cells-gcp-preview.txt beyond preset:preview to ordered cells:
1. bootstrap:wif (verify / no-op if already ensured)
2. day2:secrets, day2:state, day2:logs, day2:exec, day2:health
3. deploy:candidate (migrate→cutover→health→record)
4. migrate:dump (after deploy — D-03)
5. cost:estimate
6. cutover:dns (D-04)

Wire live_cell_loop for MAGELIFT_GCP_ACCEPTANCE=1 up path: after create-once, iterate cells with checkpoint skip, stack update (never recreate), append_row to .magelift/gcp-matrix/matrix-results.md (six-column evidence). Dump cell after first successful deploy; DNS after applicationURL exists. Document MAGELIFT_GCP_ACCEPTANCE_DIGEST must be pullable. Keep EXIT destroy + force_clean_orphans + PSA soak. Dry-run must not call gcloud up. Serial AGENTS.md: do not spawn parallel go builds from harness. Depends on 07-01/04/05 so cell commands reference real tools.
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/gcp_harness_shape_test.sh && rg -n 'day2:|deploy:candidate|migrate:dump|cost:estimate|cutover:dns' scripts/acceptance/cells-gcp-preview.txt && rg -n 'live_cell|append_row|force_clean' scripts/gcp-acceptance-local.sh</automated>
  </verify>
  <done>Dry-run green over expanded catalog; live loop + force_clean paths present in script.</done>
</task>

<task type="auto">
  <name>Checkpoint resume + evidence column contract</name>
  <files>scripts/gcp-acceptance-local.sh, scripts/acceptance/lib-checkpoint.sh, scripts/acceptance/lib-evidence.sh, docs/gcp-acceptance.md</files>
  <action>
Per D-02 / ACCEPT-02 pattern: ensure GCP harness uses shared checkpoint + evidence helpers (or GCP-prefixed copies) so killing mid-matrix resumes at first incomplete cell. Document evidence path .magelift/gcp-matrix/matrix-results.md and that certification (07-07) requires PASS rows for SC1–SC5 cells. Do not run live up in this plan.
  </action>
  <verify>
    <automated>MAGELIFT_ACCEPTANCE_DRY_RUN=1 bash tests/acceptance/gcp_harness_shape_test.sh && rg -n 'checkpoint|append_row|matrix-results' scripts/gcp-acceptance-local.sh scripts/acceptance/lib-checkpoint.sh scripts/acceptance/lib-evidence.sh docs/gcp-acceptance.md</automated>
  </verify>
  <done>Resume + evidence contract documented and dry-run proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → GCP project | Script can create/destroy paid resources when not dry-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Denial of Service (cost) | accidental live up | high | mitigate | Refuse without MAGELIFT_GCP_ACCEPTANCE=1; dry-run default for shape tests |
| T-07-12 | Tampering | hand-typed matrix rows | medium | mitigate | append_row only; refuse hand edits as certification evidence |
| T-07-SC | Tampering | package installs | low | accept | Shell/harness only |
</threat_model>

<verification>
gcp_harness_shape_test.sh dry-run green; catalog contains day2/deploy/dump/cost/dns cells.
</verification>

<success_criteria>
Harness ready for single create-once live pass; offline only in this plan.
</success_criteria>

<output>
Create `.planning/phases/07-gcp-certification/07-03-SUMMARY.md` when done
</output>
