---
phase: 07-gcp-certification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/dumpimport/import.go
  - internal/dumpimport/runner_kube.go
  - internal/dumpimport/runner_kube_test.go
  - internal/dumpimport/import_test.go
  - testdata/fixtures/migrate/tiny.sql
autonomous: true
requirements:
  - MIGRATE-04

must_haves:
  truths:
    - "Dump import can target Cloud SQL private IP via a kube-adjacent runner (kubectl exec / in-cluster mysql) instead of host 127.0.0.1 (D-03, MIGRATE-04 managed half)"
    - "Synthetic tiny.sql fixture imports and leaves tables queryable in unit/fake runner tests"
    - "Journal/status path still progresses to imported on success and failed with reason on runner error (MIGRATE-02 compatibility)"
  artifacts:
    - "internal/dumpimport/runner_kube.go (or equivalent Managed/Kube runner)"
    - "runner unit tests with fake exec"
    - "Harness-ready Options for private-IP host via runner"
  key_links:
    - "env create --dump / auto-import → dumpimport.Import with Kube runner"
    - "Cloud SQL Ipv4Enabled false → runner shares GKE VPC adjacency"
---

<objective>
Ship a VPC-adjacent dumpimport runner so the managed dump cell can succeed against private-IP Cloud SQL on the paid pass (D-03, MIGRATE-04).

Purpose: Host mysql to 10.x from the Mac cannot reach Cloud SQL; finish connectivity offline.
Output: Kube/exec runner + unit tests using tiny.sql fixture.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gcp-certification/07-CONTEXT.md
@.planning/phases/07-gcp-certification/07-RESEARCH.md
@internal/dumpimport/import.go
@testdata/fixtures/migrate/tiny.sql
@internal/cloud/gcp/database/database.go
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end kube-adjacent dumpimport runner</name>
  <files>internal/dumpimport/import.go, internal/dumpimport/runner_kube.go, internal/dumpimport/runner_kube_test.go</files>
  <action>
Per D-03 / RESEARCH Pattern 4 / MIGRATE-04 managed dump: Cloud SQL is private-IP-only (Ipv4Enabled false). Extend dumpimport with a Kube/Managed runner that pipes SQL via kubectl exec into the Magento web (or mysql-capable) pod, or an injectable Exec interface for tests.

1. Add Options fields for runner mode (host vs kube) — Host/Port remain for local compose; kube mode uses namespace, pod selector/label, container, and private SQL host/user/pass from stack outputs or env. Prefer kubectl exec piping tiny.sql over Cloud SQL Auth Proxy+IAP (discretion).
2. If Magento image may lack mysql client, document fallback Job/sidecar shape in code comment and implement the path that unit tests can fake (exec argv capture). Do not require live cluster in this plan.
3. Keep nonempty/--yes safety gates from Phase 5. Serial GOMAXPROCS=1 GOFLAGS=-p=1. No new Go modules.
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/dumpimport/ -count=1 -run 'Kube|Runner|Import|Tiny'</automated>
  </verify>
  <done>Kube runner unit path imports fixture via fake exec; host path still works for local MySQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Assert tables + failed-loud runner errors</name>
  <files>internal/dumpimport/runner_kube_test.go, internal/dumpimport/import_test.go</files>
  <behavior>
    - Fake kube exec receiving tiny.sql succeeds and verification query sees expected tables
    - Exec failure returns error (not silent success)
    - Host-mode defaults remain 127.0.0.1:3306 when kube unset
  </behavior>
  <action>
Per D-03: table-driven tests for runner success/failure; reuse testdata/fixtures/migrate/tiny.sql. Do not mark MIGRATE-04 Complete in REQUIREMENTS (that is 07-07 after live evidence).
  </action>
  <verify>
    <automated>GOMAXPROCS=1 GOFLAGS=-p=1 go test ./internal/dumpimport/ -count=1</automated>
  </verify>
  <done>dumpimport package tests green including kube runner cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator dump file → managed DB | SQL content executes with DB credentials |
| kubectl exec → pod | Cluster RBAC trusts harness kubeconfig |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | SQL fixture / dump path | high | mitigate | Keep Phase 5 nonempty/--yes refuse; synthetic fixture only in harness cell |
| T-07-08 | Information Disclosure | DB password in exec argv | medium | mitigate | Prefer env/stdin for password; do not log connection strings |
| T-07-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Full dumpimport package tests green offline.
</verification>

<success_criteria>
Managed dump connectivity path exists for Phase 7 live cell; zero cloud spend.
</success_criteria>

<output>
Create `.planning/phases/07-gcp-certification/07-04-SUMMARY.md` when done
</output>
