---
phase: 07-gcp-certification
plan: 06
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
  - 07-05
files_modified:
  - .magelift/gcp-matrix/matrix-results.md
  - .magelift/gcp-matrix/acceptance-checkpoint.json
  - .planning/phases/07-gcp-certification/scratch/07-06-live-pass.md
autonomous: false
requirements:
  - GCP-01
  - GCP-02
  - GCP-03
  - GCP-04
  - GCP-05
  - MIGRATE-04
user_setup:
  - service: gcloud
    why: "Interactive ADC refresh required before long Pulumi Up (ADC_FAIL observed)"
    env_vars: []
    dashboard_config:
      - task: "Run gcloud auth login and gcloud auth application-default login for digital-lab-341608"
        location: "Local terminal (interactive browser)"
  - service: cloudflare
    why: "Zone.DNS Edit token required for cutover:dns cell"
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare Dashboard → My Profile → API Tokens → Edit zone DNS (alexandrecourtiol.com and acourtiol.com)"
  - service: gcp-spend
    why: "Paid create-once GKE Autopilot pass (~19m create + PSA soak); destroy-when-done mandatory"
    env_vars:
      - name: MAGELIFT_GCP_ACCEPTANCE
        source: "Export MAGELIFT_GCP_ACCEPTANCE=1 for live up"
      - name: MAGELIFT_GCP_ACCEPTANCE_DIGEST
        source: "Pullable Magento OCI digest (not placeholder)"

must_haves:
  truths:
    - "Single create-once harness pass on digital-lab-341608 / europe-west1 / prefix mlgcpwt records PASS evidence for day2, deploy, dump, cost, dns cells (D-01, D-02, D-03, D-04)"
    - "EXIT destroy runs; force_clean_orphans with PSA soak; assert_clean exits 0; DNS record deleted (D-02)"
    - "No exploratory second create; serial builds only during pass (AGENTS.md)"
  artifacts:
    - ".magelift/gcp-matrix/matrix-results.md with harness-appended PASS rows"
    - "scratch/07-06-live-pass.md log of commands/outcomes including force_clean"
    - "DNS cleanup confirmation for magelift-preview.alexandrecourtiol.com"
  key_links:
    - "ADC → Pulumi Up → cells → EXIT destroy → force_clean → assert_clean"
    - "cutover:dns → Cloudflare API → --cleanup"
---

<objective>
Run the single paid GCP create-once pass with full cell evidence and teardown via force_clean + PSA soak (D-01, D-02, live GCP-01..05, MIGRATE-04).

Purpose: This is spend pass 2 of 3 — prove SC1–SC5 behaviors on real account without a second create.
Output: matrix-results.md PASS rows + live-pass scratch + clean account.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gcp-certification/07-CONTEXT.md
@.planning/phases/07-gcp-certification/07-RESEARCH.md
@docs/gcp-acceptance.md
@scripts/gcp-acceptance-local.sh
@AGENTS.md
@.cursor/rules/serial-builds-only.mdc
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Execute prereqs: ADC + Cloudflare token + destroy-when-done</name>
  <action>Refresh gcloud ADC, export Cloudflare Zone.DNS Edit token, and confirm destroy-when-done before paid create (E1–E3).</action>
  <instructions>
Offline plans 07-01..07-05 are complete; the harness is ready. Complete these three execute prerequisites (interactive / operator-held — agent cannot finish them non-interactively):

1. gcloud ADC refresh: run `gcloud auth login` and `gcloud auth application-default login` for project `digital-lab-341608`; confirm `gcloud auth application-default print-access-token` succeeds (clears ADC_FAIL). Prefer ADC over GOOGLE_OAUTH_ACCESS_TOKEN for long Up.
2. Cloudflare: export `CLOUDFLARE_API_TOKEN` (or `CF_API_TOKEN`) with Zone.DNS Edit on alexandrecourtiol.com and acourtiol.com. Optional: `./scripts/cutover-dns-cloudflare.sh --self-test` before spend.
3. Spend + destroy-when-done: confirm paid pass approved; leave `MAGELIFT_GCP_ACCEPTANCE_KEEP` unset/false; EXIT destroy + force_clean mandatory; set pullable `MAGELIFT_GCP_ACCEPTANCE_DIGEST`.

Do not start `./scripts/gcp-acceptance-local.sh up` until all three are green.
  </instructions>
  <verification>
    gcloud config get-value project prints digital-lab-341608; gcloud auth application-default print-access-token exits 0; test -n "${CLOUDFLARE_API_TOKEN:-${CF_API_TOKEN:-}}"; operator confirmed KEEP=false / destroy-when-done.
  </verification>
  <resume-signal>Type "prereqs green" (or list any blocker)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Authorize single paid create-once pass</name>
  <decision>Start the one paid GKE Autopilot create-once pass with mandatory destroy + force_clean (D-02)</decision>
  <context>
D-02 forbids exploratory second create. Spend is the ROADMAP pass 2 of 3 (~19m create + PSA soak). Auto/yolo may select option-a when prereqs green and destroy-when-done already confirmed in the prior checkpoint.
  </context>
  <options>
    <option id="option-a">
      <name>Proceed with single create-once + EXIT destroy + force_clean</name>
      <pros>Closes SC1–SC5 live evidence; matches spend map</pros>
      <cons>Real GCP cost until teardown completes</cons>
    </option>
    <option id="option-b">
      <name>Defer live pass; keep account idle</name>
      <pros>No spend</pros>
      <cons>Blocks GCP-06 / MIGRATE-04 Phase 7 half</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Live create-once pass + cells + force_clean</name>
  <files>.magelift/gcp-matrix/matrix-results.md, .magelift/gcp-matrix/acceptance-checkpoint.json, .planning/phases/07-gcp-certification/scratch/07-06-live-pass.md</files>
  <precondition>gcloud ADC print-access-token succeeds; CLOUDFLARE_API_TOKEN or CF_API_TOKEN set; MAGELIFT_GCP_ACCEPTANCE_DIGEST pullable; operator selected option-a destroy-when-done</precondition>
  <reversibility rating="costly">Paid GKE Autopilot create starts cloud spend; EXIT destroy + force_clean required (D-02).</reversibility>
  <action>
Per D-01, D-02, D-03, D-04 and GCP-01..05 live / MIGRATE-04:

1. Export MAGELIFT_GCP_ACCEPTANCE=1, MAGELIFT_GCP_PROJECT=digital-lab-341608, MAGELIFT_GCP_REGION=europe-west1, MAGELIFT_GCP_ACCEPTANCE_NAME=mlgcpwt, pullable DIGEST. KEEP must be false.
2. Run single `./scripts/gcp-acceptance-local.sh up` (or documented equivalent). One create only — no exploratory second up. Serial: no parallel go build/goreleaser during pass (AGENTS.md). If machine memory pressure rises, abort cleanly and let EXIT trap destroy.
3. Cells must cover: WIF/bootstrap proof, Composer SM write/read if in catalog, day2 logs/exec/secrets/state/health, deploy migrate→cutover→health→record, migrate:dump (kube runner + tiny.sql → journal imported), cost:estimate, cutover:dns to magelift-preview.alexandrecourtiol.com.
4. On EXIT: destroy → force_clean_orphans (producer wait + MAGELIFT_GCP_PSA_SOAK_SECS) → assert_clean → DNS --cleanup. Record outcomes in scratch/07-06-live-pass.md including clean-account confirmation (matrix PASS rows; assert_clean 0; DNS removed; no second create). Do not certify docs in this plan (07-07).
  </action>
  <verify>
    <automated>test -f .magelift/gcp-matrix/matrix-results.md && rg -n 'PASS' .magelift/gcp-matrix/matrix-results.md && test -f .planning/phases/07-gcp-certification/scratch/07-06-live-pass.md && rg -n 'force_clean|assert_clean|destroy' .planning/phases/07-gcp-certification/scratch/07-06-live-pass.md</automated>
  </verify>
  <done>Harness PASS rows recorded; destroy+force_clean+assert_clean+DNS cleanup evidenced in scratch; no second create.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → paid GCP project | Real spend and destroy |
| Harness → Cloudflare DNS | Mutates preview hostname |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Denial of Service (cost) | orphan resources | critical | mitigate | EXIT destroy + force_clean + PSA soak + assert_clean; KEEP=false |
| T-07-14 | Tampering | placeholder digest | high | mitigate | Require pullable MAGELIFT_GCP_ACCEPTANCE_DIGEST before up |
| T-07-15 | Spoofing | expired ADC mid-Up | high | mitigate | Prereq ADC refresh; refuse static OAuth for long create |
| T-07-16 | Information Disclosure | matrix secrets | medium | mitigate | Evidence rows without secret values |
| T-07-SC | Tampering | package installs | low | accept | No installs during live pass |
</threat_model>

<verification>
matrix-results.md has PASS evidence; scratch documents force_clean success; human confirms clean account.
</verification>

<success_criteria>
SC1–SC5 live behaviors evidenced on one create-once pass; account clean after force_clean.
</success_criteria>

<output>
Create `.planning/phases/07-gcp-certification/07-06-SUMMARY.md` when done
</output>
